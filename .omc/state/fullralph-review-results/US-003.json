{
  "storyId": "US-003",
  "engine": "codex",
  "mode": "review",
  "model": "(default)",
  "effort": "high",
  "codexHome": "/home/macha/.codex-dualralph",
  "cwd": "/mnt/d/workspace/fast-comment",
  "startedAt": "2026-05-29T16:57:57.549Z",
  "elapsedMs": 64951,
  "exitCode": 0,
  "verdict": "PASS",
  "reason": null,
  "lastMessageBytes": 646,
  "stdoutBytes": 647,
  "stderrBytes": 40517,
  "lastMessage": "ブロッキングな指摘はありません。\n\n`TtsDispatcher` が `BouyomiBackend` と `VoicevoxBackend` を保持する形になっており、`VoicevoxBackend` 内の `reqwest::Client` は `speak()` ごとの生成ではなく再利用されます。`update_config()` も VOICEVOX の URL / speaker が変わった場合だけ再構築するため、AC1 は満たしています。\n\nbase64 の RFC4648 ベクタと `+///` による標準アルファベット確認テストも追加済みです。`speak()` 失敗時の WebSpeech fallback は既存の `Err => emit_webspeech()` 経路が維持されています。`available()` も speak パスでは使わない用途コメントと `#[allow(dead_code)]` が入っています。\n\n確認:\n- `git diff --name-only -- src-tauri/src/tts` は `mod.rs` と `voicevox.rs` のみ\n- `git diff --check -- src-tauri/src/tts/...` は成功\n- `cargo test` は環境に `cargo` が無く実行不可でしたが、目視上この差分に型破綻は見当たりません\n\nFULLRALPH-VERDICT: PASS",
  "stdout": "ブロッキングな指摘はありません。\n\n`TtsDispatcher` が `BouyomiBackend` と `VoicevoxBackend` を保持する形になっており、`VoicevoxBackend` 内の `reqwest::Client` は `speak()` ごとの生成ではなく再利用されます。`update_config()` も VOICEVOX の URL / speaker が変わった場合だけ再構築するため、AC1 は満たしています。\n\nbase64 の RFC4648 ベクタと `+///` による標準アルファベット確認テストも追加済みです。`speak()` 失敗時の WebSpeech fallback は既存の `Err => emit_webspeech()` 経路が維持されています。`available()` も speak パスでは使わない用途コメントと `#[allow(dead_code)]` が入っています。\n\n確認:\n- `git diff --name-only -- src-tauri/src/tts` は `mod.rs` と `voicevox.rs` のみ\n- `git diff --check -- src-tauri/src/tts/...` は成功\n- `cargo test` は環境に `cargo` が無く実行不可でしたが、目視上この差分に型破綻は見当たりません\n\nFULLRALPH-VERDICT: PASS\n",
  "stderr": "Reading prompt from stdin...\nOpenAI Codex v0.128.0 (research preview)\n--------\nworkdir: /mnt/d/workspace/fast-comment\nmodel: gpt-5.5\nprovider: openai\napproval: never\nsandbox: read-only\nreasoning effort: high\nreasoning summaries: none\nsession id: 019e74ab-d0f1-7a81-90fe-51d0433284f3\n--------\nuser\n# FULLRALPH REVIEW for story US-003\n\n## Original task\nfast-comment SPEC完成度向上: 3レーン監査で検出したCRITICAL/HIGH/MEDIUM欠陥を修正し、MVP(P1-P5)を機能水準に引き上げる。WSLはビルド不可のため目視レビュー検証のみ。\n\n## Story US-003: TTS バックエンド再利用とフォールバック整備\nsrc-tauri/src/tts/ 配下の mod.rs, voicevox.rs, bouyomi.rs のみを編集。\n1) [効果大] mod.rs/voicevox.rs: speak 毎に Voicevox backend (reqwest::Client) を new していてコネクションプール(keep-alive)が効かず低遅延目標(SPEC §0)に反する。修正: TtsDispatcher 側で backend インスタンス（または reqwest::Client）を保持して再利用し、設定変更時のみ再構築する。bouyomi も backend 構造体は再利用する。\n2) voicevox.rs の自前base64実装に最低限の単体テスト(#[cfg(test)])を追加し、標準アルファベットでのエンコード正当性を保証（依存追加はしない）。\n3) available() がどこからも呼ばれずデッドコードになっている件: speak失敗時フォールバックの現挙動(TOCTOU回避)は維持しつつ、available() の用途（設定UIからの疎通テスト用）をコメント明記し #[allow(dead_code)] 等で警告抑制するか Tauri command から呼べるよう薄く公開する。実挙動(speak失敗→WebSpeechフォールバック)は壊さないこと。\ntts配下以外を変更しないこと。\n\n## Acceptance criteria (verify EACH)\n  1. Voicevox の reqwest::Client が speak 毎ではなく Dispatcher 保持で再利用され、設定変更時のみ再構築される\n  2. 自前base64に単体テストが追加され標準アルファベットのエンコードが検証されている\n  3. speak失敗時のWebSpeechフォールバック挙動が維持されている（回帰なし）\n  4. available() のデッドコード警告が解消されている（用途明記 or 公開）\n  5. tts/ 配下以外を変更していない\n\n## Files in scope (touchedFiles)\n  - src-tauri/src/tts/mod.rs\n  - src-tauri/src/tts/voicevox.rs\n  - src-tauri/src/tts/bouyomi.rs\n\n## Diff under review\n```diff\ndiff --git a/src-tauri/src/tts/mod.rs b/src-tauri/src/tts/mod.rs\nindex 2b27f1b..5cc12a5 100644\n--- a/src-tauri/src/tts/mod.rs\n+++ b/src-tauri/src/tts/mod.rs\n@@ -5,8 +5,8 @@\n //! - webspeech: 実再生は UI 側(`speechSynthesis`)。Rust は読み上げ対象テキストを\n //!   イベントで UI に渡すだけ。\n //!\n-//! ルーティング: 設定の優先バックエンドが `available()==false` なら Web Speech へ\n-//! フォールバックする。読み上げ整形(名前/URL/絵文字/長文)もここで行う。\n+//! ルーティング: 読み上げ時は優先バックエンドへ直接 `speak()` し、失敗したら\n+//! Web Speech へフォールバックする。読み上げ整形(名前/URL/絵文字/長文)もここで行う。\n \n pub mod bouyomi;\n pub mod voicevox;\n@@ -22,6 +22,10 @@ pub trait TtsBackend {\n     /// 整形済みテキストを読み上げる。\n     async fn speak(&self, text: String) -> anyhow::Result<()>;\n     /// バックエンドが現在利用可能か(接続可否の簡易判定)。\n+    ///\n+    /// speak パスでは TOCTOU と二重往復を避けるため呼ばない。設定 UI からの\n+    /// 疎通テスト用途として残す。\n+    #[allow(dead_code)]\n     async fn available(&self) -> bool;\n }\n \n@@ -31,18 +35,75 @@ pub trait TtsBackend {\n pub struct TtsDispatcher {\n     config: TtsConfig,\n     app: AppHandle,\n+    bouyomi: bouyomi::BouyomiBackend,\n+    voicevox: voicevox::VoicevoxBackend,\n }\n \n impl TtsDispatcher {\n     pub fn new(config: TtsConfig, app: AppHandle) -> Self {\n-        TtsDispatcher { config, app }\n+        let bouyomi = Self::build_bouyomi(&config);\n+        let voicevox = Self::build_voicevox(&config, &app);\n+        TtsDispatcher {\n+            config,\n+            app,\n+            bouyomi,\n+            voicevox,\n+        }\n     }\n \n     /// 設定を差し替える(設定更新コマンドから呼ぶ)。\n     pub fn update_config(&mut self, config: TtsConfig) {\n+        if self.config == config {\n+            return;\n+        }\n+\n+        if !Self::same_bouyomi_config(&self.config, &config) {\n+            self.bouyomi = Self::build_bouyomi(&config);\n+        }\n+        if !Self::same_voicevox_config(&self.config, &config) {\n+            self.voicevox = Self::build_voicevox(&config, &self.app);\n+        }\n         self.config = config;\n     }\n \n+    fn same_bouyomi_config(a: &TtsConfig, b: &TtsConfig) -> bool {\n+        let a = &a.options;\n+        let b = &b.options;\n+        a.bouyomi_host == b.bouyomi_host\n+            && a.bouyomi_port == b.bouyomi_port\n+            && a.bouyomi_speed == b.bouyomi_speed\n+            && a.bouyomi_tone == b.bouyomi_tone\n+            && a.bouyomi_volume == b.bouyomi_volume\n+            && a.bouyomi_voice == b.bouyomi_voice\n+    }\n+\n+    fn same_voicevox_config(a: &TtsConfig, b: &TtsConfig) -> bool {\n+        let a = &a.options;\n+        let b = &b.options;\n+        a.voicevox_url == b.voicevox_url && a.voicevox_speaker == b.voicevox_speaker\n+    }\n+\n+    fn build_bouyomi(config: &TtsConfig) -> bouyomi::BouyomiBackend {\n+        let opt = &config.options;\n+        bouyomi::BouyomiBackend::new(\n+            opt.bouyomi_host.clone(),\n+            opt.bouyomi_port,\n+            opt.bouyomi_speed,\n+            opt.bouyomi_tone,\n+            opt.bouyomi_volume,\n+            opt.bouyomi_voice,\n+        )\n+    }\n+\n+    fn build_voicevox(config: &TtsConfig, app: &AppHandle) -> voicevox::VoicevoxBackend {\n+        let opt = &config.options;\n+        voicevox::VoicevoxBackend::new(\n+            opt.voicevox_url.clone(),\n+            opt.voicevox_speaker,\n+            app.clone(),\n+        )\n+    }\n+\n     /// 1メッセージを読み上げる。整形 → 優先バックエンド → 失敗時フォールバック。\n     pub async fn speak_message(&self, msg: &ChatMessage) {\n         let text = self.format_for_speech(msg);\n@@ -56,28 +117,13 @@ impl TtsDispatcher {\n             TtsBackendKind::None => {}\n             TtsBackendKind::WebSpeech => self.emit_webspeech(&text),\n             TtsBackendKind::Bouyomi => {\n-                let opt = &self.config.options;\n-                let backend = bouyomi::BouyomiBackend::new(\n-                    opt.bouyomi_host.clone(),\n-                    opt.bouyomi_port,\n-                    opt.bouyomi_speed,\n-                    opt.bouyomi_tone,\n-                    opt.bouyomi_volume,\n-                    opt.bouyomi_voice,\n-                );\n-                if let Err(e) = backend.speak(text.clone()).await {\n+                if let Err(e) = self.bouyomi.speak(text.clone()).await {\n                     tracing::warn!(\"bouyomi 読み上げ失敗→Web Speech へ: {e}\");\n                     self.emit_webspeech(&text);\n                 }\n             }\n             TtsBackendKind::Voicevox => {\n-                let opt = &self.config.options;\n-                let backend = voicevox::VoicevoxBackend::new(\n-                    opt.voicevox_url.clone(),\n-                    opt.voicevox_speaker,\n-                    self.app.clone(),\n-                );\n-                if let Err(e) = backend.speak(text.clone()).await {\n+                if let Err(e) = self.voicevox.speak(text.clone()).await {\n                     tracing::warn!(\"voicevox 読み上げ失敗→Web Speech へ: {e}\");\n                     self.emit_webspeech(&text);\n                 }\ndiff --git a/src-tauri/src/tts/voicevox.rs b/src-tauri/src/tts/voicevox.rs\nindex 83a981e..40c7b6d 100644\n--- a/src-tauri/src/tts/voicevox.rs\n+++ b/src-tauri/src/tts/voicevox.rs\n@@ -123,4 +123,27 @@ mod base64_encode {\n         }\n         out\n     }\n+\n+    #[cfg(test)]\n+    mod tests {\n+        use super::encode;\n+\n+        #[test]\n+        fn encodes_rfc4648_vectors_with_standard_alphabet() {\n+            let cases = [\n+                (b\"\".as_slice(), \"\"),\n+                (b\"f\".as_slice(), \"Zg==\"),\n+                (b\"fo\".as_slice(), \"Zm8=\"),\n+                (b\"foo\".as_slice(), \"Zm9v\"),\n+                (b\"foob\".as_slice(), \"Zm9vYg==\"),\n+                (b\"fooba\".as_slice(), \"Zm9vYmE=\"),\n+                (b\"foobar\".as_slice(), \"Zm9vYmFy\"),\n+                (&[0xfb, 0xff, 0xff], \"+///\"),\n+            ];\n+\n+            for (input, expected) in cases {\n+                assert_eq!(encode(input), expected);\n+            }\n+        }\n+    }\n }\n\n```\n\n## Worker stdout tail (last 4KB)\n```\n実装完了しました。\n\n変更点:\n- [src-tauri/src/tts/mod.rs](/mnt/d/workspace/fast-comment/src-tauri/src/tts/mod.rs:55) の `update_config()` に同一設定の早期 return を追加。\n- Bouyomi/VOICEVOX の backend 設定差分だけを比較し、該当設定が変わった時だけ backend を再構築するように変更。\n- これにより `lib.rs` 側が speak 毎に `update_config()` を呼んでも、同一設定なら `VoicevoxBackend` と内部 `reqwest::Client` は再利用されます。\n\nAC確認:\n- AC1: 修正済み。VOICEVOX `reqwest::Client` は speak 毎に再生成されません。\n- AC2: [voicevox.rs](/mnt/d/workspace/fast-comment/src-tauri/src/tts/voicevox.rs:127) に base64 の標準アルファベット検証テストあり。\n- AC3: speak 失敗時の WebSpeech fallback は維持。\n- AC4: `available()` は用途コメントと `#[allow(dead_code)]` あり。\n- AC5: このパスの編集は `src-tauri/src/tts/mod.rs` のみ。許可範囲内です。\n\n検証:\n- `git diff --check -- src-tauri/src/tts/...` 成功。\n- `cargo test ...` は実行不可でした。この環境に `cargo` がありません。\n- `rustfmt` もこの環境にありません。\n\n```\n(exit code: 0)\n\n\n## Required output (machine-readable, stdout-last-line)\nReply with normal review prose, then on the FINAL line print exactly one of:\n\n  FULLRALPH-VERDICT: PASS\n  FULLRALPH-VERDICT: FAIL -- <one-line reason summary>\n\nReject (FAIL) if ANY of these are true:\n  * any acceptance criterion is not satisfied by the diff\n  * the diff touches files outside the touchedFiles list\n  * tests, types, or builds in the affected area are broken\n  * the implementation is partial or hand-wavy\n\ncodex\nレビュー対象が限定されているので、まず実際の差分と周辺型定義を確認して、ACだけでなくコンパイル上の破綻がないかを見ます。\nexec\n/bin/bash -lc \"rg -n \\\"struct TtsConfig|enum TtsBackendKind|derive\\\\(|impl PartialEq|available\\\\(\\\" src-tauri/src -S\" in /mnt/d/workspace/fast-comment\n succeeded in 0ms:\nsrc-tauri/src/config.rs:17:#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]\nsrc-tauri/src/config.rs:31:#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]\nsrc-tauri/src/config.rs:39:#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]\nsrc-tauri/src/config.rs:61:#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]\nsrc-tauri/src/config.rs:63:pub struct TtsConfig {\nsrc-tauri/src/config.rs:64:    /// 優先バックエンド。`available()==false` なら Web Speech へフォールバック。\nsrc-tauri/src/config.rs:82:#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize, Default)]\nsrc-tauri/src/config.rs:84:pub enum TtsBackendKind {\nsrc-tauri/src/config.rs:97:#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]\nsrc-tauri/src/config.rs:160:#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize, Default)]\nsrc-tauri/src/config.rs:175:#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]\nsrc-tauri/src/config.rs:194:#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize, Default)]\nsrc-tauri/src/config.rs:209:#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]\nsrc-tauri/src/moderation.rs:18:#[derive(Debug, Clone, PartialEq, Eq)]\nsrc-tauri/src/model.rs:8:#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]\nsrc-tauri/src/model.rs:15:#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]\nsrc-tauri/src/model.rs:31:#[derive(Debug, Clone, Default, PartialEq, Eq, Serialize, Deserialize)]\nsrc-tauri/src/model.rs:41:#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]\nsrc-tauri/src/model.rs:51:#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]\nsrc-tauri/src/model.rs:66:#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]\nsrc-tauri/src/model.rs:80:#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]\nsrc-tauri/src/model.rs:91:#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]\nsrc-tauri/src/bus.rs:44:#[derive(Clone)]\nsrc-tauri/src/bus.rs:192:#[derive(Clone)]\nsrc-tauri/src/bus.rs:199:#[derive(Debug, Deserialize)]\nsrc-tauri/src/bus.rs:205:#[derive(Debug, Deserialize)]\nsrc-tauri/src/tts/mod.rs:29:    async fn available(&self) -> bool;\nsrc-tauri/src/tts/mod.rs:114:        // TOCTOU と二重往復を避けるため、speak パスでは available() プローブを\nsrc-tauri/src/tts/bouyomi.rs:87:    async fn available(&self) -> bool {\nsrc-tauri/src/tts/voicevox.rs:83:    async fn available(&self) -> bool {\nsrc-tauri/src/sources/twitch.rs:279:#[derive(Default)]\nsrc-tauri/src/sources/youtube/innertube.rs:66:#[derive(Debug, Clone)]\n\nexec\n/bin/bash -lc 'git diff -- src-tauri/src/tts/mod.rs src-tauri/src/tts/voicevox.rs src-tauri/src/tts/bouyomi.rs' in /mnt/d/workspace/fast-comment\n succeeded in 0ms:\ndiff --git a/src-tauri/src/tts/mod.rs b/src-tauri/src/tts/mod.rs\nindex 2b27f1b..5cc12a5 100644\n--- a/src-tauri/src/tts/mod.rs\n+++ b/src-tauri/src/tts/mod.rs\n@@ -5,8 +5,8 @@\n //! - webspeech: 実再生は UI 側(`speechSynthesis`)。Rust は読み上げ対象テキストを\n //!   イベントで UI に渡すだけ。\n //!\n-//! ルーティング: 設定の優先バックエンドが `available()==false` なら Web Speech へ\n-//! フォールバックする。読み上げ整形(名前/URL/絵文字/長文)もここで行う。\n+//! ルーティング: 読み上げ時は優先バックエンドへ直接 `speak()` し、失敗したら\n+//! Web Speech へフォールバックする。読み上げ整形(名前/URL/絵文字/長文)もここで行う。\n \n pub mod bouyomi;\n pub mod voicevox;\n@@ -22,6 +22,10 @@ pub trait TtsBackend {\n     /// 整形済みテキストを読み上げる。\n     async fn speak(&self, text: String) -> anyhow::Result<()>;\n     /// バックエンドが現在利用可能か(接続可否の簡易判定)。\n+    ///\n+    /// speak パスでは TOCTOU と二重往復を避けるため呼ばない。設定 UI からの\n+    /// 疎通テスト用途として残す。\n+    #[allow(dead_code)]\n     async fn available(&self) -> bool;\n }\n \n@@ -31,18 +35,75 @@ pub trait TtsBackend {\n pub struct TtsDispatcher {\n     config: TtsConfig,\n     app: AppHandle,\n+    bouyomi: bouyomi::BouyomiBackend,\n+    voicevox: voicevox::VoicevoxBackend,\n }\n \n impl TtsDispatcher {\n     pub fn new(config: TtsConfig, app: AppHandle) -> Self {\n-        TtsDispatcher { config, app }\n+        let bouyomi = Self::build_bouyomi(&config);\n+        let voicevox = Self::build_voicevox(&config, &app);\n+        TtsDispatcher {\n+            config,\n+            app,\n+            bouyomi,\n+            voicevox,\n+        }\n     }\n \n     /// 設定を差し替える(設定更新コマンドから呼ぶ)。\n     pub fn update_config(&mut self, config: TtsConfig) {\n+        if self.config == config {\n+            return;\n+        }\n+\n+        if !Self::same_bouyomi_config(&self.config, &config) {\n+            self.bouyomi = Self::build_bouyomi(&config);\n+        }\n+        if !Self::same_voicevox_config(&self.config, &config) {\n+            self.voicevox = Self::build_voicevox(&config, &self.app);\n+        }\n         self.config = config;\n     }\n \n+    fn same_bouyomi_config(a: &TtsConfig, b: &TtsConfig) -> bool {\n+        let a = &a.options;\n+        let b = &b.options;\n+        a.bouyomi_host == b.bouyomi_host\n+            && a.bouyomi_port == b.bouyomi_port\n+            && a.bouyomi_speed == b.bouyomi_speed\n+            && a.bouyomi_tone == b.bouyomi_tone\n+            && a.bouyomi_volume == b.bouyomi_volume\n+            && a.bouyomi_voice == b.bouyomi_voice\n+    }\n+\n+    fn same_voicevox_config(a: &TtsConfig, b: &TtsConfig) -> bool {\n+        let a = &a.options;\n+        let b = &b.options;\n+        a.voicevox_url == b.voicevox_url && a.voicevox_speaker == b.voicevox_speaker\n+    }\n+\n+    fn build_bouyomi(config: &TtsConfig) -> bouyomi::BouyomiBackend {\n+        let opt = &config.options;\n+        bouyomi::BouyomiBackend::new(\n+            opt.bouyomi_host.clone(),\n+            opt.bouyomi_port,\n+            opt.bouyomi_speed,\n+            opt.bouyomi_tone,\n+            opt.bouyomi_volume,\n+            opt.bouyomi_voice,\n+        )\n+    }\n+\n+    fn build_voicevox(config: &TtsConfig, app: &AppHandle) -> voicevox::VoicevoxBackend {\n+        let opt = &config.options;\n+        voicevox::VoicevoxBackend::new(\n+            opt.voicevox_url.clone(),\n+            opt.voicevox_speaker,\n+            app.clone(),\n+        )\n+    }\n+\n     /// 1メッセージを読み上げる。整形 → 優先バックエンド → 失敗時フォールバック。\n     pub async fn speak_message(&self, msg: &ChatMessage) {\n         let text = self.format_for_speech(msg);\n@@ -56,28 +117,13 @@ impl TtsDispatcher {\n             TtsBackendKind::None => {}\n             TtsBackendKind::WebSpeech => self.emit_webspeech(&text),\n             TtsBackendKind::Bouyomi => {\n-                let opt = &self.config.options;\n-                let backend = bouyomi::BouyomiBackend::new(\n-                    opt.bouyomi_host.clone(),\n-                    opt.bouyomi_port,\n-                    opt.bouyomi_speed,\n-                    opt.bouyomi_tone,\n-                    opt.bouyomi_volume,\n-                    opt.bouyomi_voice,\n-                );\n-                if let Err(e) = backend.speak(text.clone()).await {\n+                if let Err(e) = self.bouyomi.speak(text.clone()).await {\n                     tracing::warn!(\"bouyomi 読み上げ失敗→Web Speech へ: {e}\");\n                     self.emit_webspeech(&text);\n                 }\n             }\n             TtsBackendKind::Voicevox => {\n-                let opt = &self.config.options;\n-                let backend = voicevox::VoicevoxBackend::new(\n-                    opt.voicevox_url.clone(),\n-                    opt.voicevox_speaker,\n-                    self.app.clone(),\n-                );\n-                if let Err(e) = backend.speak(text.clone()).await {\n+                if let Err(e) = self.voicevox.speak(text.clone()).await {\n                     tracing::warn!(\"voicevox 読み上げ失敗→Web Speech へ: {e}\");\n                     self.emit_webspeech(&text);\n                 }\ndiff --git a/src-tauri/src/tts/voicevox.rs b/src-tauri/src/tts/voicevox.rs\nindex 83a981e..40c7b6d 100644\n--- a/src-tauri/src/tts/voicevox.rs\n+++ b/src-tauri/src/tts/voicevox.rs\n@@ -123,4 +123,27 @@ mod base64_encode {\n         }\n         out\n     }\n+\n+    #[cfg(test)]\n+    mod tests {\n+        use super::encode;\n+\n+        #[test]\n+        fn encodes_rfc4648_vectors_with_standard_alphabet() {\n+            let cases = [\n+                (b\"\".as_slice(), \"\"),\n+                (b\"f\".as_slice(), \"Zg==\"),\n+                (b\"fo\".as_slice(), \"Zm8=\"),\n+                (b\"foo\".as_slice(), \"Zm9v\"),\n+                (b\"foob\".as_slice(), \"Zm9vYg==\"),\n+                (b\"fooba\".as_slice(), \"Zm9vYmE=\"),\n+                (b\"foobar\".as_slice(), \"Zm9vYmFy\"),\n+                (&[0xfb, 0xff, 0xff], \"+///\"),\n+            ];\n+\n+            for (input, expected) in cases {\n+                assert_eq!(encode(input), expected);\n+            }\n+        }\n+    }\n }\n\nexec\n/bin/bash -lc 'git diff --name-only' in /mnt/d/workspace/fast-comment\n succeeded in 162ms:\n.claude/SPEC.md\n.omc/fullralph-prd.json\n.omc/project-memory.json\n.omc/state/agent-replay-4b0a4f91-acf2-4764-bdb1-5d88fdbd50d0.jsonl\n.omc/state/fullralph-locks.json\n.omc/state/fullralph-results/US-001.codex-last.txt\n.omc/state/fullralph-results/US-001.json\n.omc/state/fullralph-results/US-002.codex-last.txt\n.omc/state/fullralph-results/US-002.json\n.omc/state/fullralph-results/US-003.codex-last.txt\n.omc/state/fullralph-results/US-003.json\n.omc/state/fullralph-results/US-004.codex-last.txt\n.omc/state/fullralph-results/US-004.json\n.omc/state/fullralph-results/US-005.codex-last.txt\n.omc/state/fullralph-results/US-005.json\n.omc/state/fullralph-results/US-006.json\n.omc/state/fullralph-results/US-007.json\n.omc/state/fullralph-results/US-008.json\n.omc/state/fullralph-results/US-009.codex-last.txt\n.omc/state/fullralph-results/US-009.json\n.omc/state/fullralph-review-prompts/US-001.txt\n.omc/state/fullralph-review-prompts/US-002.txt\n.omc/state/fullralph-review-prompts/US-003.txt\n.omc/state/fullralph-review-prompts/US-004.txt\n.omc/state/fullralph-review-prompts/US-005.txt\n.omc/state/fullralph-review-prompts/US-006.txt\n.omc/state/fullralph-review-prompts/US-007.txt\n.omc/state/fullralph-review-prompts/US-008.txt\n.omc/state/fullralph-review-prompts/US-009.txt\n.omc/state/fullralph-review-results/US-001.codex-last.txt\n.omc/state/fullralph-review-results/US-001.json\n.omc/state/fullralph-review-results/US-002.codex-last.txt\n.omc/state/fullralph-review-results/US-002.json\n.omc/state/fullralph-review-results/US-003.codex-last.txt\n.omc/state/fullralph-review-results/US-003.json\n.omc/state/fullralph-review-results/US-004.codex-last.txt\n.omc/state/fullralph-review-results/US-004.json\n.omc/state/fullralph-review-results/US-005.codex-last.txt\n.omc/state/fullralph-review-results/US-005.json\n.omc/state/fullralph-review-results/US-006.codex-last.txt\n.omc/state/fullralph-review-results/US-006.json\n.omc/state/fullralph-review-results/US-007.codex-last.txt\n.omc/state/fullralph-review-results/US-007.json\n.omc/state/fullralph-review-results/US-008.codex-last.txt\n.omc/state/fullralph-review-results/US-008.json\n.omc/state/fullralph-review-results/US-009.codex-last.txt\n.omc/state/fullralph-review-results/US-009.json\n.omc/state/hud-stdin-cache.json\n.omc/state/last-tool-error.json\n.omc/state/mission-state.json\n.omc/state/subagent-tracking.json\n.omc/wave2.workflow.js\nSETUP.md\nsrc-tauri/icons/128x128.png\nsrc-tauri/icons/128x128@2x.png\nsrc-tauri/icons/32x32.png\nsrc-tauri/icons/icon.icns\nsrc-tauri/icons/icon.ico\nsrc-tauri/src/bus.rs\nsrc-tauri/src/config.rs\nsrc-tauri/src/lib.rs\nsrc-tauri/src/sources/twitch.rs\nsrc-tauri/src/sources/youtube/innertube.rs\nsrc-tauri/src/sources/youtube/mod.rs\nsrc-tauri/src/sources/youtube/parser.rs\nsrc-tauri/src/tts/mod.rs\nsrc-tauri/src/tts/voicevox.rs\nsrc-tauri/tauri.conf.json\nsrc/lib/components/CommentItem.svelte\nsrc/lib/components/CommentList.svelte\nsrc/lib/components/Settings.svelte\nsrc/lib/ipc.ts\nsrc/lib/stores.svelte.ts\ntemplates/default/app.js\ntemplates/default/index.html\ntemplates/default/style.css\n\ncodex\n差分内の型比較は通りそうですが、実際のワークツリーにはこのストーリー外の変更も多数あります。次に `TtsDispatcher` の利用箇所と backend 実装を見て、再利用の実効性と fallback の経路を確認します。\nexec\n/bin/bash -lc \"sed -n '1,220p' src-tauri/src/tts/mod.rs\" in /mnt/d/workspace/fast-comment\nexec\n/bin/bash -lc \"sed -n '1,180p' src-tauri/src/tts/voicevox.rs\" in /mnt/d/workspace/fast-comment\nexec\n/bin/bash -lc \"rg -n \\\"TtsDispatcher|speak_message|update_config\\\\(\\\" src-tauri/src -S\" in /mnt/d/workspace/fast-comment\nexec\n/bin/bash -lc \"sed -n '1,150p' src-tauri/src/tts/bouyomi.rs\" in /mnt/d/workspace/fast-comment\n succeeded in 315ms:\n//! TTS(読み上げ)層 — 3バックエンドをアダプタ化する。\n//!\n//! - bouyomi  : 棒読みちゃん(TCP 127.0.0.1:50001 バイナリコマンド)\n//! - voicevox : VOICEVOX(HTTP /audio_query → /synthesis)\n//! - webspeech: 実再生は UI 側(`speechSynthesis`)。Rust は読み上げ対象テキストを\n//!   イベントで UI に渡すだけ。\n//!\n//! ルーティング: 読み上げ時は優先バックエンドへ直接 `speak()` し、失敗したら\n//! Web Speech へフォールバックする。読み上げ整形(名前/URL/絵文字/長文)もここで行う。\n\npub mod bouyomi;\npub mod voicevox;\n\nuse tauri::{AppHandle, Emitter};\n\nuse crate::config::{TtsBackendKind, TtsConfig};\nuse crate::model::{ChatMessage, Fragment};\n\n/// 読み上げバックエンドの共通インタフェース。\n#[allow(async_fn_in_trait)]\npub trait TtsBackend {\n    /// 整形済みテキストを読み上げる。\n    async fn speak(&self, text: String) -> anyhow::Result<()>;\n    /// バックエンドが現在利用可能か(接続可否の簡易判定)。\n    ///\n    /// speak パスでは TOCTOU と二重往復を避けるため呼ばない。設定 UI からの\n    /// 疎通テスト用途として残す。\n    #[allow(dead_code)]\n    async fn available(&self) -> bool;\n}\n\n/// 設定に基づき読み上げを振り分けるディスパッチャ。\n///\n/// `AppHandle` は Web Speech フォールバック時に UI へイベントを送るために持つ。\npub struct TtsDispatcher {\n    config: TtsConfig,\n    app: AppHandle,\n    bouyomi: bouyomi::BouyomiBackend,\n    voicevox: voicevox::VoicevoxBackend,\n}\n\nimpl TtsDispatcher {\n    pub fn new(config: TtsConfig, app: AppHandle) -> Self {\n        let bouyomi = Self::build_bouyomi(&config);\n        let voicevox = Self::build_voicevox(&config, &app);\n        TtsDispatcher {\n            config,\n            app,\n            bouyomi,\n            voicevox,\n        }\n    }\n\n    /// 設定を差し替える(設定更新コマンドから呼ぶ)。\n    pub fn update_config(&mut self, config: TtsConfig) {\n        if self.config == config {\n            return;\n        }\n\n        if !Self::same_bouyomi_config(&self.config, &config) {\n            self.bouyomi = Self::build_bouyomi(&config);\n        }\n        if !Self::same_voicevox_config(&self.config, &config) {\n            self.voicevox = Self::build_voicevox(&config, &self.app);\n        }\n        self.config = config;\n    }\n\n    fn same_bouyomi_config(a: &TtsConfig, b: &TtsConfig) -> bool {\n        let a = &a.options;\n        let b = &b.options;\n        a.bouyomi_host == b.bouyomi_host\n            && a.bouyomi_port == b.bouyomi_port\n            && a.bouyomi_speed == b.bouyomi_speed\n            && a.bouyomi_tone == b.bouyomi_tone\n            && a.bouyomi_volume == b.bouyomi_volume\n            && a.bouyomi_voice == b.bouyomi_voice\n    }\n\n    fn same_voicevox_config(a: &TtsConfig, b: &TtsConfig) -> bool {\n        let a = &a.options;\n        let b = &b.options;\n        a.voicevox_url == b.voicevox_url && a.voicevox_speaker == b.voicevox_speaker\n    }\n\n    fn build_bouyomi(config: &TtsConfig) -> bouyomi::BouyomiBackend {\n        let opt = &config.options;\n        bouyomi::BouyomiBackend::new(\n            opt.bouyomi_host.clone(),\n            opt.bouyomi_port,\n            opt.bouyomi_speed,\n            opt.bouyomi_tone,\n            opt.bouyomi_volume,\n            opt.bouyomi_voice,\n        )\n    }\n\n    fn build_voicevox(config: &TtsConfig, app: &AppHandle) -> voicevox::VoicevoxBackend {\n        let opt = &config.options;\n        voicevox::VoicevoxBackend::new(\n            opt.voicevox_url.clone(),\n            opt.voicevox_speaker,\n            app.clone(),\n        )\n    }\n\n    /// 1メッセージを読み上げる。整形 → 優先バックエンド → 失敗時フォールバック。\n    pub async fn speak_message(&self, msg: &ChatMessage) {\n        let text = self.format_for_speech(msg);\n        if text.trim().is_empty() {\n            return;\n        }\n\n        // TOCTOU と二重往復を避けるため、speak パスでは available() プローブを\n        // 行わず直接 speak() を試み、Err なら Web Speech へフォールバックする。\n        match self.config.backend {\n            TtsBackendKind::None => {}\n            TtsBackendKind::WebSpeech => self.emit_webspeech(&text),\n            TtsBackendKind::Bouyomi => {\n                if let Err(e) = self.bouyomi.speak(text.clone()).await {\n                    tracing::warn!(\"bouyomi 読み上げ失敗→Web Speech へ: {e}\");\n                    self.emit_webspeech(&text);\n                }\n            }\n            TtsBackendKind::Voicevox => {\n                if let Err(e) = self.voicevox.speak(text.clone()).await {\n                    tracing::warn!(\"voicevox 読み上げ失敗→Web Speech へ: {e}\");\n                    self.emit_webspeech(&text);\n                }\n            }\n        }\n    }\n\n    /// Web Speech 用に「読み上げテキスト」を UI へ送る(実再生は UI)。\n    fn emit_webspeech(&self, text: &str) {\n        if let Err(e) = self.app.emit(\"tts-speak\", text) {\n            tracing::warn!(\"tts-speak emit 失敗: {e}\");\n        }\n    }\n\n    /// メッセージを読み上げ用テキストへ整形する。\n    fn format_for_speech(&self, msg: &ChatMessage) -> String {\n        let opt = &self.config.options;\n\n        // 本文を組み立てる(絵文字除去オプション対応)。\n        let mut body = String::new();\n        for frag in &msg.fragments {\n            match frag {\n                Fragment::Text { text } => body.push_str(text),\n                Fragment::Emote { name, .. } => {\n                    if !opt.strip_emoji {\n                        body.push_str(name);\n                    }\n                }\n            }\n        }\n\n        // URL 省略。\n        if opt.omit_url {\n            body = omit_urls(&body);\n        }\n\n        // 名前の前置。\n        let mut text = if opt.read_name && !msg.author.name.is_empty() {\n            format!(\"{} {}\", msg.author.name, body)\n        } else {\n            body\n        };\n\n        // 長文カット(char 単位)。\n        if opt.max_length > 0 {\n            let chars: Vec<char> = text.chars().collect();\n            if chars.len() > opt.max_length {\n                text = chars[..opt.max_length].iter().collect();\n            }\n        }\n\n        text\n    }\n}\n\n/// 本文中の URL を「URL省略」へ置換する(簡易: `http`/`https` で始まる連続非空白)。\nfn omit_urls(s: &str) -> String {\n    let mut out = String::with_capacity(s.len());\n    for token in s.split_inclusive(char::is_whitespace) {\n        let trimmed = token.trim_end();\n        if trimmed.starts_with(\"http://\") || trimmed.starts_with(\"https://\") {\n            out.push_str(\"URL省略\");\n            // 区切りの空白は維持。\n            if token.len() != trimmed.len() {\n                out.push_str(&token[trimmed.len()..]);\n            }\n        } else {\n            out.push_str(token);\n        }\n    }\n    out\n}\n\n succeeded in 315ms:\n//! VOICEVOX TTS バックエンド。\n//!\n//! 既定 `http://127.0.0.1:50021`。手順:\n//!  1. `POST /audio_query?text=...&speaker=N` で音声合成クエリ(JSON)取得。\n//!  2. `POST /synthesis?speaker=N`(body=クエリJSON)で wav バイトを取得。\n//!\n//! Cargo に rodio 等の再生クレートが無いため、合成した wav は base64 にして\n//! UI へ `tts-audio` イベントで送り、UI(WebAudio)で再生させる。\n//! (「重い合成は Rust、再生は薄く UI」方針。)\n\nuse std::time::Duration;\n\nuse self::base64_encode::encode as b64encode;\nuse reqwest::Client;\nuse serde_json::Value;\nuse tauri::{AppHandle, Emitter};\n\nuse super::TtsBackend;\n\n/// 合成リクエスト全体のタイムアウト(エンジン stall 時に永久待ちを防ぐ)。\nconst REQUEST_TIMEOUT: Duration = Duration::from_secs(10);\n\n/// VOICEVOX エンジンへの設定。\npub struct VoicevoxBackend {\n    base_url: String,\n    speaker: u32,\n    app: AppHandle,\n    http: Client,\n}\n\nimpl VoicevoxBackend {\n    pub fn new(base_url: String, speaker: u32, app: AppHandle) -> Self {\n        VoicevoxBackend {\n            base_url: base_url.trim_end_matches('/').to_string(),\n            speaker,\n            app,\n            http: Client::builder()\n                .timeout(REQUEST_TIMEOUT)\n                .build()\n                .unwrap_or_else(|_| Client::new()),\n        }\n    }\n}\n\nimpl TtsBackend for VoicevoxBackend {\n    async fn speak(&self, text: String) -> anyhow::Result<()> {\n        let speaker = self.speaker.to_string();\n\n        // 1) audio_query\n        let query_url = format!(\"{}/audio_query\", self.base_url);\n        let query: Value = self\n            .http\n            .post(&query_url)\n            .query(&[(\"text\", text.as_str()), (\"speaker\", speaker.as_str())])\n            .send()\n            .await?\n            .error_for_status()?\n            .json()\n            .await?;\n\n        // 2) synthesis(wav バイト)\n        let synth_url = format!(\"{}/synthesis\", self.base_url);\n        let wav = self\n            .http\n            .post(&synth_url)\n            .query(&[(\"speaker\", speaker.as_str())])\n            .header(\"Content-Type\", \"application/json\")\n            .json(&query)\n            .send()\n            .await?\n            .error_for_status()?\n            .bytes()\n            .await?;\n\n        // 3) base64 にして UI へ。UI は data URI で再生する。\n        let b64 = b64encode(&wav);\n        if let Err(e) = self.app.emit(\"tts-audio\", b64) {\n            tracing::warn!(\"tts-audio emit 失敗: {e}\");\n        }\n        Ok(())\n    }\n\n    async fn available(&self) -> bool {\n        // /version を叩けるかで判定。\n        let url = format!(\"{}/version\", self.base_url);\n        matches!(\n            tokio::time::timeout(\n                std::time::Duration::from_millis(500),\n                self.http.get(&url).send()\n            )\n            .await,\n            Ok(Ok(resp)) if resp.status().is_success()\n        )\n    }\n}\n\n/// 依存追加を避けるための最小 base64 エンコーダ(標準アルファベット)。\nmod base64_encode {\n    const ALPHABET: &[u8; 64] =\n        b\"ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/\";\n\n    /// バイト列を base64 文字列へ変換する。\n    pub fn encode(data: &[u8]) -> String {\n        let mut out = String::with_capacity((data.len() + 2) / 3 * 4);\n        for chunk in data.chunks(3) {\n            let b0 = chunk[0] as u32;\n            let b1 = *chunk.get(1).unwrap_or(&0) as u32;\n            let b2 = *chunk.get(2).unwrap_or(&0) as u32;\n            let n = (b0 << 16) | (b1 << 8) | b2;\n\n            out.push(ALPHABET[((n >> 18) & 0x3f) as usize] as char);\n            out.push(ALPHABET[((n >> 12) & 0x3f) as usize] as char);\n            if chunk.len() > 1 {\n                out.push(ALPHABET[((n >> 6) & 0x3f) as usize] as char);\n            } else {\n                out.push('=');\n            }\n            if chunk.len() > 2 {\n                out.push(ALPHABET[(n & 0x3f) as usize] as char);\n            } else {\n                out.push('=');\n            }\n        }\n        out\n    }\n\n    #[cfg(test)]\n    mod tests {\n        use super::encode;\n\n        #[test]\n        fn encodes_rfc4648_vectors_with_standard_alphabet() {\n            let cases = [\n                (b\"\".as_slice(), \"\"),\n                (b\"f\".as_slice(), \"Zg==\"),\n                (b\"fo\".as_slice(), \"Zm8=\"),\n                (b\"foo\".as_slice(), \"Zm9v\"),\n                (b\"foob\".as_slice(), \"Zm9vYg==\"),\n                (b\"fooba\".as_slice(), \"Zm9vYmE=\"),\n                (b\"foobar\".as_slice(), \"Zm9vYmFy\"),\n                (&[0xfb, 0xff, 0xff], \"+///\"),\n            ];\n\n            for (input, expected) in cases {\n                assert_eq!(encode(input), expected);\n            }\n        }\n    }\n}\n\n succeeded in 346ms:\n//! 棒読みちゃん(BouyomiChan)TTS バックエンド。\n//!\n//! `TCP 127.0.0.1:50001` へ独自バイナリコマンドを送る。\n//! コマンド 0x0001(Talk)のレイアウト(すべてリトルエンディアン):\n//!\n//! | offset | size | 内容                              |\n//! |--------|------|-----------------------------------|\n//! | 0      | 2    | コマンド = 0x0001                  |\n//! | 2      | 2    | 速度 speed (i16, -1=既定)          |\n//! | 4      | 2    | 音程 tone  (i16, -1=既定)          |\n//! | 6      | 2    | 音量 volume(i16, -1=既定)          |\n//! | 8      | 2    | 声質 voice (i16, 0=既定)           |\n//! | 10     | 1    | 文字コード(0 = UTF-8)             |\n//! | 11     | 4    | 本文バイト長(i32)                 |\n//! | 15     | n    | 本文(UTF-8)                       |\n\nuse std::time::Duration;\n\nuse tokio::io::AsyncWriteExt;\nuse tokio::net::TcpStream;\n\nuse super::TtsBackend;\n\nconst CMD_TALK: u16 = 0x0001;\nconst CHARCODE_UTF8: u8 = 0;\n\n/// 接続〜送出のタイムアウト(stall 時に速やかに失敗し Web Speech へフォールバック)。\nconst SPEAK_TIMEOUT: Duration = Duration::from_secs(3);\n\n/// 棒読みちゃんへの送信設定。\npub struct BouyomiBackend {\n    host: String,\n    port: u16,\n    speed: i16,\n    tone: i16,\n    volume: i16,\n    voice: i16,\n}\n\nimpl BouyomiBackend {\n    pub fn new(host: String, port: u16, speed: i16, tone: i16, volume: i16, voice: i16) -> Self {\n        BouyomiBackend {\n            host,\n            port,\n            speed,\n            tone,\n            volume,\n            voice,\n        }\n    }\n\n    fn addr(&self) -> String {\n        format!(\"{}:{}\", self.host, self.port)\n    }\n\n    /// Talk コマンドのバイト列を組み立てる。\n    fn build_packet(&self, text: &str) -> Vec<u8> {\n        let body = text.as_bytes();\n        let mut buf = Vec::with_capacity(15 + body.len());\n        buf.extend_from_slice(&CMD_TALK.to_le_bytes());\n        buf.extend_from_slice(&self.speed.to_le_bytes());\n        buf.extend_from_slice(&self.tone.to_le_bytes());\n        buf.extend_from_slice(&self.volume.to_le_bytes());\n        buf.extend_from_slice(&self.voice.to_le_bytes());\n        buf.push(CHARCODE_UTF8);\n        buf.extend_from_slice(&(body.len() as i32).to_le_bytes());\n        buf.extend_from_slice(body);\n        buf\n    }\n}\n\nimpl TtsBackend for BouyomiBackend {\n    async fn speak(&self, text: String) -> anyhow::Result<()> {\n        let packet = self.build_packet(&text);\n        let addr = self.addr();\n        tokio::time::timeout(SPEAK_TIMEOUT, async {\n            let mut stream = TcpStream::connect(&addr).await?;\n            stream.write_all(&packet).await?;\n            stream.flush().await?;\n            Ok::<(), anyhow::Error>(())\n        })\n        .await\n        .map_err(|_| anyhow::anyhow!(\"棒読みちゃん送出がタイムアウト({}秒)\", SPEAK_TIMEOUT.as_secs()))??;\n        Ok(())\n    }\n\n    async fn available(&self) -> bool {\n        // ポートへ接続できるかで簡易判定(短いタイムアウト)。\n        let connect = TcpStream::connect(self.addr());\n        matches!(\n            tokio::time::timeout(std::time::Duration::from_millis(500), connect).await,\n            Ok(Ok(_))\n        )\n    }\n}\n\n succeeded in 363ms:\nsrc-tauri/src/lib.rs:34:use crate::tts::TtsDispatcher;\nsrc-tauri/src/lib.rs:89:fn update_config(\nsrc-tauri/src/lib.rs:104:        .update_config(&new_config.moderation);\nsrc-tauri/src/lib.rs:332:/// bounded mpsc を逐次に消費し、1つの `TtsDispatcher` を再利用する。\nsrc-tauri/src/lib.rs:335:/// `speak_message` は内部で speak→Err 時に Web Speech フォールバックする(#15 と整合)。\nsrc-tauri/src/lib.rs:342:        let mut dispatcher = TtsDispatcher::new(initial_cfg, app.clone());\nsrc-tauri/src/lib.rs:357:                    dispatcher.update_config(cfg);\nsrc-tauri/src/lib.rs:358:                    dispatcher.speak_message(&msg).await;\nsrc-tauri/src/moderation.rs:51:    pub fn update_config(&mut self, config: &ModerationConfig) {\nsrc-tauri/src/tts/mod.rs:35:pub struct TtsDispatcher {\nsrc-tauri/src/tts/mod.rs:42:impl TtsDispatcher {\nsrc-tauri/src/tts/mod.rs:46:        TtsDispatcher {\nsrc-tauri/src/tts/mod.rs:55:    pub fn update_config(&mut self, config: TtsConfig) {\nsrc-tauri/src/tts/mod.rs:108:    pub async fn speak_message(&self, msg: &ChatMessage) {\n\nexec\n/bin/bash -lc \"sed -n '320,370p' src-tauri/src/lib.rs\" in /mnt/d/workspace/fast-comment\nexec\n/bin/bash -lc \"sed -n '55,115p' src-tauri/src/config.rs\" in /mnt/d/workspace/fast-comment\nexec\n/bin/bash -lc 'cargo test --manifest-path src-tauri/Cargo.toml tts::voicevox::base64_encode::tests::encodes_rfc4648_vectors_with_standard_alphabet' in /mnt/d/workspace/fast-comment\n succeeded in 300ms:\n            template: default_obs_template(),\n        }\n    }\n}\n\n/// TTS(読み上げ)設定。\n#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]\n#[serde(rename_all = \"camelCase\")]\npub struct TtsConfig {\n    /// 優先バックエンド。`available()==false` なら Web Speech へフォールバック。\n    #[serde(default)]\n    pub backend: TtsBackendKind,\n    /// バックエンド別の細かな調整値。\n    #[serde(default)]\n    pub options: TtsOptions,\n}\n\nimpl Default for TtsConfig {\n    fn default() -> Self {\n        TtsConfig {\n            backend: TtsBackendKind::default(),\n            options: TtsOptions::default(),\n        }\n    }\n}\n\n/// 読み上げバックエンドの種類。\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize, Default)]\n#[serde(rename_all = \"camelCase\")]\npub enum TtsBackendKind {\n    /// 棒読みちゃん(TCP 127.0.0.1:50001)。\n    Bouyomi,\n    /// VOICEVOX(HTTP 127.0.0.1:50021)。\n    Voicevox,\n    /// ブラウザ `speechSynthesis`(UI 側で再生)。既定。\n    #[default]\n    WebSpeech,\n    /// 読み上げ無効。\n    None,\n}\n\n/// 読み上げの整形/エンジン調整パラメータ。\n#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]\n#[serde(rename_all = \"camelCase\")]\npub struct TtsOptions {\n    /// 棒読み: 接続先ホスト。\n    #[serde(default = \"default_bouyomi_host\")]\n    pub bouyomi_host: String,\n    /// 棒読み: 接続先ポート。既定 50001。\n    #[serde(default = \"default_bouyomi_port\")]\n    pub bouyomi_port: u16,\n    /// 棒読み: 速度(-1=デフォルト)。\n    #[serde(default = \"default_minus_one\")]\n    pub bouyomi_speed: i16,\n    /// 棒読み: 音量(-1=デフォルト)。\n    #[serde(default = \"default_minus_one\")]\n    pub bouyomi_volume: i16,\n    /// 棒読み: 音程(-1=デフォルト)。\n    #[serde(default = \"default_minus_one\")]\n    pub bouyomi_tone: i16,\n    /// 棒読み: 声質(0=デフォルト)。\n\n succeeded in 302ms:\n\n                    // 下流(UI/OBS)へ。\n                    bus.publish(msg);\n                }\n            }\n        }\n        tracing::info!(\"moderation/TTS パイプライン終了\");\n    });\n}\n\n/// 単一の長命 TTS ワーカーを起動する。\n///\n/// bounded mpsc を逐次に消費し、1つの `TtsDispatcher` を再利用する。\n/// 設定は都度スナップショットを取り `update_config` で反映する(バックエンド再構築は\n/// 各 speak 内で行うが、毎メッセージのタスク spawn / 再プローブは行わない)。\n/// `speak_message` は内部で speak→Err 時に Web Speech フォールバックする(#15 と整合)。\nfn spawn_tts_worker(app: AppHandle, mut rx: mpsc::Receiver<ChatMessage>, cancel: CancellationToken) {\n    tokio::spawn(async move {\n        let initial_cfg = {\n            let state = app.state::<AppState>();\n            state.config.lock().unwrap().tts.clone()\n        };\n        let mut dispatcher = TtsDispatcher::new(initial_cfg, app.clone());\n\n        loop {\n            tokio::select! {\n                _ = cancel.cancelled() => break,\n                recv = rx.recv() => {\n                    let msg = match recv {\n                        Some(m) => m,\n                        None => break,\n                    };\n                    // 最新設定を反映してから読み上げる。\n                    let cfg = {\n                        let state = app.state::<AppState>();\n                        state.config.lock().unwrap().tts.clone()\n                    };\n                    dispatcher.update_config(cfg);\n                    dispatcher.speak_message(&msg).await;\n                }\n            }\n        }\n        tracing::info!(\"TTS ワーカー終了\");\n    });\n}\n\n/// OBS テンプレートの配信元ディレクトリを解決する。\n///\n/// リソース同梱の `templates/` を優先し、無ければ実行ファイル隣 / CWD を試す。\nfn resolve_templates_dir(app: &AppHandle) -> PathBuf {\n    // Tauri リソースに同梱した templates を探す。\n\n exited 127 in 456ms:\n/bin/bash: line 1: cargo: command not found\n\ncodex\n`cargo` はこの環境に無く、実行検証はできませんでした。目視では `TtsConfig` は `PartialEq` を持っており、`VoicevoxBackend` の `Client` も保持フィールド経由で使われているため、この差分自体に型上の明確な破綻は見えていません。\nexec\n/bin/bash -lc 'git diff --name-only -- src-tauri/src/tts' in /mnt/d/workspace/fast-comment\nexec\n/bin/bash -lc 'git diff --check -- src-tauri/src/tts/mod.rs src-tauri/src/tts/voicevox.rs src-tauri/src/tts/bouyomi.rs' in /mnt/d/workspace/fast-comment\n succeeded in 283ms:\nsrc-tauri/src/tts/mod.rs\nsrc-tauri/src/tts/voicevox.rs\n\n succeeded in 289ms:\ncodex\nブロッキングな指摘はありません。\n\n`TtsDispatcher` が `BouyomiBackend` と `VoicevoxBackend` を保持する形になっており、`VoicevoxBackend` 内の `reqwest::Client` は `speak()` ごとの生成ではなく再利用されます。`update_config()` も VOICEVOX の URL / speaker が変わった場合だけ再構築するため、AC1 は満たしています。\n\nbase64 の RFC4648 ベクタと `+///` による標準アルファベット確認テストも追加済みです。`speak()` 失敗時の WebSpeech fallback は既存の `Err => emit_webspeech()` 経路が維持されています。`available()` も speak パスでは使わない用途コメントと `#[allow(dead_code)]` が入っています。\n\n確認:\n- `git diff --name-only -- src-tauri/src/tts` は `mod.rs` と `voicevox.rs` のみ\n- `git diff --check -- src-tauri/src/tts/...` は成功\n- `cargo test` は環境に `cargo` が無く実行不可でしたが、目視上この差分に型破綻は見当たりません\n\nFULLRALPH-VERDICT: PASS\ntokens used\n31,166\n"
}