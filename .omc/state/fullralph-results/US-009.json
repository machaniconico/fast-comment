{
  "storyId": "US-009",
  "engine": "codex",
  "mode": "implement",
  "model": "(default)",
  "effort": "high",
  "codexHome": "/home/macha/.codex-dualralph",
  "cwd": "/mnt/d/workspace/fast-comment",
  "startedAt": "2026-05-29T16:42:41.565Z",
  "elapsedMs": 172897,
  "exitCode": 0,
  "lastMessageBytes": 739,
  "stdoutBytes": 740,
  "stderrBytes": 135104,
  "lastMessage": "実装完了しました。変更は指定範囲の `templates/default/app.js`, `templates/default/style.css`, `templates/default/index.html` のみです。\n\n**変更内容**\n- `?ws=` 指定時/未指定時どちらも `new URL(...).searchParams.set('channel', ...)` で `channel` を付与。\n- `max` / `ttl` を `Number.isFinite && > 0` で検証し、不正値は既定値へフォールバック。\n- `System` / `system` メッセージを `.system` として描画し、CSSを追加。\n- `displayColor` は `#hex` のみ適用、badge/emote URL は `http:` / `https:` のみ許可。\n- 退場アニメ時間を CSS 変数 `--leave-ms` に集約し、JSは `getComputedStyle` から取得。\n- OBS用途で不要な viewport meta を削除。\n- batch配列受信と既存の再接続バックオフ処理は維持。\n\n**検証**\n- `node --check templates/default/app.js`\n- `git diff --check -- templates/default/app.js templates/default/style.css templates/default/index.html`\n- VMスタブで URL構築、NaNフォールバック、System描画、色/URLサニタイズの実行確認済み。",
  "stdout": "実装完了しました。変更は指定範囲の `templates/default/app.js`, `templates/default/style.css`, `templates/default/index.html` のみです。\n\n**変更内容**\n- `?ws=` 指定時/未指定時どちらも `new URL(...).searchParams.set('channel', ...)` で `channel` を付与。\n- `max` / `ttl` を `Number.isFinite && > 0` で検証し、不正値は既定値へフォールバック。\n- `System` / `system` メッセージを `.system` として描画し、CSSを追加。\n- `displayColor` は `#hex` のみ適用、badge/emote URL は `http:` / `https:` のみ許可。\n- 退場アニメ時間を CSS 変数 `--leave-ms` に集約し、JSは `getComputedStyle` から取得。\n- OBS用途で不要な viewport meta を削除。\n- batch配列受信と既存の再接続バックオフ処理は維持。\n\n**検証**\n- `node --check templates/default/app.js`\n- `git diff --check -- templates/default/app.js templates/default/style.css templates/default/index.html`\n- VMスタブで URL構築、NaNフォールバック、System描画、色/URLサニタイズの実行確認済み。\n",
  "stderr": "Reading prompt from stdin...\nOpenAI Codex v0.128.0 (research preview)\n--------\nworkdir: /mnt/d/workspace/fast-comment\nmodel: gpt-5.5\nprovider: openai\napproval: never\nsandbox: workspace-write [workdir, /tmp, $TMPDIR, /home/macha/.codex-dualralph/memories]\nreasoning effort: high\nreasoning summaries: none\nsession id: 019e749d-d709-71a2-acff-78d131f815e7\n--------\nuser\n# FULLRALPH IMPLEMENTATION for US-009\n\nImplement this user story end-to-end. Touch ONLY the declared files. Do not begin discussing -- start editing immediately.\n\n## Original task\nfast-comment SPEC完成度向上: 3レーン監査で検出したCRITICAL/HIGH/MEDIUM欠陥を修正し、MVP(P1-P5)を機能水準に引き上げる。WSLはビルド不可のため目視レビュー検証のみ。\n\n## Story US-009: OBSテンプレ堅牢化 (URL構築 / NaNガード / System種別 / 色サニタイズ)\ntemplates/default/ 配下の app.js, style.css, index.html のみを編集。\n1) [HIGH] app.js: WS_URL = params.get('ws') || 'ws://127.0.0.1:11180/ws' + location.search が演算子優先順位で壊れている。?ws= 指定時に channel が付かず、未指定時のみ location.search が付く。修正: const base = params.get('ws') || 'ws://127.0.0.1:11180/ws'; とし、channel は常に new URL + searchParams で base に付与する(ws指定とchannel併用可)。\n2) [MEDIUM] app.js: parseInt の NaN ガード無し。?max=abc で MAX_ROWS=NaN→行無限増殖、?ttl 不正で setTimeout(...,NaN)=即時発火で全消滅。Number.isFinite && >0 チェックで既定値にフォールバック。\n3) [MEDIUM] app.js/style.css: MessageKind::System が来た時 author色も背景クラスも付かず素表示。.system クラス分岐を追加するか System は非表示にするポリシーを決めて実装。\n4) [MEDIUM] app.js: displayColor を author.style.color に無検証で直挿し。/^#[0-9a-fA-F]{3,8}$/ で検証してから適用。frag.url/badge.imageUrl は http(s)スキームチェック。\n5) [LOW] app.js/style.css: 退場アニメ時間がJS(420ms)とCSS(400ms)で分散。CSS変数 --leave-ms を定義しJSから getComputedStyle で読む等で一元化。\n6) [LOW] index.html: OBSブラウザソースで無意味な viewport meta は削除可(任意)。\nbatch(配列)受信契約と既存の再接続バックオフ(5秒安定後リセット/error→close集約)は維持すること(これらは既に良実装)。templates/default/ 以外を変更しないこと。\n\n## Acceptance criteria (reviewer will check each)\n  1. ?ws= 指定時も channel クエリが正しく付与され、未指定時の既定WS URLでも channel が付く\n  2. ?max / ?ttl が不正値でも Number.isFinite ガードで既定値にフォールバックし暴走しない\n  3. System 種別メッセージが適切に描画(または非表示ポリシー)で破綻しない\n  4. displayColor が #hex 検証後にのみ適用され、url系は http(s)スキーム検証される\n  5. batch配列受信契約と既存の再接続バックオフ(5秒安定後リセット/error→close集約)が維持されている\n  6. templates/default/ 以外を変更していない\n\n## Files you MAY touch (file lock scope)\n  - templates/default/app.js\n  - templates/default/style.css\n  - templates/default/index.html\n\n## Operating rules\n- Stay strictly inside the touchedFiles list. Do not create or edit files outside it.\n- Run any local tests, linters, or type-checks needed to verify your work before finishing.\n- When done, print a short summary of what changed and which acceptance criteria you verified.\n\ncodex\n作業対象を `templates/default` の3ファイルに限定して、まず既存実装の該当箇所を確認します。\nexec\n/bin/bash -lc \"sed -n '1,260p' templates/default/style.css\" in /mnt/d/workspace/fast-comment\nexec\n/bin/bash -lc \"sed -n '1,260p' templates/default/app.js\" in /mnt/d/workspace/fast-comment\nexec\n/bin/bash -lc \"sed -n '1,220p' templates/default/index.html\" in /mnt/d/workspace/fast-comment\n succeeded in 372ms:\n/* OBS Browser Source overlay — transparent background */\n\n*, *::before, *::after {\n  box-sizing: border-box;\n  margin: 0;\n  padding: 0;\n}\n\nhtml, body {\n  width: 100%;\n  height: 100%;\n  background: transparent;\n  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Hiragino Sans', 'Yu Gothic UI', sans-serif;\n  font-size: 14px;\n  overflow: hidden;\n}\n\n#overlay {\n  position: fixed;\n  bottom: 0;\n  left: 0;\n  right: 0;\n  /* max visible rows — adjust via URL param max (default 8) */\n  max-height: 100vh;\n  display: flex;\n  flex-direction: column;\n  justify-content: flex-end;\n  gap: 4px;\n  padding: 8px;\n  pointer-events: none;\n}\n\n/* ---- Comment row ---- */\n.comment {\n  display: flex;\n  align-items: flex-start;\n  gap: 6px;\n  padding: 5px 10px;\n  border-radius: 6px;\n  background: rgba(14, 14, 14, 0.82);\n  backdrop-filter: blur(4px);\n  max-width: 520px;\n  word-break: break-word;\n  line-height: 1.45;\n\n  /* Entrance animation */\n  animation: fade-slide-in 0.25s ease-out both;\n}\n\n/* Exit animation applied via JS */\n.comment.leaving {\n  animation: fade-out 0.4s ease-in forwards;\n}\n\n@keyframes fade-slide-in {\n  from {\n    opacity: 0;\n    transform: translateY(8px);\n  }\n  to {\n    opacity: 1;\n    transform: translateY(0);\n  }\n}\n\n@keyframes fade-out {\n  from { opacity: 1; transform: scale(1); }\n  to   { opacity: 0; transform: scale(0.96); }\n}\n\n/* ---- Platform dot ---- */\n.platform-dot {\n  display: inline-block;\n  width: 8px;\n  height: 8px;\n  border-radius: 50%;\n  flex-shrink: 0;\n  margin-top: 5px;\n}\n\n.platform-dot.twitch  { background: #9146ff; }\n.platform-dot.youtube { background: #ff0000; }\n\n/* ---- Badge images ---- */\n.badge-img {\n  width: 18px;\n  height: 18px;\n  vertical-align: middle;\n  flex-shrink: 0;\n}\n\n/* ---- Author ---- */\n.author {\n  font-weight: 700;\n  font-size: 13px;\n  color: #e0e0e0;\n  flex-shrink: 0;\n  white-space: nowrap;\n}\n\n.sep {\n  color: rgba(255,255,255,0.35);\n  flex-shrink: 0;\n}\n\n/* ---- Text fragments ---- */\n.fragments {\n  color: #f5f5f5;\n  font-size: 14px;\n}\n\n/* ---- Emote images ---- */\n.emote {\n  height: 22px;\n  width: auto;\n  vertical-align: middle;\n}\n\n/* ---- SuperChat / Bits highlight ---- */\n.comment.superchat {\n  background: rgba(255, 214, 0, 0.92);\n  color: #000;\n}\n.comment.superchat .author { color: #000; }\n.comment.superchat .fragments { color: #000; }\n.comment.superchat .sep { color: rgba(0,0,0,0.4); }\n\n.comment.membership {\n  background: rgba(0, 200, 83, 0.88);\n  color: #fff;\n}\n.comment.membership .author { color: #fff; }\n\n.comment.bits {\n  background: rgba(145, 70, 255, 0.85);\n  color: #fff;\n}\n\n/* ---- Amount badge ---- */\n.amount {\n  display: inline-block;\n  background: rgba(0,0,0,0.25);\n  border-radius: 10px;\n  padding: 1px 7px;\n  font-size: 12px;\n  font-weight: 700;\n  flex-shrink: 0;\n}\n\n succeeded in 374ms:\n/**\n * fast-comment OBS overlay — default template\n * No dependencies. Plain ES2020 JS.\n *\n * URL params:\n *   ?template=default   (unused here, consumed by axum router)\n *   ?channel=<id>        optional filter: only show this channel\n *   ?max=8               max visible rows (default 8)\n *   ?ttl=12000           ms before a row auto-exits (default 12000)\n *   ?ws=ws://127.0.0.1:11180/ws  override WS endpoint\n */\n\n(function () {\n  'use strict';\n\n  // ---- Config from URL params ----\n  const params = new URLSearchParams(location.search);\n  const CHANNEL_FILTER = params.get('channel') || null;\n  const MAX_ROWS = parseInt(params.get('max') || '8', 10);\n  const TTL_MS   = parseInt(params.get('ttl')  || '12000', 10);\n  const WS_URL   = params.get('ws') || 'ws://127.0.0.1:11180/ws' + location.search;\n\n  const overlay = document.getElementById('overlay');\n\n  // ---- Active row tracking ----\n  // Each entry: { el, timerId }\n  const rows = [];\n\n  // ---- WebSocket ----\n  let ws = null;\n  let reconnectDelay = 1000;\n  let stableTimer = null;\n\n  function connect() {\n    ws = new WebSocket(WS_URL);\n\n    ws.addEventListener('open', () => {\n      // Reset backoff only after the connection has been stable for 5 s.\n      // This prevents a fast open→close flapping loop from keeping the delay\n      // pinned at 1000 ms (the timer is cancelled in the close handler).\n      stableTimer = setTimeout(() => {\n        reconnectDelay = 1000;\n      }, 5000);\n    });\n\n    ws.addEventListener('message', (ev) => {\n      let batch;\n      try {\n        batch = JSON.parse(ev.data);\n      } catch {\n        return;\n      }\n      if (!Array.isArray(batch)) batch = [batch];\n      batch.forEach(handleMessage);\n    });\n\n    ws.addEventListener('close', () => {\n      // Cancel the stability timer so a premature close does not reset the\n      // backoff before we have had a genuinely stable connection.\n      clearTimeout(stableTimer);\n      stableTimer = null;\n      ws = null;\n      setTimeout(connect, reconnectDelay);\n      reconnectDelay = Math.min(reconnectDelay * 2, 16000);\n    });\n\n    ws.addEventListener('error', () => {\n      ws && ws.close();\n    });\n  }\n\n  // ---- Message handler ----\n  function handleMessage(msg) {\n    // Channel filter\n    if (CHANNEL_FILTER && msg.channel !== CHANNEL_FILTER) return;\n\n    const el = buildRow(msg);\n    addRow(el);\n  }\n\n  // ---- Build DOM row ----\n  function buildRow(msg) {\n    const div = document.createElement('div');\n    div.className = 'comment';\n\n    // Kind class for CSS highlights\n    if (msg.kind === 'superChat')  div.classList.add('superchat');\n    if (msg.kind === 'membership') div.classList.add('membership');\n    if (msg.kind === 'bits')       div.classList.add('bits');\n\n    // Platform dot\n    const dot = document.createElement('span');\n    dot.className = 'platform-dot ' + (msg.platform || '');\n    div.appendChild(dot);\n\n    // Badges\n    const badges = (msg.author && msg.author.badges) || [];\n    badges.forEach((badge) => {\n      if (badge.imageUrl) {\n        const img = document.createElement('img');\n        img.className = 'badge-img';\n        img.src = badge.imageUrl;\n        img.alt = badge.label || '';\n        img.title = badge.label || '';\n        div.appendChild(img);\n      }\n    });\n\n    // Author name\n    const author = document.createElement('span');\n    author.className = 'author';\n    const color = msg.author && msg.author.displayColor;\n    if (color && msg.kind === 'normal') {\n      author.style.color = color;\n    }\n    author.textContent = (msg.author && msg.author.name) || '';\n    div.appendChild(author);\n\n    // Separator\n    const sep = document.createElement('span');\n    sep.className = 'sep';\n    sep.textContent = ': ';\n    div.appendChild(sep);\n\n    // Amount (SuperChat / Bits)\n    if (msg.amount) {\n      const amt = document.createElement('span');\n      amt.className = 'amount';\n      amt.textContent = msg.amount.rawText || '';\n      div.appendChild(amt);\n    }\n\n    // Fragments\n    const fragWrap = document.createElement('span');\n    fragWrap.className = 'fragments';\n    const fragments = msg.fragments || [];\n    fragments.forEach((frag) => {\n      if (frag.type === 'text') {\n        fragWrap.appendChild(document.createTextNode(frag.text));\n      } else if (frag.type === 'emote') {\n        const img = document.createElement('img');\n        img.className = 'emote';\n        img.src = frag.url;\n        img.alt = frag.name || '';\n        img.title = frag.name || '';\n        fragWrap.appendChild(img);\n      }\n    });\n    div.appendChild(fragWrap);\n\n    return div;\n  }\n\n  // ---- Row lifecycle ----\n  function addRow(el) {\n    // Evict oldest if over limit\n    while (rows.length >= MAX_ROWS) {\n      evictOldest();\n    }\n\n    overlay.appendChild(el);\n\n    const timerId = setTimeout(() => removeRow(el), TTL_MS);\n    rows.push({ el, timerId });\n  }\n\n  function evictOldest() {\n    const oldest = rows.shift();\n    if (!oldest) return;\n    clearTimeout(oldest.timerId);\n    startLeave(oldest.el);\n  }\n\n  function removeRow(el) {\n    const idx = rows.findIndex((r) => r.el === el);\n    if (idx !== -1) {\n      clearTimeout(rows[idx].timerId);\n      rows.splice(idx, 1);\n    }\n    startLeave(el);\n  }\n\n  function startLeave(el) {\n    el.classList.add('leaving');\n    // Remove from DOM after animation (matches CSS 0.4s)\n    setTimeout(() => {\n      if (el.parentNode) el.parentNode.removeChild(el);\n    }, 420);\n  }\n\n  // ---- Start ----\n  connect();\n})();\n\n succeeded in 505ms:\n<!doctype html>\n<html lang=\"ja\">\n  <head>\n    <meta charset=\"UTF-8\" />\n    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\" />\n    <title>fast-comment overlay</title>\n    <link rel=\"stylesheet\" href=\"style.css\" />\n  </head>\n  <body>\n    <div id=\"overlay\"></div>\n    <script src=\"app.js\"></script>\n  </body>\n</html>\n\ncodex\n既存の受信処理と再接続まわりはそのまま残せそうです。ここから URL 構築、数値パラメータ、System 表示、URL/色検証、退場時間の一元化だけを狭く差し替えます。\napply patch\npatch: completed\n/mnt/d/workspace/fast-comment/templates/default/app.js\ndiff --git a/templates/default/app.js b/templates/default/app.js\nindex b290518af2c9183bf0de029fa14fb65be50896d5..4e8a47d5d9d35ecd0e429b6ac11e38f0d3b7ff01\n--- a/templates/default/app.js\n+++ b/templates/default/app.js\n@@ -16,11 +16,12 @@\n   // ---- Config from URL params ----\n   const params = new URLSearchParams(location.search);\n   const CHANNEL_FILTER = params.get('channel') || null;\n-  const MAX_ROWS = parseInt(params.get('max') || '8', 10);\n-  const TTL_MS   = parseInt(params.get('ttl')  || '12000', 10);\n-  const WS_URL   = params.get('ws') || 'ws://127.0.0.1:11180/ws' + location.search;\n+  const MAX_ROWS = positiveIntParam('max', 8);\n+  const TTL_MS   = positiveIntParam('ttl', 12000);\n+  const WS_URL   = buildWsUrl(params.get('ws') || 'ws://127.0.0.1:11180/ws');\n \n   const overlay = document.getElementById('overlay');\n+  const LEAVE_MS = cssDurationMs('--leave-ms', 400);\n \n   // ---- Active row tracking ----\n   // Each entry: { el, timerId }\n@@ -82,11 +83,13 @@\n   function buildRow(msg) {\n     const div = document.createElement('div');\n     div.className = 'comment';\n+    const kind = msg.kind || 'normal';\n \n     // Kind class for CSS highlights\n-    if (msg.kind === 'superChat')  div.classList.add('superchat');\n-    if (msg.kind === 'membership') div.classList.add('membership');\n-    if (msg.kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'superChat')  div.classList.add('superchat');\n+    if (kind === 'membership') div.classList.add('membership');\n+    if (kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'system' || kind === 'System') div.classList.add('system');\n \n     // Platform dot\n     const dot = document.createElement('span');\n@@ -96,7 +99,7 @@\n     // Badges\n     const badges = (msg.author && msg.author.badges) || [];\n     badges.forEach((badge) => {\n-      if (badge.imageUrl) {\n+      if (isHttpUrl(badge.imageUrl)) {\n         const img = document.createElement('img');\n         img.className = 'badge-img';\n         img.src = badge.imageUrl;\n@@ -110,7 +113,7 @@\n     const author = document.createElement('span');\n     author.className = 'author';\n     const color = msg.author && msg.author.displayColor;\n-    if (color && msg.kind === 'normal') {\n+    if (isSafeHexColor(color) && kind === 'normal') {\n       author.style.color = color;\n     }\n     author.textContent = (msg.author && msg.author.name) || '';\n@@ -137,7 +140,7 @@\n     fragments.forEach((frag) => {\n       if (frag.type === 'text') {\n         fragWrap.appendChild(document.createTextNode(frag.text));\n-      } else if (frag.type === 'emote') {\n+      } else if (frag.type === 'emote' && isHttpUrl(frag.url)) {\n         const img = document.createElement('img');\n         img.className = 'emote';\n         img.src = frag.url;\n@@ -182,10 +185,52 @@\n \n   function startLeave(el) {\n     el.classList.add('leaving');\n-    // Remove from DOM after animation (matches CSS 0.4s)\n+    // Remove from DOM after the CSS-driven exit animation.\n     setTimeout(() => {\n       if (el.parentNode) el.parentNode.removeChild(el);\n-    }, 420);\n+    }, LEAVE_MS);\n+  }\n+\n+  function positiveIntParam(name, fallback) {\n+    const raw = params.get(name);\n+    if (raw === null) return fallback;\n+\n+    const value = Number(raw);\n+    if (!Number.isFinite(value) || value <= 0) return fallback;\n+    return Math.floor(value);\n+  }\n+\n+  function buildWsUrl(base) {\n+    const url = new URL(base, location.href);\n+    if (CHANNEL_FILTER) {\n+      url.searchParams.set('channel', CHANNEL_FILTER);\n+    }\n+    return url.toString();\n+  }\n+\n+  function isSafeHexColor(value) {\n+    return typeof value === 'string' && /^#[0-9a-fA-F]{3,8}$/.test(value);\n+  }\n+\n+  function isHttpUrl(value) {\n+    if (typeof value !== 'string') return false;\n+\n+    try {\n+      const url = new URL(value);\n+      return url.protocol === 'http:' || url.protocol === 'https:';\n+    } catch {\n+      return false;\n+    }\n+  }\n+\n+  function cssDurationMs(name, fallback) {\n+    const value = getComputedStyle(document.documentElement).getPropertyValue(name).trim();\n+    const match = value.match(/^([0-9]*\\.?[0-9]+)(ms|s)$/);\n+    if (!match) return fallback;\n+\n+    const duration = Number(match[1]);\n+    if (!Number.isFinite(duration) || duration < 0) return fallback;\n+    return match[2] === 's' ? duration * 1000 : duration;\n   }\n \n   // ---- Start ----\n\ndiff --git a/templates/default/app.js b/templates/default/app.js\nindex b290518af2c9183bf0de029fa14fb65be50896d5..4e8a47d5d9d35ecd0e429b6ac11e38f0d3b7ff01\n--- a/templates/default/app.js\n+++ b/templates/default/app.js\n@@ -16,11 +16,12 @@\n   // ---- Config from URL params ----\n   const params = new URLSearchParams(location.search);\n   const CHANNEL_FILTER = params.get('channel') || null;\n-  const MAX_ROWS = parseInt(params.get('max') || '8', 10);\n-  const TTL_MS   = parseInt(params.get('ttl')  || '12000', 10);\n-  const WS_URL   = params.get('ws') || 'ws://127.0.0.1:11180/ws' + location.search;\n+  const MAX_ROWS = positiveIntParam('max', 8);\n+  const TTL_MS   = positiveIntParam('ttl', 12000);\n+  const WS_URL   = buildWsUrl(params.get('ws') || 'ws://127.0.0.1:11180/ws');\n \n   const overlay = document.getElementById('overlay');\n+  const LEAVE_MS = cssDurationMs('--leave-ms', 400);\n \n   // ---- Active row tracking ----\n   // Each entry: { el, timerId }\n@@ -82,11 +83,13 @@\n   function buildRow(msg) {\n     const div = document.createElement('div');\n     div.className = 'comment';\n+    const kind = msg.kind || 'normal';\n \n     // Kind class for CSS highlights\n-    if (msg.kind === 'superChat')  div.classList.add('superchat');\n-    if (msg.kind === 'membership') div.classList.add('membership');\n-    if (msg.kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'superChat')  div.classList.add('superchat');\n+    if (kind === 'membership') div.classList.add('membership');\n+    if (kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'system' || kind === 'System') div.classList.add('system');\n \n     // Platform dot\n     const dot = document.createElement('span');\n@@ -96,7 +99,7 @@\n     // Badges\n     const badges = (msg.author && msg.author.badges) || [];\n     badges.forEach((badge) => {\n-      if (badge.imageUrl) {\n+      if (isHttpUrl(badge.imageUrl)) {\n         const img = document.createElement('img');\n         img.className = 'badge-img';\n         img.src = badge.imageUrl;\n@@ -110,7 +113,7 @@\n     const author = document.createElement('span');\n     author.className = 'author';\n     const color = msg.author && msg.author.displayColor;\n-    if (color && msg.kind === 'normal') {\n+    if (isSafeHexColor(color) && kind === 'normal') {\n       author.style.color = color;\n     }\n     author.textContent = (msg.author && msg.author.name) || '';\n@@ -137,7 +140,7 @@\n     fragments.forEach((frag) => {\n       if (frag.type === 'text') {\n         fragWrap.appendChild(document.createTextNode(frag.text));\n-      } else if (frag.type === 'emote') {\n+      } else if (frag.type === 'emote' && isHttpUrl(frag.url)) {\n         const img = document.createElement('img');\n         img.className = 'emote';\n         img.src = frag.url;\n@@ -182,10 +185,52 @@\n \n   function startLeave(el) {\n     el.classList.add('leaving');\n-    // Remove from DOM after animation (matches CSS 0.4s)\n+    // Remove from DOM after the CSS-driven exit animation.\n     setTimeout(() => {\n       if (el.parentNode) el.parentNode.removeChild(el);\n-    }, 420);\n+    }, LEAVE_MS);\n+  }\n+\n+  function positiveIntParam(name, fallback) {\n+    const raw = params.get(name);\n+    if (raw === null) return fallback;\n+\n+    const value = Number(raw);\n+    if (!Number.isFinite(value) || value <= 0) return fallback;\n+    return Math.floor(value);\n+  }\n+\n+  function buildWsUrl(base) {\n+    const url = new URL(base, location.href);\n+    if (CHANNEL_FILTER) {\n+      url.searchParams.set('channel', CHANNEL_FILTER);\n+    }\n+    return url.toString();\n+  }\n+\n+  function isSafeHexColor(value) {\n+    return typeof value === 'string' && /^#[0-9a-fA-F]{3,8}$/.test(value);\n+  }\n+\n+  function isHttpUrl(value) {\n+    if (typeof value !== 'string') return false;\n+\n+    try {\n+      const url = new URL(value);\n+      return url.protocol === 'http:' || url.protocol === 'https:';\n+    } catch {\n+      return false;\n+    }\n+  }\n+\n+  function cssDurationMs(name, fallback) {\n+    const value = getComputedStyle(document.documentElement).getPropertyValue(name).trim();\n+    const match = value.match(/^([0-9]*\\.?[0-9]+)(ms|s)$/);\n+    if (!match) return fallback;\n+\n+    const duration = Number(match[1]);\n+    if (!Number.isFinite(duration) || duration < 0) return fallback;\n+    return match[2] === 's' ? duration * 1000 : duration;\n   }\n \n   // ---- Start ----\n\napply patch\npatch: completed\n/mnt/d/workspace/fast-comment/templates/default/style.css\ndiff --git a/templates/default/app.js b/templates/default/app.js\nindex b290518af2c9183bf0de029fa14fb65be50896d5..4e8a47d5d9d35ecd0e429b6ac11e38f0d3b7ff01\n--- a/templates/default/app.js\n+++ b/templates/default/app.js\n@@ -16,11 +16,12 @@\n   // ---- Config from URL params ----\n   const params = new URLSearchParams(location.search);\n   const CHANNEL_FILTER = params.get('channel') || null;\n-  const MAX_ROWS = parseInt(params.get('max') || '8', 10);\n-  const TTL_MS   = parseInt(params.get('ttl')  || '12000', 10);\n-  const WS_URL   = params.get('ws') || 'ws://127.0.0.1:11180/ws' + location.search;\n+  const MAX_ROWS = positiveIntParam('max', 8);\n+  const TTL_MS   = positiveIntParam('ttl', 12000);\n+  const WS_URL   = buildWsUrl(params.get('ws') || 'ws://127.0.0.1:11180/ws');\n \n   const overlay = document.getElementById('overlay');\n+  const LEAVE_MS = cssDurationMs('--leave-ms', 400);\n \n   // ---- Active row tracking ----\n   // Each entry: { el, timerId }\n@@ -82,11 +83,13 @@\n   function buildRow(msg) {\n     const div = document.createElement('div');\n     div.className = 'comment';\n+    const kind = msg.kind || 'normal';\n \n     // Kind class for CSS highlights\n-    if (msg.kind === 'superChat')  div.classList.add('superchat');\n-    if (msg.kind === 'membership') div.classList.add('membership');\n-    if (msg.kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'superChat')  div.classList.add('superchat');\n+    if (kind === 'membership') div.classList.add('membership');\n+    if (kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'system' || kind === 'System') div.classList.add('system');\n \n     // Platform dot\n     const dot = document.createElement('span');\n@@ -96,7 +99,7 @@\n     // Badges\n     const badges = (msg.author && msg.author.badges) || [];\n     badges.forEach((badge) => {\n-      if (badge.imageUrl) {\n+      if (isHttpUrl(badge.imageUrl)) {\n         const img = document.createElement('img');\n         img.className = 'badge-img';\n         img.src = badge.imageUrl;\n@@ -110,7 +113,7 @@\n     const author = document.createElement('span');\n     author.className = 'author';\n     const color = msg.author && msg.author.displayColor;\n-    if (color && msg.kind === 'normal') {\n+    if (isSafeHexColor(color) && kind === 'normal') {\n       author.style.color = color;\n     }\n     author.textContent = (msg.author && msg.author.name) || '';\n@@ -137,7 +140,7 @@\n     fragments.forEach((frag) => {\n       if (frag.type === 'text') {\n         fragWrap.appendChild(document.createTextNode(frag.text));\n-      } else if (frag.type === 'emote') {\n+      } else if (frag.type === 'emote' && isHttpUrl(frag.url)) {\n         const img = document.createElement('img');\n         img.className = 'emote';\n         img.src = frag.url;\n@@ -182,10 +185,52 @@\n \n   function startLeave(el) {\n     el.classList.add('leaving');\n-    // Remove from DOM after animation (matches CSS 0.4s)\n+    // Remove from DOM after the CSS-driven exit animation.\n     setTimeout(() => {\n       if (el.parentNode) el.parentNode.removeChild(el);\n-    }, 420);\n+    }, LEAVE_MS);\n+  }\n+\n+  function positiveIntParam(name, fallback) {\n+    const raw = params.get(name);\n+    if (raw === null) return fallback;\n+\n+    const value = Number(raw);\n+    if (!Number.isFinite(value) || value <= 0) return fallback;\n+    return Math.floor(value);\n+  }\n+\n+  function buildWsUrl(base) {\n+    const url = new URL(base, location.href);\n+    if (CHANNEL_FILTER) {\n+      url.searchParams.set('channel', CHANNEL_FILTER);\n+    }\n+    return url.toString();\n+  }\n+\n+  function isSafeHexColor(value) {\n+    return typeof value === 'string' && /^#[0-9a-fA-F]{3,8}$/.test(value);\n+  }\n+\n+  function isHttpUrl(value) {\n+    if (typeof value !== 'string') return false;\n+\n+    try {\n+      const url = new URL(value);\n+      return url.protocol === 'http:' || url.protocol === 'https:';\n+    } catch {\n+      return false;\n+    }\n+  }\n+\n+  function cssDurationMs(name, fallback) {\n+    const value = getComputedStyle(document.documentElement).getPropertyValue(name).trim();\n+    const match = value.match(/^([0-9]*\\.?[0-9]+)(ms|s)$/);\n+    if (!match) return fallback;\n+\n+    const duration = Number(match[1]);\n+    if (!Number.isFinite(duration) || duration < 0) return fallback;\n+    return match[2] === 's' ? duration * 1000 : duration;\n   }\n \n   // ---- Start ----\ndiff --git a/templates/default/style.css b/templates/default/style.css\nindex 05692edd4b93bf60a536289f534944b8655f60f8..4c36f4d0a72bd4762c1aca8a2b3b86291f438a0d\n--- a/templates/default/style.css\n+++ b/templates/default/style.css\n@@ -1,5 +1,9 @@\n /* OBS Browser Source overlay — transparent background */\n \n+:root {\n+  --leave-ms: 400ms;\n+}\n+\n *, *::before, *::after {\n   box-sizing: border-box;\n   margin: 0;\n@@ -49,7 +53,7 @@\n \n /* Exit animation applied via JS */\n .comment.leaving {\n-  animation: fade-out 0.4s ease-in forwards;\n+  animation: fade-out var(--leave-ms) ease-in forwards;\n }\n \n @keyframes fade-slide-in {\n@@ -136,6 +140,15 @@\n   color: #fff;\n }\n \n+.comment.system {\n+  background: rgba(32, 38, 44, 0.88);\n+  color: #d6dde5;\n+  font-style: italic;\n+}\n+.comment.system .author { color: #9fb3c8; }\n+.comment.system .fragments { color: #d6dde5; }\n+.comment.system .sep { color: rgba(214,221,229,0.45); }\n+\n /* ---- Amount badge ---- */\n .amount {\n   display: inline-block;\n\ndiff --git a/templates/default/app.js b/templates/default/app.js\nindex b290518af2c9183bf0de029fa14fb65be50896d5..4e8a47d5d9d35ecd0e429b6ac11e38f0d3b7ff01\n--- a/templates/default/app.js\n+++ b/templates/default/app.js\n@@ -16,11 +16,12 @@\n   // ---- Config from URL params ----\n   const params = new URLSearchParams(location.search);\n   const CHANNEL_FILTER = params.get('channel') || null;\n-  const MAX_ROWS = parseInt(params.get('max') || '8', 10);\n-  const TTL_MS   = parseInt(params.get('ttl')  || '12000', 10);\n-  const WS_URL   = params.get('ws') || 'ws://127.0.0.1:11180/ws' + location.search;\n+  const MAX_ROWS = positiveIntParam('max', 8);\n+  const TTL_MS   = positiveIntParam('ttl', 12000);\n+  const WS_URL   = buildWsUrl(params.get('ws') || 'ws://127.0.0.1:11180/ws');\n \n   const overlay = document.getElementById('overlay');\n+  const LEAVE_MS = cssDurationMs('--leave-ms', 400);\n \n   // ---- Active row tracking ----\n   // Each entry: { el, timerId }\n@@ -82,11 +83,13 @@\n   function buildRow(msg) {\n     const div = document.createElement('div');\n     div.className = 'comment';\n+    const kind = msg.kind || 'normal';\n \n     // Kind class for CSS highlights\n-    if (msg.kind === 'superChat')  div.classList.add('superchat');\n-    if (msg.kind === 'membership') div.classList.add('membership');\n-    if (msg.kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'superChat')  div.classList.add('superchat');\n+    if (kind === 'membership') div.classList.add('membership');\n+    if (kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'system' || kind === 'System') div.classList.add('system');\n \n     // Platform dot\n     const dot = document.createElement('span');\n@@ -96,7 +99,7 @@\n     // Badges\n     const badges = (msg.author && msg.author.badges) || [];\n     badges.forEach((badge) => {\n-      if (badge.imageUrl) {\n+      if (isHttpUrl(badge.imageUrl)) {\n         const img = document.createElement('img');\n         img.className = 'badge-img';\n         img.src = badge.imageUrl;\n@@ -110,7 +113,7 @@\n     const author = document.createElement('span');\n     author.className = 'author';\n     const color = msg.author && msg.author.displayColor;\n-    if (color && msg.kind === 'normal') {\n+    if (isSafeHexColor(color) && kind === 'normal') {\n       author.style.color = color;\n     }\n     author.textContent = (msg.author && msg.author.name) || '';\n@@ -137,7 +140,7 @@\n     fragments.forEach((frag) => {\n       if (frag.type === 'text') {\n         fragWrap.appendChild(document.createTextNode(frag.text));\n-      } else if (frag.type === 'emote') {\n+      } else if (frag.type === 'emote' && isHttpUrl(frag.url)) {\n         const img = document.createElement('img');\n         img.className = 'emote';\n         img.src = frag.url;\n@@ -182,10 +185,52 @@\n \n   function startLeave(el) {\n     el.classList.add('leaving');\n-    // Remove from DOM after animation (matches CSS 0.4s)\n+    // Remove from DOM after the CSS-driven exit animation.\n     setTimeout(() => {\n       if (el.parentNode) el.parentNode.removeChild(el);\n-    }, 420);\n+    }, LEAVE_MS);\n+  }\n+\n+  function positiveIntParam(name, fallback) {\n+    const raw = params.get(name);\n+    if (raw === null) return fallback;\n+\n+    const value = Number(raw);\n+    if (!Number.isFinite(value) || value <= 0) return fallback;\n+    return Math.floor(value);\n+  }\n+\n+  function buildWsUrl(base) {\n+    const url = new URL(base, location.href);\n+    if (CHANNEL_FILTER) {\n+      url.searchParams.set('channel', CHANNEL_FILTER);\n+    }\n+    return url.toString();\n+  }\n+\n+  function isSafeHexColor(value) {\n+    return typeof value === 'string' && /^#[0-9a-fA-F]{3,8}$/.test(value);\n+  }\n+\n+  function isHttpUrl(value) {\n+    if (typeof value !== 'string') return false;\n+\n+    try {\n+      const url = new URL(value);\n+      return url.protocol === 'http:' || url.protocol === 'https:';\n+    } catch {\n+      return false;\n+    }\n+  }\n+\n+  function cssDurationMs(name, fallback) {\n+    const value = getComputedStyle(document.documentElement).getPropertyValue(name).trim();\n+    const match = value.match(/^([0-9]*\\.?[0-9]+)(ms|s)$/);\n+    if (!match) return fallback;\n+\n+    const duration = Number(match[1]);\n+    if (!Number.isFinite(duration) || duration < 0) return fallback;\n+    return match[2] === 's' ? duration * 1000 : duration;\n   }\n \n   // ---- Start ----\ndiff --git a/templates/default/style.css b/templates/default/style.css\nindex 05692edd4b93bf60a536289f534944b8655f60f8..4c36f4d0a72bd4762c1aca8a2b3b86291f438a0d\n--- a/templates/default/style.css\n+++ b/templates/default/style.css\n@@ -1,5 +1,9 @@\n /* OBS Browser Source overlay — transparent background */\n \n+:root {\n+  --leave-ms: 400ms;\n+}\n+\n *, *::before, *::after {\n   box-sizing: border-box;\n   margin: 0;\n@@ -49,7 +53,7 @@\n \n /* Exit animation applied via JS */\n .comment.leaving {\n-  animation: fade-out 0.4s ease-in forwards;\n+  animation: fade-out var(--leave-ms) ease-in forwards;\n }\n \n @keyframes fade-slide-in {\n@@ -136,6 +140,15 @@\n   color: #fff;\n }\n \n+.comment.system {\n+  background: rgba(32, 38, 44, 0.88);\n+  color: #d6dde5;\n+  font-style: italic;\n+}\n+.comment.system .author { color: #9fb3c8; }\n+.comment.system .fragments { color: #d6dde5; }\n+.comment.system .sep { color: rgba(214,221,229,0.45); }\n+\n /* ---- Amount badge ---- */\n .amount {\n   display: inline-block;\n\napply patch\npatch: completed\n/mnt/d/workspace/fast-comment/templates/default/index.html\ndiff --git a/templates/default/app.js b/templates/default/app.js\nindex b290518af2c9183bf0de029fa14fb65be50896d5..4e8a47d5d9d35ecd0e429b6ac11e38f0d3b7ff01\n--- a/templates/default/app.js\n+++ b/templates/default/app.js\n@@ -16,11 +16,12 @@\n   // ---- Config from URL params ----\n   const params = new URLSearchParams(location.search);\n   const CHANNEL_FILTER = params.get('channel') || null;\n-  const MAX_ROWS = parseInt(params.get('max') || '8', 10);\n-  const TTL_MS   = parseInt(params.get('ttl')  || '12000', 10);\n-  const WS_URL   = params.get('ws') || 'ws://127.0.0.1:11180/ws' + location.search;\n+  const MAX_ROWS = positiveIntParam('max', 8);\n+  const TTL_MS   = positiveIntParam('ttl', 12000);\n+  const WS_URL   = buildWsUrl(params.get('ws') || 'ws://127.0.0.1:11180/ws');\n \n   const overlay = document.getElementById('overlay');\n+  const LEAVE_MS = cssDurationMs('--leave-ms', 400);\n \n   // ---- Active row tracking ----\n   // Each entry: { el, timerId }\n@@ -82,11 +83,13 @@\n   function buildRow(msg) {\n     const div = document.createElement('div');\n     div.className = 'comment';\n+    const kind = msg.kind || 'normal';\n \n     // Kind class for CSS highlights\n-    if (msg.kind === 'superChat')  div.classList.add('superchat');\n-    if (msg.kind === 'membership') div.classList.add('membership');\n-    if (msg.kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'superChat')  div.classList.add('superchat');\n+    if (kind === 'membership') div.classList.add('membership');\n+    if (kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'system' || kind === 'System') div.classList.add('system');\n \n     // Platform dot\n     const dot = document.createElement('span');\n@@ -96,7 +99,7 @@\n     // Badges\n     const badges = (msg.author && msg.author.badges) || [];\n     badges.forEach((badge) => {\n-      if (badge.imageUrl) {\n+      if (isHttpUrl(badge.imageUrl)) {\n         const img = document.createElement('img');\n         img.className = 'badge-img';\n         img.src = badge.imageUrl;\n@@ -110,7 +113,7 @@\n     const author = document.createElement('span');\n     author.className = 'author';\n     const color = msg.author && msg.author.displayColor;\n-    if (color && msg.kind === 'normal') {\n+    if (isSafeHexColor(color) && kind === 'normal') {\n       author.style.color = color;\n     }\n     author.textContent = (msg.author && msg.author.name) || '';\n@@ -137,7 +140,7 @@\n     fragments.forEach((frag) => {\n       if (frag.type === 'text') {\n         fragWrap.appendChild(document.createTextNode(frag.text));\n-      } else if (frag.type === 'emote') {\n+      } else if (frag.type === 'emote' && isHttpUrl(frag.url)) {\n         const img = document.createElement('img');\n         img.className = 'emote';\n         img.src = frag.url;\n@@ -182,10 +185,52 @@\n \n   function startLeave(el) {\n     el.classList.add('leaving');\n-    // Remove from DOM after animation (matches CSS 0.4s)\n+    // Remove from DOM after the CSS-driven exit animation.\n     setTimeout(() => {\n       if (el.parentNode) el.parentNode.removeChild(el);\n-    }, 420);\n+    }, LEAVE_MS);\n+  }\n+\n+  function positiveIntParam(name, fallback) {\n+    const raw = params.get(name);\n+    if (raw === null) return fallback;\n+\n+    const value = Number(raw);\n+    if (!Number.isFinite(value) || value <= 0) return fallback;\n+    return Math.floor(value);\n+  }\n+\n+  function buildWsUrl(base) {\n+    const url = new URL(base, location.href);\n+    if (CHANNEL_FILTER) {\n+      url.searchParams.set('channel', CHANNEL_FILTER);\n+    }\n+    return url.toString();\n+  }\n+\n+  function isSafeHexColor(value) {\n+    return typeof value === 'string' && /^#[0-9a-fA-F]{3,8}$/.test(value);\n+  }\n+\n+  function isHttpUrl(value) {\n+    if (typeof value !== 'string') return false;\n+\n+    try {\n+      const url = new URL(value);\n+      return url.protocol === 'http:' || url.protocol === 'https:';\n+    } catch {\n+      return false;\n+    }\n+  }\n+\n+  function cssDurationMs(name, fallback) {\n+    const value = getComputedStyle(document.documentElement).getPropertyValue(name).trim();\n+    const match = value.match(/^([0-9]*\\.?[0-9]+)(ms|s)$/);\n+    if (!match) return fallback;\n+\n+    const duration = Number(match[1]);\n+    if (!Number.isFinite(duration) || duration < 0) return fallback;\n+    return match[2] === 's' ? duration * 1000 : duration;\n   }\n \n   // ---- Start ----\ndiff --git a/templates/default/index.html b/templates/default/index.html\nindex b8f1d118231da15f84dc71fe39a4d288f0689898..75d92c72aa656a00b261450b07a87fdef4d9db66\n--- a/templates/default/index.html\n+++ b/templates/default/index.html\n@@ -2,7 +2,6 @@\n <html lang=\"ja\">\n   <head>\n     <meta charset=\"UTF-8\" />\n-    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\" />\n     <title>fast-comment overlay</title>\n     <link rel=\"stylesheet\" href=\"style.css\" />\n   </head>\ndiff --git a/templates/default/style.css b/templates/default/style.css\nindex 05692edd4b93bf60a536289f534944b8655f60f8..4c36f4d0a72bd4762c1aca8a2b3b86291f438a0d\n--- a/templates/default/style.css\n+++ b/templates/default/style.css\n@@ -1,5 +1,9 @@\n /* OBS Browser Source overlay — transparent background */\n \n+:root {\n+  --leave-ms: 400ms;\n+}\n+\n *, *::before, *::after {\n   box-sizing: border-box;\n   margin: 0;\n@@ -49,7 +53,7 @@\n \n /* Exit animation applied via JS */\n .comment.leaving {\n-  animation: fade-out 0.4s ease-in forwards;\n+  animation: fade-out var(--leave-ms) ease-in forwards;\n }\n \n @keyframes fade-slide-in {\n@@ -136,6 +140,15 @@\n   color: #fff;\n }\n \n+.comment.system {\n+  background: rgba(32, 38, 44, 0.88);\n+  color: #d6dde5;\n+  font-style: italic;\n+}\n+.comment.system .author { color: #9fb3c8; }\n+.comment.system .fragments { color: #d6dde5; }\n+.comment.system .sep { color: rgba(214,221,229,0.45); }\n+\n /* ---- Amount badge ---- */\n .amount {\n   display: inline-block;\n\ndiff --git a/templates/default/app.js b/templates/default/app.js\nindex b290518af2c9183bf0de029fa14fb65be50896d5..4e8a47d5d9d35ecd0e429b6ac11e38f0d3b7ff01\n--- a/templates/default/app.js\n+++ b/templates/default/app.js\n@@ -16,11 +16,12 @@\n   // ---- Config from URL params ----\n   const params = new URLSearchParams(location.search);\n   const CHANNEL_FILTER = params.get('channel') || null;\n-  const MAX_ROWS = parseInt(params.get('max') || '8', 10);\n-  const TTL_MS   = parseInt(params.get('ttl')  || '12000', 10);\n-  const WS_URL   = params.get('ws') || 'ws://127.0.0.1:11180/ws' + location.search;\n+  const MAX_ROWS = positiveIntParam('max', 8);\n+  const TTL_MS   = positiveIntParam('ttl', 12000);\n+  const WS_URL   = buildWsUrl(params.get('ws') || 'ws://127.0.0.1:11180/ws');\n \n   const overlay = document.getElementById('overlay');\n+  const LEAVE_MS = cssDurationMs('--leave-ms', 400);\n \n   // ---- Active row tracking ----\n   // Each entry: { el, timerId }\n@@ -82,11 +83,13 @@\n   function buildRow(msg) {\n     const div = document.createElement('div');\n     div.className = 'comment';\n+    const kind = msg.kind || 'normal';\n \n     // Kind class for CSS highlights\n-    if (msg.kind === 'superChat')  div.classList.add('superchat');\n-    if (msg.kind === 'membership') div.classList.add('membership');\n-    if (msg.kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'superChat')  div.classList.add('superchat');\n+    if (kind === 'membership') div.classList.add('membership');\n+    if (kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'system' || kind === 'System') div.classList.add('system');\n \n     // Platform dot\n     const dot = document.createElement('span');\n@@ -96,7 +99,7 @@\n     // Badges\n     const badges = (msg.author && msg.author.badges) || [];\n     badges.forEach((badge) => {\n-      if (badge.imageUrl) {\n+      if (isHttpUrl(badge.imageUrl)) {\n         const img = document.createElement('img');\n         img.className = 'badge-img';\n         img.src = badge.imageUrl;\n@@ -110,7 +113,7 @@\n     const author = document.createElement('span');\n     author.className = 'author';\n     const color = msg.author && msg.author.displayColor;\n-    if (color && msg.kind === 'normal') {\n+    if (isSafeHexColor(color) && kind === 'normal') {\n       author.style.color = color;\n     }\n     author.textContent = (msg.author && msg.author.name) || '';\n@@ -137,7 +140,7 @@\n     fragments.forEach((frag) => {\n       if (frag.type === 'text') {\n         fragWrap.appendChild(document.createTextNode(frag.text));\n-      } else if (frag.type === 'emote') {\n+      } else if (frag.type === 'emote' && isHttpUrl(frag.url)) {\n         const img = document.createElement('img');\n         img.className = 'emote';\n         img.src = frag.url;\n@@ -182,10 +185,52 @@\n \n   function startLeave(el) {\n     el.classList.add('leaving');\n-    // Remove from DOM after animation (matches CSS 0.4s)\n+    // Remove from DOM after the CSS-driven exit animation.\n     setTimeout(() => {\n       if (el.parentNode) el.parentNode.removeChild(el);\n-    }, 420);\n+    }, LEAVE_MS);\n+  }\n+\n+  function positiveIntParam(name, fallback) {\n+    const raw = params.get(name);\n+    if (raw === null) return fallback;\n+\n+    const value = Number(raw);\n+    if (!Number.isFinite(value) || value <= 0) return fallback;\n+    return Math.floor(value);\n+  }\n+\n+  function buildWsUrl(base) {\n+    const url = new URL(base, location.href);\n+    if (CHANNEL_FILTER) {\n+      url.searchParams.set('channel', CHANNEL_FILTER);\n+    }\n+    return url.toString();\n+  }\n+\n+  function isSafeHexColor(value) {\n+    return typeof value === 'string' && /^#[0-9a-fA-F]{3,8}$/.test(value);\n+  }\n+\n+  function isHttpUrl(value) {\n+    if (typeof value !== 'string') return false;\n+\n+    try {\n+      const url = new URL(value);\n+      return url.protocol === 'http:' || url.protocol === 'https:';\n+    } catch {\n+      return false;\n+    }\n+  }\n+\n+  function cssDurationMs(name, fallback) {\n+    const value = getComputedStyle(document.documentElement).getPropertyValue(name).trim();\n+    const match = value.match(/^([0-9]*\\.?[0-9]+)(ms|s)$/);\n+    if (!match) return fallback;\n+\n+    const duration = Number(match[1]);\n+    if (!Number.isFinite(duration) || duration < 0) return fallback;\n+    return match[2] === 's' ? duration * 1000 : duration;\n   }\n \n   // ---- Start ----\ndiff --git a/templates/default/index.html b/templates/default/index.html\nindex b8f1d118231da15f84dc71fe39a4d288f0689898..75d92c72aa656a00b261450b07a87fdef4d9db66\n--- a/templates/default/index.html\n+++ b/templates/default/index.html\n@@ -2,7 +2,6 @@\n <html lang=\"ja\">\n   <head>\n     <meta charset=\"UTF-8\" />\n-    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\" />\n     <title>fast-comment overlay</title>\n     <link rel=\"stylesheet\" href=\"style.css\" />\n   </head>\ndiff --git a/templates/default/style.css b/templates/default/style.css\nindex 05692edd4b93bf60a536289f534944b8655f60f8..4c36f4d0a72bd4762c1aca8a2b3b86291f438a0d\n--- a/templates/default/style.css\n+++ b/templates/default/style.css\n@@ -1,5 +1,9 @@\n /* OBS Browser Source overlay — transparent background */\n \n+:root {\n+  --leave-ms: 400ms;\n+}\n+\n *, *::before, *::after {\n   box-sizing: border-box;\n   margin: 0;\n@@ -49,7 +53,7 @@\n \n /* Exit animation applied via JS */\n .comment.leaving {\n-  animation: fade-out 0.4s ease-in forwards;\n+  animation: fade-out var(--leave-ms) ease-in forwards;\n }\n \n @keyframes fade-slide-in {\n@@ -136,6 +140,15 @@\n   color: #fff;\n }\n \n+.comment.system {\n+  background: rgba(32, 38, 44, 0.88);\n+  color: #d6dde5;\n+  font-style: italic;\n+}\n+.comment.system .author { color: #9fb3c8; }\n+.comment.system .fragments { color: #d6dde5; }\n+.comment.system .sep { color: rgba(214,221,229,0.45); }\n+\n /* ---- Amount badge ---- */\n .amount {\n   display: inline-block;\n\ncodex\n編集は3ファイル内に収まりました。次に構文チェックと差分確認で、既存の batch 受信・再接続処理を崩していないかも含めて確認します。\nexec\n/bin/bash -lc 'node --check templates/default/app.js' in /mnt/d/workspace/fast-comment\nexec\n/bin/bash -lc 'git diff -- templates/default/app.js templates/default/style.css templates/default/index.html' in /mnt/d/workspace/fast-comment\nexec\n/bin/bash -lc 'git status --short -- templates/default/app.js templates/default/style.css templates/default/index.html' in /mnt/d/workspace/fast-comment\n succeeded in 382ms:\n succeeded in 344ms:\ndiff --git a/templates/default/app.js b/templates/default/app.js\nindex b290518..4e8a47d 100644\n--- a/templates/default/app.js\n+++ b/templates/default/app.js\n@@ -16,11 +16,12 @@\n   // ---- Config from URL params ----\n   const params = new URLSearchParams(location.search);\n   const CHANNEL_FILTER = params.get('channel') || null;\n-  const MAX_ROWS = parseInt(params.get('max') || '8', 10);\n-  const TTL_MS   = parseInt(params.get('ttl')  || '12000', 10);\n-  const WS_URL   = params.get('ws') || 'ws://127.0.0.1:11180/ws' + location.search;\n+  const MAX_ROWS = positiveIntParam('max', 8);\n+  const TTL_MS   = positiveIntParam('ttl', 12000);\n+  const WS_URL   = buildWsUrl(params.get('ws') || 'ws://127.0.0.1:11180/ws');\n \n   const overlay = document.getElementById('overlay');\n+  const LEAVE_MS = cssDurationMs('--leave-ms', 400);\n \n   // ---- Active row tracking ----\n   // Each entry: { el, timerId }\n@@ -82,11 +83,13 @@\n   function buildRow(msg) {\n     const div = document.createElement('div');\n     div.className = 'comment';\n+    const kind = msg.kind || 'normal';\n \n     // Kind class for CSS highlights\n-    if (msg.kind === 'superChat')  div.classList.add('superchat');\n-    if (msg.kind === 'membership') div.classList.add('membership');\n-    if (msg.kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'superChat')  div.classList.add('superchat');\n+    if (kind === 'membership') div.classList.add('membership');\n+    if (kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'system' || kind === 'System') div.classList.add('system');\n \n     // Platform dot\n     const dot = document.createElement('span');\n@@ -96,7 +99,7 @@\n     // Badges\n     const badges = (msg.author && msg.author.badges) || [];\n     badges.forEach((badge) => {\n-      if (badge.imageUrl) {\n+      if (isHttpUrl(badge.imageUrl)) {\n         const img = document.createElement('img');\n         img.className = 'badge-img';\n         img.src = badge.imageUrl;\n@@ -110,7 +113,7 @@\n     const author = document.createElement('span');\n     author.className = 'author';\n     const color = msg.author && msg.author.displayColor;\n-    if (color && msg.kind === 'normal') {\n+    if (isSafeHexColor(color) && kind === 'normal') {\n       author.style.color = color;\n     }\n     author.textContent = (msg.author && msg.author.name) || '';\n@@ -137,7 +140,7 @@\n     fragments.forEach((frag) => {\n       if (frag.type === 'text') {\n         fragWrap.appendChild(document.createTextNode(frag.text));\n-      } else if (frag.type === 'emote') {\n+      } else if (frag.type === 'emote' && isHttpUrl(frag.url)) {\n         const img = document.createElement('img');\n         img.className = 'emote';\n         img.src = frag.url;\n@@ -182,10 +185,52 @@\n \n   function startLeave(el) {\n     el.classList.add('leaving');\n-    // Remove from DOM after animation (matches CSS 0.4s)\n+    // Remove from DOM after the CSS-driven exit animation.\n     setTimeout(() => {\n       if (el.parentNode) el.parentNode.removeChild(el);\n-    }, 420);\n+    }, LEAVE_MS);\n+  }\n+\n+  function positiveIntParam(name, fallback) {\n+    const raw = params.get(name);\n+    if (raw === null) return fallback;\n+\n+    const value = Number(raw);\n+    if (!Number.isFinite(value) || value <= 0) return fallback;\n+    return Math.floor(value);\n+  }\n+\n+  function buildWsUrl(base) {\n+    const url = new URL(base, location.href);\n+    if (CHANNEL_FILTER) {\n+      url.searchParams.set('channel', CHANNEL_FILTER);\n+    }\n+    return url.toString();\n+  }\n+\n+  function isSafeHexColor(value) {\n+    return typeof value === 'string' && /^#[0-9a-fA-F]{3,8}$/.test(value);\n+  }\n+\n+  function isHttpUrl(value) {\n+    if (typeof value !== 'string') return false;\n+\n+    try {\n+      const url = new URL(value);\n+      return url.protocol === 'http:' || url.protocol === 'https:';\n+    } catch {\n+      return false;\n+    }\n+  }\n+\n+  function cssDurationMs(name, fallback) {\n+    const value = getComputedStyle(document.documentElement).getPropertyValue(name).trim();\n+    const match = value.match(/^([0-9]*\\.?[0-9]+)(ms|s)$/);\n+    if (!match) return fallback;\n+\n+    const duration = Number(match[1]);\n+    if (!Number.isFinite(duration) || duration < 0) return fallback;\n+    return match[2] === 's' ? duration * 1000 : duration;\n   }\n \n   // ---- Start ----\ndiff --git a/templates/default/index.html b/templates/default/index.html\nindex b8f1d11..75d92c7 100644\n--- a/templates/default/index.html\n+++ b/templates/default/index.html\n@@ -2,7 +2,6 @@\n <html lang=\"ja\">\n   <head>\n     <meta charset=\"UTF-8\" />\n-    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\" />\n     <title>fast-comment overlay</title>\n     <link rel=\"stylesheet\" href=\"style.css\" />\n   </head>\ndiff --git a/templates/default/style.css b/templates/default/style.css\nindex 05692ed..4c36f4d 100644\n--- a/templates/default/style.css\n+++ b/templates/default/style.css\n@@ -1,5 +1,9 @@\n /* OBS Browser Source overlay — transparent background */\n \n+:root {\n+  --leave-ms: 400ms;\n+}\n+\n *, *::before, *::after {\n   box-sizing: border-box;\n   margin: 0;\n@@ -49,7 +53,7 @@ html, body {\n \n /* Exit animation applied via JS */\n .comment.leaving {\n-  animation: fade-out 0.4s ease-in forwards;\n+  animation: fade-out var(--leave-ms) ease-in forwards;\n }\n \n @keyframes fade-slide-in {\n@@ -136,6 +140,15 @@ html, body {\n   color: #fff;\n }\n \n+.comment.system {\n+  background: rgba(32, 38, 44, 0.88);\n+  color: #d6dde5;\n+  font-style: italic;\n+}\n+.comment.system .author { color: #9fb3c8; }\n+.comment.system .fragments { color: #d6dde5; }\n+.comment.system .sep { color: rgba(214,221,229,0.45); }\n+\n /* ---- Amount badge ---- */\n .amount {\n   display: inline-block;\n\ndiff --git a/templates/default/app.js b/templates/default/app.js\nindex b290518af2c9183bf0de029fa14fb65be50896d5..4e8a47d5d9d35ecd0e429b6ac11e38f0d3b7ff01\n--- a/templates/default/app.js\n+++ b/templates/default/app.js\n@@ -16,11 +16,12 @@\n   // ---- Config from URL params ----\n   const params = new URLSearchParams(location.search);\n   const CHANNEL_FILTER = params.get('channel') || null;\n-  const MAX_ROWS = parseInt(params.get('max') || '8', 10);\n-  const TTL_MS   = parseInt(params.get('ttl')  || '12000', 10);\n-  const WS_URL   = params.get('ws') || 'ws://127.0.0.1:11180/ws' + location.search;\n+  const MAX_ROWS = positiveIntParam('max', 8);\n+  const TTL_MS   = positiveIntParam('ttl', 12000);\n+  const WS_URL   = buildWsUrl(params.get('ws') || 'ws://127.0.0.1:11180/ws');\n \n   const overlay = document.getElementById('overlay');\n+  const LEAVE_MS = cssDurationMs('--leave-ms', 400);\n \n   // ---- Active row tracking ----\n   // Each entry: { el, timerId }\n@@ -82,11 +83,13 @@\n   function buildRow(msg) {\n     const div = document.createElement('div');\n     div.className = 'comment';\n+    const kind = msg.kind || 'normal';\n \n     // Kind class for CSS highlights\n-    if (msg.kind === 'superChat')  div.classList.add('superchat');\n-    if (msg.kind === 'membership') div.classList.add('membership');\n-    if (msg.kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'superChat')  div.classList.add('superchat');\n+    if (kind === 'membership') div.classList.add('membership');\n+    if (kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'system' || kind === 'System') div.classList.add('system');\n \n     // Platform dot\n     const dot = document.createElement('span');\n@@ -96,7 +99,7 @@\n     // Badges\n     const badges = (msg.author && msg.author.badges) || [];\n     badges.forEach((badge) => {\n-      if (badge.imageUrl) {\n+      if (isHttpUrl(badge.imageUrl)) {\n         const img = document.createElement('img');\n         img.className = 'badge-img';\n         img.src = badge.imageUrl;\n@@ -110,7 +113,7 @@\n     const author = document.createElement('span');\n     author.className = 'author';\n     const color = msg.author && msg.author.displayColor;\n-    if (color && msg.kind === 'normal') {\n+    if (isSafeHexColor(color) && kind === 'normal') {\n       author.style.color = color;\n     }\n     author.textContent = (msg.author && msg.author.name) || '';\n@@ -137,7 +140,7 @@\n     fragments.forEach((frag) => {\n       if (frag.type === 'text') {\n         fragWrap.appendChild(document.createTextNode(frag.text));\n-      } else if (frag.type === 'emote') {\n+      } else if (frag.type === 'emote' && isHttpUrl(frag.url)) {\n         const img = document.createElement('img');\n         img.className = 'emote';\n         img.src = frag.url;\n@@ -182,10 +185,52 @@\n \n   function startLeave(el) {\n     el.classList.add('leaving');\n-    // Remove from DOM after animation (matches CSS 0.4s)\n+    // Remove from DOM after the CSS-driven exit animation.\n     setTimeout(() => {\n       if (el.parentNode) el.parentNode.removeChild(el);\n-    }, 420);\n+    }, LEAVE_MS);\n+  }\n+\n+  function positiveIntParam(name, fallback) {\n+    const raw = params.get(name);\n+    if (raw === null) return fallback;\n+\n+    const value = Number(raw);\n+    if (!Number.isFinite(value) || value <= 0) return fallback;\n+    return Math.floor(value);\n+  }\n+\n+  function buildWsUrl(base) {\n+    const url = new URL(base, location.href);\n+    if (CHANNEL_FILTER) {\n+      url.searchParams.set('channel', CHANNEL_FILTER);\n+    }\n+    return url.toString();\n+  }\n+\n+  function isSafeHexColor(value) {\n+    return typeof value === 'string' && /^#[0-9a-fA-F]{3,8}$/.test(value);\n+  }\n+\n+  function isHttpUrl(value) {\n+    if (typeof value !== 'string') return false;\n+\n+    try {\n+      const url = new URL(value);\n+      return url.protocol === 'http:' || url.protocol === 'https:';\n+    } catch {\n+      return false;\n+    }\n+  }\n+\n+  function cssDurationMs(name, fallback) {\n+    const value = getComputedStyle(document.documentElement).getPropertyValue(name).trim();\n+    const match = value.match(/^([0-9]*\\.?[0-9]+)(ms|s)$/);\n+    if (!match) return fallback;\n+\n+    const duration = Number(match[1]);\n+    if (!Number.isFinite(duration) || duration < 0) return fallback;\n+    return match[2] === 's' ? duration * 1000 : duration;\n   }\n \n   // ---- Start ----\ndiff --git a/templates/default/index.html b/templates/default/index.html\nindex b8f1d118231da15f84dc71fe39a4d288f0689898..75d92c72aa656a00b261450b07a87fdef4d9db66\n--- a/templates/default/index.html\n+++ b/templates/default/index.html\n@@ -2,7 +2,6 @@\n <html lang=\"ja\">\n   <head>\n     <meta charset=\"UTF-8\" />\n-    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\" />\n     <title>fast-comment overlay</title>\n     <link rel=\"stylesheet\" href=\"style.css\" />\n   </head>\ndiff --git a/templates/default/style.css b/templates/default/style.css\nindex 05692edd4b93bf60a536289f534944b8655f60f8..4c36f4d0a72bd4762c1aca8a2b3b86291f438a0d\n--- a/templates/default/style.css\n+++ b/templates/default/style.css\n@@ -1,5 +1,9 @@\n /* OBS Browser Source overlay — transparent background */\n \n+:root {\n+  --leave-ms: 400ms;\n+}\n+\n *, *::before, *::after {\n   box-sizing: border-box;\n   margin: 0;\n@@ -49,7 +53,7 @@\n \n /* Exit animation applied via JS */\n .comment.leaving {\n-  animation: fade-out 0.4s ease-in forwards;\n+  animation: fade-out var(--leave-ms) ease-in forwards;\n }\n \n @keyframes fade-slide-in {\n@@ -136,6 +140,15 @@\n   color: #fff;\n }\n \n+.comment.system {\n+  background: rgba(32, 38, 44, 0.88);\n+  color: #d6dde5;\n+  font-style: italic;\n+}\n+.comment.system .author { color: #9fb3c8; }\n+.comment.system .fragments { color: #d6dde5; }\n+.comment.system .sep { color: rgba(214,221,229,0.45); }\n+\n /* ---- Amount badge ---- */\n .amount {\n   display: inline-block;\n\n succeeded in 411ms:\n M templates/default/app.js\n M templates/default/index.html\n M templates/default/style.css\n\nexec\n/bin/bash -lc 'git status --short' in /mnt/d/workspace/fast-comment\ndiff --git a/templates/default/app.js b/templates/default/app.js\nindex b290518af2c9183bf0de029fa14fb65be50896d5..4e8a47d5d9d35ecd0e429b6ac11e38f0d3b7ff01\n--- a/templates/default/app.js\n+++ b/templates/default/app.js\n@@ -16,11 +16,12 @@\n   // ---- Config from URL params ----\n   const params = new URLSearchParams(location.search);\n   const CHANNEL_FILTER = params.get('channel') || null;\n-  const MAX_ROWS = parseInt(params.get('max') || '8', 10);\n-  const TTL_MS   = parseInt(params.get('ttl')  || '12000', 10);\n-  const WS_URL   = params.get('ws') || 'ws://127.0.0.1:11180/ws' + location.search;\n+  const MAX_ROWS = positiveIntParam('max', 8);\n+  const TTL_MS   = positiveIntParam('ttl', 12000);\n+  const WS_URL   = buildWsUrl(params.get('ws') || 'ws://127.0.0.1:11180/ws');\n \n   const overlay = document.getElementById('overlay');\n+  const LEAVE_MS = cssDurationMs('--leave-ms', 400);\n \n   // ---- Active row tracking ----\n   // Each entry: { el, timerId }\n@@ -82,11 +83,13 @@\n   function buildRow(msg) {\n     const div = document.createElement('div');\n     div.className = 'comment';\n+    const kind = msg.kind || 'normal';\n \n     // Kind class for CSS highlights\n-    if (msg.kind === 'superChat')  div.classList.add('superchat');\n-    if (msg.kind === 'membership') div.classList.add('membership');\n-    if (msg.kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'superChat')  div.classList.add('superchat');\n+    if (kind === 'membership') div.classList.add('membership');\n+    if (kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'system' || kind === 'System') div.classList.add('system');\n \n     // Platform dot\n     const dot = document.createElement('span');\n@@ -96,7 +99,7 @@\n     // Badges\n     const badges = (msg.author && msg.author.badges) || [];\n     badges.forEach((badge) => {\n-      if (badge.imageUrl) {\n+      if (isHttpUrl(badge.imageUrl)) {\n         const img = document.createElement('img');\n         img.className = 'badge-img';\n         img.src = badge.imageUrl;\n@@ -110,7 +113,7 @@\n     const author = document.createElement('span');\n     author.className = 'author';\n     const color = msg.author && msg.author.displayColor;\n-    if (color && msg.kind === 'normal') {\n+    if (isSafeHexColor(color) && kind === 'normal') {\n       author.style.color = color;\n     }\n     author.textContent = (msg.author && msg.author.name) || '';\n@@ -137,7 +140,7 @@\n     fragments.forEach((frag) => {\n       if (frag.type === 'text') {\n         fragWrap.appendChild(document.createTextNode(frag.text));\n-      } else if (frag.type === 'emote') {\n+      } else if (frag.type === 'emote' && isHttpUrl(frag.url)) {\n         const img = document.createElement('img');\n         img.className = 'emote';\n         img.src = frag.url;\n@@ -182,10 +185,52 @@\n \n   function startLeave(el) {\n     el.classList.add('leaving');\n-    // Remove from DOM after animation (matches CSS 0.4s)\n+    // Remove from DOM after the CSS-driven exit animation.\n     setTimeout(() => {\n       if (el.parentNode) el.parentNode.removeChild(el);\n-    }, 420);\n+    }, LEAVE_MS);\n+  }\n+\n+  function positiveIntParam(name, fallback) {\n+    const raw = params.get(name);\n+    if (raw === null) return fallback;\n+\n+    const value = Number(raw);\n+    if (!Number.isFinite(value) || value <= 0) return fallback;\n+    return Math.floor(value);\n+  }\n+\n+  function buildWsUrl(base) {\n+    const url = new URL(base, location.href);\n+    if (CHANNEL_FILTER) {\n+      url.searchParams.set('channel', CHANNEL_FILTER);\n+    }\n+    return url.toString();\n+  }\n+\n+  function isSafeHexColor(value) {\n+    return typeof value === 'string' && /^#[0-9a-fA-F]{3,8}$/.test(value);\n+  }\n+\n+  function isHttpUrl(value) {\n+    if (typeof value !== 'string') return false;\n+\n+    try {\n+      const url = new URL(value);\n+      return url.protocol === 'http:' || url.protocol === 'https:';\n+    } catch {\n+      return false;\n+    }\n+  }\n+\n+  function cssDurationMs(name, fallback) {\n+    const value = getComputedStyle(document.documentElement).getPropertyValue(name).trim();\n+    const match = value.match(/^([0-9]*\\.?[0-9]+)(ms|s)$/);\n+    if (!match) return fallback;\n+\n+    const duration = Number(match[1]);\n+    if (!Number.isFinite(duration) || duration < 0) return fallback;\n+    return match[2] === 's' ? duration * 1000 : duration;\n   }\n \n   // ---- Start ----\ndiff --git a/templates/default/index.html b/templates/default/index.html\nindex b8f1d118231da15f84dc71fe39a4d288f0689898..75d92c72aa656a00b261450b07a87fdef4d9db66\n--- a/templates/default/index.html\n+++ b/templates/default/index.html\n@@ -2,7 +2,6 @@\n <html lang=\"ja\">\n   <head>\n     <meta charset=\"UTF-8\" />\n-    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\" />\n     <title>fast-comment overlay</title>\n     <link rel=\"stylesheet\" href=\"style.css\" />\n   </head>\ndiff --git a/templates/default/style.css b/templates/default/style.css\nindex 05692edd4b93bf60a536289f534944b8655f60f8..4c36f4d0a72bd4762c1aca8a2b3b86291f438a0d\n--- a/templates/default/style.css\n+++ b/templates/default/style.css\n@@ -1,5 +1,9 @@\n /* OBS Browser Source overlay — transparent background */\n \n+:root {\n+  --leave-ms: 400ms;\n+}\n+\n *, *::before, *::after {\n   box-sizing: border-box;\n   margin: 0;\n@@ -49,7 +53,7 @@\n \n /* Exit animation applied via JS */\n .comment.leaving {\n-  animation: fade-out 0.4s ease-in forwards;\n+  animation: fade-out var(--leave-ms) ease-in forwards;\n }\n \n @keyframes fade-slide-in {\n@@ -136,6 +140,15 @@\n   color: #fff;\n }\n \n+.comment.system {\n+  background: rgba(32, 38, 44, 0.88);\n+  color: #d6dde5;\n+  font-style: italic;\n+}\n+.comment.system .author { color: #9fb3c8; }\n+.comment.system .fragments { color: #d6dde5; }\n+.comment.system .sep { color: rgba(214,221,229,0.45); }\n+\n /* ---- Amount badge ---- */\n .amount {\n   display: inline-block;\n\n succeeded in 620ms:\n M .omc/project-memory.json\n M .omc/state/agent-replay-4b0a4f91-acf2-4764-bdb1-5d88fdbd50d0.jsonl\n M .omc/state/hud-stdin-cache.json\n M .omc/state/last-tool-error.json\n M .omc/state/mission-state.json\n M src-tauri/src/sources/twitch.rs\n M src-tauri/src/sources/youtube/innertube.rs\n M src-tauri/src/sources/youtube/mod.rs\n M src-tauri/src/sources/youtube/parser.rs\n M src-tauri/src/tts/mod.rs\n M src-tauri/src/tts/voicevox.rs\n M src/lib/components/CommentItem.svelte\n M src/lib/components/CommentList.svelte\n M src/lib/ipc.ts\n M src/lib/stores.svelte.ts\n M templates/default/app.js\n M templates/default/index.html\n M templates/default/style.css\n?? .omc/fullralph-prd.json\n?? .omc/state/fullralph-locks.json\n?? .omc/state/fullralph-results/\n?? .omc/state/subagent-tracking.json\n?? src-tauri/icons/\n\ncodex\n数値ガードの確認中に、`max=0.5` のような正の小数を整数化すると 0 になり得る点がありました。暴走防止の趣旨に合わせて、整数化後も 1 以上のときだけ採用する形に締めます。\napply patch\npatch: completed\n/mnt/d/workspace/fast-comment/templates/default/app.js\ndiff --git a/templates/default/app.js b/templates/default/app.js\nindex b290518af2c9183bf0de029fa14fb65be50896d5..5e84608e1ef8bdb6c97a73f27762c935320b0818\n--- a/templates/default/app.js\n+++ b/templates/default/app.js\n@@ -16,11 +16,12 @@\n   // ---- Config from URL params ----\n   const params = new URLSearchParams(location.search);\n   const CHANNEL_FILTER = params.get('channel') || null;\n-  const MAX_ROWS = parseInt(params.get('max') || '8', 10);\n-  const TTL_MS   = parseInt(params.get('ttl')  || '12000', 10);\n-  const WS_URL   = params.get('ws') || 'ws://127.0.0.1:11180/ws' + location.search;\n+  const MAX_ROWS = positiveIntParam('max', 8);\n+  const TTL_MS   = positiveIntParam('ttl', 12000);\n+  const WS_URL   = buildWsUrl(params.get('ws') || 'ws://127.0.0.1:11180/ws');\n \n   const overlay = document.getElementById('overlay');\n+  const LEAVE_MS = cssDurationMs('--leave-ms', 400);\n \n   // ---- Active row tracking ----\n   // Each entry: { el, timerId }\n@@ -82,11 +83,13 @@\n   function buildRow(msg) {\n     const div = document.createElement('div');\n     div.className = 'comment';\n+    const kind = msg.kind || 'normal';\n \n     // Kind class for CSS highlights\n-    if (msg.kind === 'superChat')  div.classList.add('superchat');\n-    if (msg.kind === 'membership') div.classList.add('membership');\n-    if (msg.kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'superChat')  div.classList.add('superchat');\n+    if (kind === 'membership') div.classList.add('membership');\n+    if (kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'system' || kind === 'System') div.classList.add('system');\n \n     // Platform dot\n     const dot = document.createElement('span');\n@@ -96,7 +99,7 @@\n     // Badges\n     const badges = (msg.author && msg.author.badges) || [];\n     badges.forEach((badge) => {\n-      if (badge.imageUrl) {\n+      if (isHttpUrl(badge.imageUrl)) {\n         const img = document.createElement('img');\n         img.className = 'badge-img';\n         img.src = badge.imageUrl;\n@@ -110,7 +113,7 @@\n     const author = document.createElement('span');\n     author.className = 'author';\n     const color = msg.author && msg.author.displayColor;\n-    if (color && msg.kind === 'normal') {\n+    if (isSafeHexColor(color) && kind === 'normal') {\n       author.style.color = color;\n     }\n     author.textContent = (msg.author && msg.author.name) || '';\n@@ -137,7 +140,7 @@\n     fragments.forEach((frag) => {\n       if (frag.type === 'text') {\n         fragWrap.appendChild(document.createTextNode(frag.text));\n-      } else if (frag.type === 'emote') {\n+      } else if (frag.type === 'emote' && isHttpUrl(frag.url)) {\n         const img = document.createElement('img');\n         img.className = 'emote';\n         img.src = frag.url;\n@@ -182,10 +185,53 @@\n \n   function startLeave(el) {\n     el.classList.add('leaving');\n-    // Remove from DOM after animation (matches CSS 0.4s)\n+    // Remove from DOM after the CSS-driven exit animation.\n     setTimeout(() => {\n       if (el.parentNode) el.parentNode.removeChild(el);\n-    }, 420);\n+    }, LEAVE_MS);\n+  }\n+\n+  function positiveIntParam(name, fallback) {\n+    const raw = params.get(name);\n+    if (raw === null) return fallback;\n+\n+    const value = Number(raw);\n+    if (!Number.isFinite(value) || value <= 0) return fallback;\n+    const intValue = Math.floor(value);\n+    return intValue > 0 ? intValue : fallback;\n+  }\n+\n+  function buildWsUrl(base) {\n+    const url = new URL(base, location.href);\n+    if (CHANNEL_FILTER) {\n+      url.searchParams.set('channel', CHANNEL_FILTER);\n+    }\n+    return url.toString();\n+  }\n+\n+  function isSafeHexColor(value) {\n+    return typeof value === 'string' && /^#[0-9a-fA-F]{3,8}$/.test(value);\n+  }\n+\n+  function isHttpUrl(value) {\n+    if (typeof value !== 'string') return false;\n+\n+    try {\n+      const url = new URL(value);\n+      return url.protocol === 'http:' || url.protocol === 'https:';\n+    } catch {\n+      return false;\n+    }\n+  }\n+\n+  function cssDurationMs(name, fallback) {\n+    const value = getComputedStyle(document.documentElement).getPropertyValue(name).trim();\n+    const match = value.match(/^([0-9]*\\.?[0-9]+)(ms|s)$/);\n+    if (!match) return fallback;\n+\n+    const duration = Number(match[1]);\n+    if (!Number.isFinite(duration) || duration < 0) return fallback;\n+    return match[2] === 's' ? duration * 1000 : duration;\n   }\n \n   // ---- Start ----\ndiff --git a/templates/default/index.html b/templates/default/index.html\nindex b8f1d118231da15f84dc71fe39a4d288f0689898..75d92c72aa656a00b261450b07a87fdef4d9db66\n--- a/templates/default/index.html\n+++ b/templates/default/index.html\n@@ -2,7 +2,6 @@\n <html lang=\"ja\">\n   <head>\n     <meta charset=\"UTF-8\" />\n-    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\" />\n     <title>fast-comment overlay</title>\n     <link rel=\"stylesheet\" href=\"style.css\" />\n   </head>\ndiff --git a/templates/default/style.css b/templates/default/style.css\nindex 05692edd4b93bf60a536289f534944b8655f60f8..4c36f4d0a72bd4762c1aca8a2b3b86291f438a0d\n--- a/templates/default/style.css\n+++ b/templates/default/style.css\n@@ -1,5 +1,9 @@\n /* OBS Browser Source overlay — transparent background */\n \n+:root {\n+  --leave-ms: 400ms;\n+}\n+\n *, *::before, *::after {\n   box-sizing: border-box;\n   margin: 0;\n@@ -49,7 +53,7 @@\n \n /* Exit animation applied via JS */\n .comment.leaving {\n-  animation: fade-out 0.4s ease-in forwards;\n+  animation: fade-out var(--leave-ms) ease-in forwards;\n }\n \n @keyframes fade-slide-in {\n@@ -136,6 +140,15 @@\n   color: #fff;\n }\n \n+.comment.system {\n+  background: rgba(32, 38, 44, 0.88);\n+  color: #d6dde5;\n+  font-style: italic;\n+}\n+.comment.system .author { color: #9fb3c8; }\n+.comment.system .fragments { color: #d6dde5; }\n+.comment.system .sep { color: rgba(214,221,229,0.45); }\n+\n /* ---- Amount badge ---- */\n .amount {\n   display: inline-block;\n\ndiff --git a/templates/default/app.js b/templates/default/app.js\nindex b290518af2c9183bf0de029fa14fb65be50896d5..5e84608e1ef8bdb6c97a73f27762c935320b0818\n--- a/templates/default/app.js\n+++ b/templates/default/app.js\n@@ -16,11 +16,12 @@\n   // ---- Config from URL params ----\n   const params = new URLSearchParams(location.search);\n   const CHANNEL_FILTER = params.get('channel') || null;\n-  const MAX_ROWS = parseInt(params.get('max') || '8', 10);\n-  const TTL_MS   = parseInt(params.get('ttl')  || '12000', 10);\n-  const WS_URL   = params.get('ws') || 'ws://127.0.0.1:11180/ws' + location.search;\n+  const MAX_ROWS = positiveIntParam('max', 8);\n+  const TTL_MS   = positiveIntParam('ttl', 12000);\n+  const WS_URL   = buildWsUrl(params.get('ws') || 'ws://127.0.0.1:11180/ws');\n \n   const overlay = document.getElementById('overlay');\n+  const LEAVE_MS = cssDurationMs('--leave-ms', 400);\n \n   // ---- Active row tracking ----\n   // Each entry: { el, timerId }\n@@ -82,11 +83,13 @@\n   function buildRow(msg) {\n     const div = document.createElement('div');\n     div.className = 'comment';\n+    const kind = msg.kind || 'normal';\n \n     // Kind class for CSS highlights\n-    if (msg.kind === 'superChat')  div.classList.add('superchat');\n-    if (msg.kind === 'membership') div.classList.add('membership');\n-    if (msg.kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'superChat')  div.classList.add('superchat');\n+    if (kind === 'membership') div.classList.add('membership');\n+    if (kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'system' || kind === 'System') div.classList.add('system');\n \n     // Platform dot\n     const dot = document.createElement('span');\n@@ -96,7 +99,7 @@\n     // Badges\n     const badges = (msg.author && msg.author.badges) || [];\n     badges.forEach((badge) => {\n-      if (badge.imageUrl) {\n+      if (isHttpUrl(badge.imageUrl)) {\n         const img = document.createElement('img');\n         img.className = 'badge-img';\n         img.src = badge.imageUrl;\n@@ -110,7 +113,7 @@\n     const author = document.createElement('span');\n     author.className = 'author';\n     const color = msg.author && msg.author.displayColor;\n-    if (color && msg.kind === 'normal') {\n+    if (isSafeHexColor(color) && kind === 'normal') {\n       author.style.color = color;\n     }\n     author.textContent = (msg.author && msg.author.name) || '';\n@@ -137,7 +140,7 @@\n     fragments.forEach((frag) => {\n       if (frag.type === 'text') {\n         fragWrap.appendChild(document.createTextNode(frag.text));\n-      } else if (frag.type === 'emote') {\n+      } else if (frag.type === 'emote' && isHttpUrl(frag.url)) {\n         const img = document.createElement('img');\n         img.className = 'emote';\n         img.src = frag.url;\n@@ -182,10 +185,53 @@\n \n   function startLeave(el) {\n     el.classList.add('leaving');\n-    // Remove from DOM after animation (matches CSS 0.4s)\n+    // Remove from DOM after the CSS-driven exit animation.\n     setTimeout(() => {\n       if (el.parentNode) el.parentNode.removeChild(el);\n-    }, 420);\n+    }, LEAVE_MS);\n+  }\n+\n+  function positiveIntParam(name, fallback) {\n+    const raw = params.get(name);\n+    if (raw === null) return fallback;\n+\n+    const value = Number(raw);\n+    if (!Number.isFinite(value) || value <= 0) return fallback;\n+    const intValue = Math.floor(value);\n+    return intValue > 0 ? intValue : fallback;\n+  }\n+\n+  function buildWsUrl(base) {\n+    const url = new URL(base, location.href);\n+    if (CHANNEL_FILTER) {\n+      url.searchParams.set('channel', CHANNEL_FILTER);\n+    }\n+    return url.toString();\n+  }\n+\n+  function isSafeHexColor(value) {\n+    return typeof value === 'string' && /^#[0-9a-fA-F]{3,8}$/.test(value);\n+  }\n+\n+  function isHttpUrl(value) {\n+    if (typeof value !== 'string') return false;\n+\n+    try {\n+      const url = new URL(value);\n+      return url.protocol === 'http:' || url.protocol === 'https:';\n+    } catch {\n+      return false;\n+    }\n+  }\n+\n+  function cssDurationMs(name, fallback) {\n+    const value = getComputedStyle(document.documentElement).getPropertyValue(name).trim();\n+    const match = value.match(/^([0-9]*\\.?[0-9]+)(ms|s)$/);\n+    if (!match) return fallback;\n+\n+    const duration = Number(match[1]);\n+    if (!Number.isFinite(duration) || duration < 0) return fallback;\n+    return match[2] === 's' ? duration * 1000 : duration;\n   }\n \n   // ---- Start ----\ndiff --git a/templates/default/index.html b/templates/default/index.html\nindex b8f1d118231da15f84dc71fe39a4d288f0689898..75d92c72aa656a00b261450b07a87fdef4d9db66\n--- a/templates/default/index.html\n+++ b/templates/default/index.html\n@@ -2,7 +2,6 @@\n <html lang=\"ja\">\n   <head>\n     <meta charset=\"UTF-8\" />\n-    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\" />\n     <title>fast-comment overlay</title>\n     <link rel=\"stylesheet\" href=\"style.css\" />\n   </head>\ndiff --git a/templates/default/style.css b/templates/default/style.css\nindex 05692edd4b93bf60a536289f534944b8655f60f8..4c36f4d0a72bd4762c1aca8a2b3b86291f438a0d\n--- a/templates/default/style.css\n+++ b/templates/default/style.css\n@@ -1,5 +1,9 @@\n /* OBS Browser Source overlay — transparent background */\n \n+:root {\n+  --leave-ms: 400ms;\n+}\n+\n *, *::before, *::after {\n   box-sizing: border-box;\n   margin: 0;\n@@ -49,7 +53,7 @@\n \n /* Exit animation applied via JS */\n .comment.leaving {\n-  animation: fade-out 0.4s ease-in forwards;\n+  animation: fade-out var(--leave-ms) ease-in forwards;\n }\n \n @keyframes fade-slide-in {\n@@ -136,6 +140,15 @@\n   color: #fff;\n }\n \n+.comment.system {\n+  background: rgba(32, 38, 44, 0.88);\n+  color: #d6dde5;\n+  font-style: italic;\n+}\n+.comment.system .author { color: #9fb3c8; }\n+.comment.system .fragments { color: #d6dde5; }\n+.comment.system .sep { color: rgba(214,221,229,0.45); }\n+\n /* ---- Amount badge ---- */\n .amount {\n   display: inline-block;\n\napply patch\npatch: completed\n/mnt/d/workspace/fast-comment/templates/default/app.js\ndiff --git a/templates/default/app.js b/templates/default/app.js\nindex b290518af2c9183bf0de029fa14fb65be50896d5..ac1697525bcc291262f875695ca4608b39acebd7\n--- a/templates/default/app.js\n+++ b/templates/default/app.js\n@@ -16,11 +16,12 @@\n   // ---- Config from URL params ----\n   const params = new URLSearchParams(location.search);\n   const CHANNEL_FILTER = params.get('channel') || null;\n-  const MAX_ROWS = parseInt(params.get('max') || '8', 10);\n-  const TTL_MS   = parseInt(params.get('ttl')  || '12000', 10);\n-  const WS_URL   = params.get('ws') || 'ws://127.0.0.1:11180/ws' + location.search;\n+  const MAX_ROWS = positiveIntParam('max', 8);\n+  const TTL_MS   = positiveIntParam('ttl', 12000);\n+  const WS_URL   = buildWsUrl(params.get('ws') || 'ws://127.0.0.1:11180/ws');\n \n   const overlay = document.getElementById('overlay');\n+  const LEAVE_MS = cssDurationMs('--leave-ms', 400);\n \n   // ---- Active row tracking ----\n   // Each entry: { el, timerId }\n@@ -82,11 +83,14 @@\n   function buildRow(msg) {\n     const div = document.createElement('div');\n     div.className = 'comment';\n+    const kind = msg.kind || 'normal';\n+    const isSystem = kind === 'system' || kind === 'System';\n \n     // Kind class for CSS highlights\n-    if (msg.kind === 'superChat')  div.classList.add('superchat');\n-    if (msg.kind === 'membership') div.classList.add('membership');\n-    if (msg.kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'superChat')  div.classList.add('superchat');\n+    if (kind === 'membership') div.classList.add('membership');\n+    if (kind === 'bits')       div.classList.add('bits');\n+    if (isSystem) div.classList.add('system');\n \n     // Platform dot\n     const dot = document.createElement('span');\n@@ -96,7 +100,7 @@\n     // Badges\n     const badges = (msg.author && msg.author.badges) || [];\n     badges.forEach((badge) => {\n-      if (badge.imageUrl) {\n+      if (isHttpUrl(badge.imageUrl)) {\n         const img = document.createElement('img');\n         img.className = 'badge-img';\n         img.src = badge.imageUrl;\n@@ -110,10 +114,10 @@\n     const author = document.createElement('span');\n     author.className = 'author';\n     const color = msg.author && msg.author.displayColor;\n-    if (color && msg.kind === 'normal') {\n+    if (isSafeHexColor(color) && kind === 'normal') {\n       author.style.color = color;\n     }\n-    author.textContent = (msg.author && msg.author.name) || '';\n+    author.textContent = (msg.author && msg.author.name) || (isSystem ? 'System' : '');\n     div.appendChild(author);\n \n     // Separator\n@@ -137,7 +141,7 @@\n     fragments.forEach((frag) => {\n       if (frag.type === 'text') {\n         fragWrap.appendChild(document.createTextNode(frag.text));\n-      } else if (frag.type === 'emote') {\n+      } else if (frag.type === 'emote' && isHttpUrl(frag.url)) {\n         const img = document.createElement('img');\n         img.className = 'emote';\n         img.src = frag.url;\n@@ -182,10 +186,53 @@\n \n   function startLeave(el) {\n     el.classList.add('leaving');\n-    // Remove from DOM after animation (matches CSS 0.4s)\n+    // Remove from DOM after the CSS-driven exit animation.\n     setTimeout(() => {\n       if (el.parentNode) el.parentNode.removeChild(el);\n-    }, 420);\n+    }, LEAVE_MS);\n+  }\n+\n+  function positiveIntParam(name, fallback) {\n+    const raw = params.get(name);\n+    if (raw === null) return fallback;\n+\n+    const value = Number(raw);\n+    if (!Number.isFinite(value) || value <= 0) return fallback;\n+    const intValue = Math.floor(value);\n+    return intValue > 0 ? intValue : fallback;\n+  }\n+\n+  function buildWsUrl(base) {\n+    const url = new URL(base, location.href);\n+    if (CHANNEL_FILTER) {\n+      url.searchParams.set('channel', CHANNEL_FILTER);\n+    }\n+    return url.toString();\n+  }\n+\n+  function isSafeHexColor(value) {\n+    return typeof value === 'string' && /^#[0-9a-fA-F]{3,8}$/.test(value);\n+  }\n+\n+  function isHttpUrl(value) {\n+    if (typeof value !== 'string') return false;\n+\n+    try {\n+      const url = new URL(value);\n+      return url.protocol === 'http:' || url.protocol === 'https:';\n+    } catch {\n+      return false;\n+    }\n+  }\n+\n+  function cssDurationMs(name, fallback) {\n+    const value = getComputedStyle(document.documentElement).getPropertyValue(name).trim();\n+    const match = value.match(/^([0-9]*\\.?[0-9]+)(ms|s)$/);\n+    if (!match) return fallback;\n+\n+    const duration = Number(match[1]);\n+    if (!Number.isFinite(duration) || duration < 0) return fallback;\n+    return match[2] === 's' ? duration * 1000 : duration;\n   }\n \n   // ---- Start ----\ndiff --git a/templates/default/index.html b/templates/default/index.html\nindex b8f1d118231da15f84dc71fe39a4d288f0689898..75d92c72aa656a00b261450b07a87fdef4d9db66\n--- a/templates/default/index.html\n+++ b/templates/default/index.html\n@@ -2,7 +2,6 @@\n <html lang=\"ja\">\n   <head>\n     <meta charset=\"UTF-8\" />\n-    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\" />\n     <title>fast-comment overlay</title>\n     <link rel=\"stylesheet\" href=\"style.css\" />\n   </head>\ndiff --git a/templates/default/style.css b/templates/default/style.css\nindex 05692edd4b93bf60a536289f534944b8655f60f8..4c36f4d0a72bd4762c1aca8a2b3b86291f438a0d\n--- a/templates/default/style.css\n+++ b/templates/default/style.css\n@@ -1,5 +1,9 @@\n /* OBS Browser Source overlay — transparent background */\n \n+:root {\n+  --leave-ms: 400ms;\n+}\n+\n *, *::before, *::after {\n   box-sizing: border-box;\n   margin: 0;\n@@ -49,7 +53,7 @@\n \n /* Exit animation applied via JS */\n .comment.leaving {\n-  animation: fade-out 0.4s ease-in forwards;\n+  animation: fade-out var(--leave-ms) ease-in forwards;\n }\n \n @keyframes fade-slide-in {\n@@ -136,6 +140,15 @@\n   color: #fff;\n }\n \n+.comment.system {\n+  background: rgba(32, 38, 44, 0.88);\n+  color: #d6dde5;\n+  font-style: italic;\n+}\n+.comment.system .author { color: #9fb3c8; }\n+.comment.system .fragments { color: #d6dde5; }\n+.comment.system .sep { color: rgba(214,221,229,0.45); }\n+\n /* ---- Amount badge ---- */\n .amount {\n   display: inline-block;\n\ndiff --git a/templates/default/app.js b/templates/default/app.js\nindex b290518af2c9183bf0de029fa14fb65be50896d5..ac1697525bcc291262f875695ca4608b39acebd7\n--- a/templates/default/app.js\n+++ b/templates/default/app.js\n@@ -16,11 +16,12 @@\n   // ---- Config from URL params ----\n   const params = new URLSearchParams(location.search);\n   const CHANNEL_FILTER = params.get('channel') || null;\n-  const MAX_ROWS = parseInt(params.get('max') || '8', 10);\n-  const TTL_MS   = parseInt(params.get('ttl')  || '12000', 10);\n-  const WS_URL   = params.get('ws') || 'ws://127.0.0.1:11180/ws' + location.search;\n+  const MAX_ROWS = positiveIntParam('max', 8);\n+  const TTL_MS   = positiveIntParam('ttl', 12000);\n+  const WS_URL   = buildWsUrl(params.get('ws') || 'ws://127.0.0.1:11180/ws');\n \n   const overlay = document.getElementById('overlay');\n+  const LEAVE_MS = cssDurationMs('--leave-ms', 400);\n \n   // ---- Active row tracking ----\n   // Each entry: { el, timerId }\n@@ -82,11 +83,14 @@\n   function buildRow(msg) {\n     const div = document.createElement('div');\n     div.className = 'comment';\n+    const kind = msg.kind || 'normal';\n+    const isSystem = kind === 'system' || kind === 'System';\n \n     // Kind class for CSS highlights\n-    if (msg.kind === 'superChat')  div.classList.add('superchat');\n-    if (msg.kind === 'membership') div.classList.add('membership');\n-    if (msg.kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'superChat')  div.classList.add('superchat');\n+    if (kind === 'membership') div.classList.add('membership');\n+    if (kind === 'bits')       div.classList.add('bits');\n+    if (isSystem) div.classList.add('system');\n \n     // Platform dot\n     const dot = document.createElement('span');\n@@ -96,7 +100,7 @@\n     // Badges\n     const badges = (msg.author && msg.author.badges) || [];\n     badges.forEach((badge) => {\n-      if (badge.imageUrl) {\n+      if (isHttpUrl(badge.imageUrl)) {\n         const img = document.createElement('img');\n         img.className = 'badge-img';\n         img.src = badge.imageUrl;\n@@ -110,10 +114,10 @@\n     const author = document.createElement('span');\n     author.className = 'author';\n     const color = msg.author && msg.author.displayColor;\n-    if (color && msg.kind === 'normal') {\n+    if (isSafeHexColor(color) && kind === 'normal') {\n       author.style.color = color;\n     }\n-    author.textContent = (msg.author && msg.author.name) || '';\n+    author.textContent = (msg.author && msg.author.name) || (isSystem ? 'System' : '');\n     div.appendChild(author);\n \n     // Separator\n@@ -137,7 +141,7 @@\n     fragments.forEach((frag) => {\n       if (frag.type === 'text') {\n         fragWrap.appendChild(document.createTextNode(frag.text));\n-      } else if (frag.type === 'emote') {\n+      } else if (frag.type === 'emote' && isHttpUrl(frag.url)) {\n         const img = document.createElement('img');\n         img.className = 'emote';\n         img.src = frag.url;\n@@ -182,10 +186,53 @@\n \n   function startLeave(el) {\n     el.classList.add('leaving');\n-    // Remove from DOM after animation (matches CSS 0.4s)\n+    // Remove from DOM after the CSS-driven exit animation.\n     setTimeout(() => {\n       if (el.parentNode) el.parentNode.removeChild(el);\n-    }, 420);\n+    }, LEAVE_MS);\n+  }\n+\n+  function positiveIntParam(name, fallback) {\n+    const raw = params.get(name);\n+    if (raw === null) return fallback;\n+\n+    const value = Number(raw);\n+    if (!Number.isFinite(value) || value <= 0) return fallback;\n+    const intValue = Math.floor(value);\n+    return intValue > 0 ? intValue : fallback;\n+  }\n+\n+  function buildWsUrl(base) {\n+    const url = new URL(base, location.href);\n+    if (CHANNEL_FILTER) {\n+      url.searchParams.set('channel', CHANNEL_FILTER);\n+    }\n+    return url.toString();\n+  }\n+\n+  function isSafeHexColor(value) {\n+    return typeof value === 'string' && /^#[0-9a-fA-F]{3,8}$/.test(value);\n+  }\n+\n+  function isHttpUrl(value) {\n+    if (typeof value !== 'string') return false;\n+\n+    try {\n+      const url = new URL(value);\n+      return url.protocol === 'http:' || url.protocol === 'https:';\n+    } catch {\n+      return false;\n+    }\n+  }\n+\n+  function cssDurationMs(name, fallback) {\n+    const value = getComputedStyle(document.documentElement).getPropertyValue(name).trim();\n+    const match = value.match(/^([0-9]*\\.?[0-9]+)(ms|s)$/);\n+    if (!match) return fallback;\n+\n+    const duration = Number(match[1]);\n+    if (!Number.isFinite(duration) || duration < 0) return fallback;\n+    return match[2] === 's' ? duration * 1000 : duration;\n   }\n \n   // ---- Start ----\ndiff --git a/templates/default/index.html b/templates/default/index.html\nindex b8f1d118231da15f84dc71fe39a4d288f0689898..75d92c72aa656a00b261450b07a87fdef4d9db66\n--- a/templates/default/index.html\n+++ b/templates/default/index.html\n@@ -2,7 +2,6 @@\n <html lang=\"ja\">\n   <head>\n     <meta charset=\"UTF-8\" />\n-    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\" />\n     <title>fast-comment overlay</title>\n     <link rel=\"stylesheet\" href=\"style.css\" />\n   </head>\ndiff --git a/templates/default/style.css b/templates/default/style.css\nindex 05692edd4b93bf60a536289f534944b8655f60f8..4c36f4d0a72bd4762c1aca8a2b3b86291f438a0d\n--- a/templates/default/style.css\n+++ b/templates/default/style.css\n@@ -1,5 +1,9 @@\n /* OBS Browser Source overlay — transparent background */\n \n+:root {\n+  --leave-ms: 400ms;\n+}\n+\n *, *::before, *::after {\n   box-sizing: border-box;\n   margin: 0;\n@@ -49,7 +53,7 @@\n \n /* Exit animation applied via JS */\n .comment.leaving {\n-  animation: fade-out 0.4s ease-in forwards;\n+  animation: fade-out var(--leave-ms) ease-in forwards;\n }\n \n @keyframes fade-slide-in {\n@@ -136,6 +140,15 @@\n   color: #fff;\n }\n \n+.comment.system {\n+  background: rgba(32, 38, 44, 0.88);\n+  color: #d6dde5;\n+  font-style: italic;\n+}\n+.comment.system .author { color: #9fb3c8; }\n+.comment.system .fragments { color: #d6dde5; }\n+.comment.system .sep { color: rgba(214,221,229,0.45); }\n+\n /* ---- Amount badge ---- */\n .amount {\n   display: inline-block;\n\nexec\n/bin/bash -lc \"nl -ba templates/default/app.js | sed -n '1,240p'\" in /mnt/d/workspace/fast-comment\nexec\n/bin/bash -lc 'node --check templates/default/app.js' in /mnt/d/workspace/fast-comment\nexec\n/bin/bash -lc 'nl -ba templates/default/index.html' in /mnt/d/workspace/fast-comment\nexec\n/bin/bash -lc \"nl -ba templates/default/style.css | sed -n '1,180p'\" in /mnt/d/workspace/fast-comment\n succeeded in 366ms:\n     1\t/**\n     2\t * fast-comment OBS overlay — default template\n     3\t * No dependencies. Plain ES2020 JS.\n     4\t *\n     5\t * URL params:\n     6\t *   ?template=default   (unused here, consumed by axum router)\n     7\t *   ?channel=<id>        optional filter: only show this channel\n     8\t *   ?max=8               max visible rows (default 8)\n     9\t *   ?ttl=12000           ms before a row auto-exits (default 12000)\n    10\t *   ?ws=ws://127.0.0.1:11180/ws  override WS endpoint\n    11\t */\n    12\t\n    13\t(function () {\n    14\t  'use strict';\n    15\t\n    16\t  // ---- Config from URL params ----\n    17\t  const params = new URLSearchParams(location.search);\n    18\t  const CHANNEL_FILTER = params.get('channel') || null;\n    19\t  const MAX_ROWS = positiveIntParam('max', 8);\n    20\t  const TTL_MS   = positiveIntParam('ttl', 12000);\n    21\t  const WS_URL   = buildWsUrl(params.get('ws') || 'ws://127.0.0.1:11180/ws');\n    22\t\n    23\t  const overlay = document.getElementById('overlay');\n    24\t  const LEAVE_MS = cssDurationMs('--leave-ms', 400);\n    25\t\n    26\t  // ---- Active row tracking ----\n    27\t  // Each entry: { el, timerId }\n    28\t  const rows = [];\n    29\t\n    30\t  // ---- WebSocket ----\n    31\t  let ws = null;\n    32\t  let reconnectDelay = 1000;\n    33\t  let stableTimer = null;\n    34\t\n    35\t  function connect() {\n    36\t    ws = new WebSocket(WS_URL);\n    37\t\n    38\t    ws.addEventListener('open', () => {\n    39\t      // Reset backoff only after the connection has been stable for 5 s.\n    40\t      // This prevents a fast open→close flapping loop from keeping the delay\n    41\t      // pinned at 1000 ms (the timer is cancelled in the close handler).\n    42\t      stableTimer = setTimeout(() => {\n    43\t        reconnectDelay = 1000;\n    44\t      }, 5000);\n    45\t    });\n    46\t\n    47\t    ws.addEventListener('message', (ev) => {\n    48\t      let batch;\n    49\t      try {\n    50\t        batch = JSON.parse(ev.data);\n    51\t      } catch {\n    52\t        return;\n    53\t      }\n    54\t      if (!Array.isArray(batch)) batch = [batch];\n    55\t      batch.forEach(handleMessage);\n    56\t    });\n    57\t\n    58\t    ws.addEventListener('close', () => {\n    59\t      // Cancel the stability timer so a premature close does not reset the\n    60\t      // backoff before we have had a genuinely stable connection.\n    61\t      clearTimeout(stableTimer);\n    62\t      stableTimer = null;\n    63\t      ws = null;\n    64\t      setTimeout(connect, reconnectDelay);\n    65\t      reconnectDelay = Math.min(reconnectDelay * 2, 16000);\n    66\t    });\n    67\t\n    68\t    ws.addEventListener('error', () => {\n    69\t      ws && ws.close();\n    70\t    });\n    71\t  }\n    72\t\n    73\t  // ---- Message handler ----\n    74\t  function handleMessage(msg) {\n    75\t    // Channel filter\n    76\t    if (CHANNEL_FILTER && msg.channel !== CHANNEL_FILTER) return;\n    77\t\n    78\t    const el = buildRow(msg);\n    79\t    addRow(el);\n    80\t  }\n    81\t\n    82\t  // ---- Build DOM row ----\n    83\t  function buildRow(msg) {\n    84\t    const div = document.createElement('div');\n    85\t    div.className = 'comment';\n    86\t    const kind = msg.kind || 'normal';\n    87\t    const isSystem = kind === 'system' || kind === 'System';\n    88\t\n    89\t    // Kind class for CSS highlights\n    90\t    if (kind === 'superChat')  div.classList.add('superchat');\n    91\t    if (kind === 'membership') div.classList.add('membership');\n    92\t    if (kind === 'bits')       div.classList.add('bits');\n    93\t    if (isSystem) div.classList.add('system');\n    94\t\n    95\t    // Platform dot\n    96\t    const dot = document.createElement('span');\n    97\t    dot.className = 'platform-dot ' + (msg.platform || '');\n    98\t    div.appendChild(dot);\n    99\t\n   100\t    // Badges\n   101\t    const badges = (msg.author && msg.author.badges) || [];\n   102\t    badges.forEach((badge) => {\n   103\t      if (isHttpUrl(badge.imageUrl)) {\n   104\t        const img = document.createElement('img');\n   105\t        img.className = 'badge-img';\n   106\t        img.src = badge.imageUrl;\n   107\t        img.alt = badge.label || '';\n   108\t        img.title = badge.label || '';\n   109\t        div.appendChild(img);\n   110\t      }\n   111\t    });\n   112\t\n   113\t    // Author name\n   114\t    const author = document.createElement('span');\n   115\t    author.className = 'author';\n   116\t    const color = msg.author && msg.author.displayColor;\n   117\t    if (isSafeHexColor(color) && kind === 'normal') {\n   118\t      author.style.color = color;\n   119\t    }\n   120\t    author.textContent = (msg.author && msg.author.name) || (isSystem ? 'System' : '');\n   121\t    div.appendChild(author);\n   122\t\n   123\t    // Separator\n   124\t    const sep = document.createElement('span');\n   125\t    sep.className = 'sep';\n   126\t    sep.textContent = ': ';\n   127\t    div.appendChild(sep);\n   128\t\n   129\t    // Amount (SuperChat / Bits)\n   130\t    if (msg.amount) {\n   131\t      const amt = document.createElement('span');\n   132\t      amt.className = 'amount';\n   133\t      amt.textContent = msg.amount.rawText || '';\n   134\t      div.appendChild(amt);\n   135\t    }\n   136\t\n   137\t    // Fragments\n   138\t    const fragWrap = document.createElement('span');\n   139\t    fragWrap.className = 'fragments';\n   140\t    const fragments = msg.fragments || [];\n   141\t    fragments.forEach((frag) => {\n   142\t      if (frag.type === 'text') {\n   143\t        fragWrap.appendChild(document.createTextNode(frag.text));\n   144\t      } else if (frag.type === 'emote' && isHttpUrl(frag.url)) {\n   145\t        const img = document.createElement('img');\n   146\t        img.className = 'emote';\n   147\t        img.src = frag.url;\n   148\t        img.alt = frag.name || '';\n   149\t        img.title = frag.name || '';\n   150\t        fragWrap.appendChild(img);\n   151\t      }\n   152\t    });\n   153\t    div.appendChild(fragWrap);\n   154\t\n   155\t    return div;\n   156\t  }\n   157\t\n   158\t  // ---- Row lifecycle ----\n   159\t  function addRow(el) {\n   160\t    // Evict oldest if over limit\n   161\t    while (rows.length >= MAX_ROWS) {\n   162\t      evictOldest();\n   163\t    }\n   164\t\n   165\t    overlay.appendChild(el);\n   166\t\n   167\t    const timerId = setTimeout(() => removeRow(el), TTL_MS);\n   168\t    rows.push({ el, timerId });\n   169\t  }\n   170\t\n   171\t  function evictOldest() {\n   172\t    const oldest = rows.shift();\n   173\t    if (!oldest) return;\n   174\t    clearTimeout(oldest.timerId);\n   175\t    startLeave(oldest.el);\n   176\t  }\n   177\t\n   178\t  function removeRow(el) {\n   179\t    const idx = rows.findIndex((r) => r.el === el);\n   180\t    if (idx !== -1) {\n   181\t      clearTimeout(rows[idx].timerId);\n   182\t      rows.splice(idx, 1);\n   183\t    }\n   184\t    startLeave(el);\n   185\t  }\n   186\t\n   187\t  function startLeave(el) {\n   188\t    el.classList.add('leaving');\n   189\t    // Remove from DOM after the CSS-driven exit animation.\n   190\t    setTimeout(() => {\n   191\t      if (el.parentNode) el.parentNode.removeChild(el);\n   192\t    }, LEAVE_MS);\n   193\t  }\n   194\t\n   195\t  function positiveIntParam(name, fallback) {\n   196\t    const raw = params.get(name);\n   197\t    if (raw === null) return fallback;\n   198\t\n   199\t    const value = Number(raw);\n   200\t    if (!Number.isFinite(value) || value <= 0) return fallback;\n   201\t    const intValue = Math.floor(value);\n   202\t    return intValue > 0 ? intValue : fallback;\n   203\t  }\n   204\t\n   205\t  function buildWsUrl(base) {\n   206\t    const url = new URL(base, location.href);\n   207\t    if (CHANNEL_FILTER) {\n   208\t      url.searchParams.set('channel', CHANNEL_FILTER);\n   209\t    }\n   210\t    return url.toString();\n   211\t  }\n   212\t\n   213\t  function isSafeHexColor(value) {\n   214\t    return typeof value === 'string' && /^#[0-9a-fA-F]{3,8}$/.test(value);\n   215\t  }\n   216\t\n   217\t  function isHttpUrl(value) {\n   218\t    if (typeof value !== 'string') return false;\n   219\t\n   220\t    try {\n   221\t      const url = new URL(value);\n   222\t      return url.protocol === 'http:' || url.protocol === 'https:';\n   223\t    } catch {\n   224\t      return false;\n   225\t    }\n   226\t  }\n   227\t\n   228\t  function cssDurationMs(name, fallback) {\n   229\t    const value = getComputedStyle(document.documentElement).getPropertyValue(name).trim();\n   230\t    const match = value.match(/^([0-9]*\\.?[0-9]+)(ms|s)$/);\n   231\t    if (!match) return fallback;\n   232\t\n   233\t    const duration = Number(match[1]);\n   234\t    if (!Number.isFinite(duration) || duration < 0) return fallback;\n   235\t    return match[2] === 's' ? duration * 1000 : duration;\n   236\t  }\n   237\t\n   238\t  // ---- Start ----\n   239\t  connect();\n   240\t})();\n\n succeeded in 390ms:\ndiff --git a/templates/default/app.js b/templates/default/app.js\nindex b290518af2c9183bf0de029fa14fb65be50896d5..ac1697525bcc291262f875695ca4608b39acebd7\n--- a/templates/default/app.js\n+++ b/templates/default/app.js\n@@ -16,11 +16,12 @@\n   // ---- Config from URL params ----\n   const params = new URLSearchParams(location.search);\n   const CHANNEL_FILTER = params.get('channel') || null;\n-  const MAX_ROWS = parseInt(params.get('max') || '8', 10);\n-  const TTL_MS   = parseInt(params.get('ttl')  || '12000', 10);\n-  const WS_URL   = params.get('ws') || 'ws://127.0.0.1:11180/ws' + location.search;\n+  const MAX_ROWS = positiveIntParam('max', 8);\n+  const TTL_MS   = positiveIntParam('ttl', 12000);\n+  const WS_URL   = buildWsUrl(params.get('ws') || 'ws://127.0.0.1:11180/ws');\n \n   const overlay = document.getElementById('overlay');\n+  const LEAVE_MS = cssDurationMs('--leave-ms', 400);\n \n   // ---- Active row tracking ----\n   // Each entry: { el, timerId }\n@@ -82,11 +83,14 @@\n   function buildRow(msg) {\n     const div = document.createElement('div');\n     div.className = 'comment';\n+    const kind = msg.kind || 'normal';\n+    const isSystem = kind === 'system' || kind === 'System';\n \n     // Kind class for CSS highlights\n-    if (msg.kind === 'superChat')  div.classList.add('superchat');\n-    if (msg.kind === 'membership') div.classList.add('membership');\n-    if (msg.kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'superChat')  div.classList.add('superchat');\n+    if (kind === 'membership') div.classList.add('membership');\n+    if (kind === 'bits')       div.classList.add('bits');\n+    if (isSystem) div.classList.add('system');\n \n     // Platform dot\n     const dot = document.createElement('span');\n@@ -96,7 +100,7 @@\n     // Badges\n     const badges = (msg.author && msg.author.badges) || [];\n     badges.forEach((badge) => {\n-      if (badge.imageUrl) {\n+      if (isHttpUrl(badge.imageUrl)) {\n         const img = document.createElement('img');\n         img.className = 'badge-img';\n         img.src = badge.imageUrl;\n@@ -110,10 +114,10 @@\n     const author = document.createElement('span');\n     author.className = 'author';\n     const color = msg.author && msg.author.displayColor;\n-    if (color && msg.kind === 'normal') {\n+    if (isSafeHexColor(color) && kind === 'normal') {\n       author.style.color = color;\n     }\n-    author.textContent = (msg.author && msg.author.name) || '';\n+    author.textContent = (msg.author && msg.author.name) || (isSystem ? 'System' : '');\n     div.appendChild(author);\n \n     // Separator\n@@ -137,7 +141,7 @@\n     fragments.forEach((frag) => {\n       if (frag.type === 'text') {\n         fragWrap.appendChild(document.createTextNode(frag.text));\n-      } else if (frag.type === 'emote') {\n+      } else if (frag.type === 'emote' && isHttpUrl(frag.url)) {\n         const img = document.createElement('img');\n         img.className = 'emote';\n         img.src = frag.url;\n@@ -182,10 +186,53 @@\n \n   function startLeave(el) {\n     el.classList.add('leaving');\n-    // Remove from DOM after animation (matches CSS 0.4s)\n+    // Remove from DOM after the CSS-driven exit animation.\n     setTimeout(() => {\n       if (el.parentNode) el.parentNode.removeChild(el);\n-    }, 420);\n+    }, LEAVE_MS);\n+  }\n+\n+  function positiveIntParam(name, fallback) {\n+    const raw = params.get(name);\n+    if (raw === null) return fallback;\n+\n+    const value = Number(raw);\n+    if (!Number.isFinite(value) || value <= 0) return fallback;\n+    const intValue = Math.floor(value);\n+    return intValue > 0 ? intValue : fallback;\n+  }\n+\n+  function buildWsUrl(base) {\n+    const url = new URL(base, location.href);\n+    if (CHANNEL_FILTER) {\n+      url.searchParams.set('channel', CHANNEL_FILTER);\n+    }\n+    return url.toString();\n+  }\n+\n+  function isSafeHexColor(value) {\n+    return typeof value === 'string' && /^#[0-9a-fA-F]{3,8}$/.test(value);\n+  }\n+\n+  function isHttpUrl(value) {\n+    if (typeof value !== 'string') return false;\n+\n+    try {\n+      const url = new URL(value);\n+      return url.protocol === 'http:' || url.protocol === 'https:';\n+    } catch {\n+      return false;\n+    }\n+  }\n+\n+  function cssDurationMs(name, fallback) {\n+    const value = getComputedStyle(document.documentElement).getPropertyValue(name).trim();\n+    const match = value.match(/^([0-9]*\\.?[0-9]+)(ms|s)$/);\n+    if (!match) return fallback;\n+\n+    const duration = Number(match[1]);\n+    if (!Number.isFinite(duration) || duration < 0) return fallback;\n+    return match[2] === 's' ? duration * 1000 : duration;\n   }\n \n   // ---- Start ----\ndiff --git a/templates/default/index.html b/templates/default/index.html\nindex b8f1d118231da15f84dc71fe39a4d288f0689898..75d92c72aa656a00b261450b07a87fdef4d9db66\n--- a/templates/default/index.html\n+++ b/templates/default/index.html\n@@ -2,7 +2,6 @@\n <html lang=\"ja\">\n   <head>\n     <meta charset=\"UTF-8\" />\n-    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\" />\n     <title>fast-comment overlay</title>\n     <link rel=\"stylesheet\" href=\"style.css\" />\n   </head>\ndiff --git a/templates/default/style.css b/templates/default/style.css\nindex 05692edd4b93bf60a536289f534944b8655f60f8..4c36f4d0a72bd4762c1aca8a2b3b86291f438a0d\n--- a/templates/default/style.css\n+++ b/templates/default/style.css\n@@ -1,5 +1,9 @@\n /* OBS Browser Source overlay — transparent background */\n \n+:root {\n+  --leave-ms: 400ms;\n+}\n+\n *, *::before, *::after {\n   box-sizing: border-box;\n   margin: 0;\n@@ -49,7 +53,7 @@\n \n /* Exit animation applied via JS */\n .comment.leaving {\n-  animation: fade-out 0.4s ease-in forwards;\n+  animation: fade-out var(--leave-ms) ease-in forwards;\n }\n \n @keyframes fade-slide-in {\n@@ -136,6 +140,15 @@\n   color: #fff;\n }\n \n+.comment.system {\n+  background: rgba(32, 38, 44, 0.88);\n+  color: #d6dde5;\n+  font-style: italic;\n+}\n+.comment.system .author { color: #9fb3c8; }\n+.comment.system .fragments { color: #d6dde5; }\n+.comment.system .sep { color: rgba(214,221,229,0.45); }\n+\n /* ---- Amount badge ---- */\n .amount {\n   display: inline-block;\n\n succeeded in 436ms:\n     1\t/* OBS Browser Source overlay — transparent background */\n     2\t\n     3\t:root {\n     4\t  --leave-ms: 400ms;\n     5\t}\n     6\t\n     7\t*, *::before, *::after {\n     8\t  box-sizing: border-box;\n     9\t  margin: 0;\n    10\t  padding: 0;\n    11\t}\n    12\t\n    13\thtml, body {\n    14\t  width: 100%;\n    15\t  height: 100%;\n    16\t  background: transparent;\n    17\t  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Hiragino Sans', 'Yu Gothic UI', sans-serif;\n    18\t  font-size: 14px;\n    19\t  overflow: hidden;\n    20\t}\n    21\t\n    22\t#overlay {\n    23\t  position: fixed;\n    24\t  bottom: 0;\n    25\t  left: 0;\n    26\t  right: 0;\n    27\t  /* max visible rows — adjust via URL param max (default 8) */\n    28\t  max-height: 100vh;\n    29\t  display: flex;\n    30\t  flex-direction: column;\n    31\t  justify-content: flex-end;\n    32\t  gap: 4px;\n    33\t  padding: 8px;\n    34\t  pointer-events: none;\n    35\t}\n    36\t\n    37\t/* ---- Comment row ---- */\n    38\t.comment {\n    39\t  display: flex;\n    40\t  align-items: flex-start;\n    41\t  gap: 6px;\n    42\t  padding: 5px 10px;\n    43\t  border-radius: 6px;\n    44\t  background: rgba(14, 14, 14, 0.82);\n    45\t  backdrop-filter: blur(4px);\n    46\t  max-width: 520px;\n    47\t  word-break: break-word;\n    48\t  line-height: 1.45;\n    49\t\n    50\t  /* Entrance animation */\n    51\t  animation: fade-slide-in 0.25s ease-out both;\n    52\t}\n    53\t\n    54\t/* Exit animation applied via JS */\n    55\t.comment.leaving {\n    56\t  animation: fade-out var(--leave-ms) ease-in forwards;\n    57\t}\n    58\t\n    59\t@keyframes fade-slide-in {\n    60\t  from {\n    61\t    opacity: 0;\n    62\t    transform: translateY(8px);\n    63\t  }\n    64\t  to {\n    65\t    opacity: 1;\n    66\t    transform: translateY(0);\n    67\t  }\n    68\t}\n    69\t\n    70\t@keyframes fade-out {\n    71\t  from { opacity: 1; transform: scale(1); }\n    72\t  to   { opacity: 0; transform: scale(0.96); }\n    73\t}\n    74\t\n    75\t/* ---- Platform dot ---- */\n    76\t.platform-dot {\n    77\t  display: inline-block;\n    78\t  width: 8px;\n    79\t  height: 8px;\n    80\t  border-radius: 50%;\n    81\t  flex-shrink: 0;\n    82\t  margin-top: 5px;\n    83\t}\n    84\t\n    85\t.platform-dot.twitch  { background: #9146ff; }\n    86\t.platform-dot.youtube { background: #ff0000; }\n    87\t\n    88\t/* ---- Badge images ---- */\n    89\t.badge-img {\n    90\t  width: 18px;\n    91\t  height: 18px;\n    92\t  vertical-align: middle;\n    93\t  flex-shrink: 0;\n    94\t}\n    95\t\n    96\t/* ---- Author ---- */\n    97\t.author {\n    98\t  font-weight: 700;\n    99\t  font-size: 13px;\n   100\t  color: #e0e0e0;\n   101\t  flex-shrink: 0;\n   102\t  white-space: nowrap;\n   103\t}\n   104\t\n   105\t.sep {\n   106\t  color: rgba(255,255,255,0.35);\n   107\t  flex-shrink: 0;\n   108\t}\n   109\t\n   110\t/* ---- Text fragments ---- */\n   111\t.fragments {\n   112\t  color: #f5f5f5;\n   113\t  font-size: 14px;\n   114\t}\n   115\t\n   116\t/* ---- Emote images ---- */\n   117\t.emote {\n   118\t  height: 22px;\n   119\t  width: auto;\n   120\t  vertical-align: middle;\n   121\t}\n   122\t\n   123\t/* ---- SuperChat / Bits highlight ---- */\n   124\t.comment.superchat {\n   125\t  background: rgba(255, 214, 0, 0.92);\n   126\t  color: #000;\n   127\t}\n   128\t.comment.superchat .author { color: #000; }\n   129\t.comment.superchat .fragments { color: #000; }\n   130\t.comment.superchat .sep { color: rgba(0,0,0,0.4); }\n   131\t\n   132\t.comment.membership {\n   133\t  background: rgba(0, 200, 83, 0.88);\n   134\t  color: #fff;\n   135\t}\n   136\t.comment.membership .author { color: #fff; }\n   137\t\n   138\t.comment.bits {\n   139\t  background: rgba(145, 70, 255, 0.85);\n   140\t  color: #fff;\n   141\t}\n   142\t\n   143\t.comment.system {\n   144\t  background: rgba(32, 38, 44, 0.88);\n   145\t  color: #d6dde5;\n   146\t  font-style: italic;\n   147\t}\n   148\t.comment.system .author { color: #9fb3c8; }\n   149\t.comment.system .fragments { color: #d6dde5; }\n   150\t.comment.system .sep { color: rgba(214,221,229,0.45); }\n   151\t\n   152\t/* ---- Amount badge ---- */\n   153\t.amount {\n   154\t  display: inline-block;\n   155\t  background: rgba(0,0,0,0.25);\n   156\t  border-radius: 10px;\n   157\t  padding: 1px 7px;\n   158\t  font-size: 12px;\n   159\t  font-weight: 700;\n   160\t  flex-shrink: 0;\n   161\t}\n\n succeeded in 459ms:\n     1\t<!doctype html>\n     2\t<html lang=\"ja\">\n     3\t  <head>\n     4\t    <meta charset=\"UTF-8\" />\n     5\t    <title>fast-comment overlay</title>\n     6\t    <link rel=\"stylesheet\" href=\"style.css\" />\n     7\t  </head>\n     8\t  <body>\n     9\t    <div id=\"overlay\"></div>\n    10\t    <script src=\"app.js\"></script>\n    11\t  </body>\n    12\t</html>\n\ncodex\n構文チェックは通っています。加えて、実ファイルを VM 上の軽い DOM/WebSocket スタブで読み込み、URL 付与、数値フォールバック、System/サニタイズの主要経路を実行確認します。\nexec\n/bin/bash -lc \"node <<'NODE'\nconst assert = require('assert');\nconst fs = require('fs');\nconst vm = require('vm');\n\nconst source = fs.readFileSync('templates/default/app.js', 'utf8');\n\nclass Element {\n  constructor(tagName) {\n    this.tagName = tagName;\n    this.children = [];\n    this.parentNode = null;\n    this.style = {};\n    this.className = '';\n    this.textContent = '';\n    this.classList = {\n      add: (...tokens) => {\n        const classes = new Set(this.className.split(/\\\\s+/).filter(Boolean));\n        tokens.forEach((token) => classes.add(token));\n        this.className = Array.from(classes).join(' ');\n      },\n    };\n  }\n\n  appendChild(child) {\n    child.parentNode = this;\n    this.children.push(child);\n    return child;\n  }\n\n  removeChild(child) {\n    this.children = this.children.filter((item) => item \"'!== child);\n    child.parentNode = null;\n  }\n}\n\nfunction run(search) {\n  const overlay = new Element('\"'div');\n  const timers = [];\n  let socket = null;\n\n  class FakeWebSocket {\n    constructor(url) {\n      this.url = url;\n      this.handlers = {};\n      socket = this;\n    }\n\n    addEventListener(name, handler) {\n      this.handlers[name] = handler;\n    }\n\n    close() {\n      if (this.handlers.close) this.handlers.close();\n    }\n  }\n\n  const context = {\n    location: new URL(\"'`http://localhost/overlay${search}`),\n    URL,\n    URLSearchParams,\n    WebSocket: FakeWebSocket,\n    document: {\n      documentElement: new Element('\"'html'),\n      getElementById: () => overlay,\n      createElement: (tagName) => new Element(tagName),\n      createTextNode: (text) => ({ nodeType: 3, textContent: String(text), parentNode: null }),\n    },\n    getComputedStyle: () => ({ getPropertyValue: (name) => (name === '--leave-ms' ? '400ms' : '') }),\n    setTimeout: (handler, ms) => {\n      timers.push(ms);\n      return timers.length;\n    },\n    clearTimeout: () => {},\n  };\n\n  vm.runInNewContext(source, context);\n  return { overlay, socket, timers };\n}\n\nlet env = run('?channel=abc');\nassert.strictEqual(env.socket.url, 'ws://127.0.0.1:11180/ws?channel=abc');\n\nenv = run('?ws=ws%3A%2F%2Fexample.test%2Fcustom%3Fx%3D1&channel=abc');\nassert.strictEqual(env.socket.url, 'ws://example.test/custom?x=1&channel=abc');\n\nenv = run('?max=abc&ttl=bad');\nenv.socket.handlers.message({\n  data: JSON.stringify({\n    kind: 'normal',\n    platform: 'twitch',\n    author: {\n      name: 'Alice',\n      displayColor: 'red',\n      badges: [{ imageUrl: 'javascript:alert(1)', label: 'bad' }, { imageUrl: 'https://cdn.test/badge.png', label: 'ok' }],\n    },\n    fragments: [{ type: 'emote', url: 'ftp://cdn.test/emote.png' }, { type: 'emote', url: 'https://cdn.test/emote.png' }],\n  }),\n});\nassert.strictEqual(env.timers.at(-1), 12000);\nassert.strictEqual(env.overlay.children.length, 1);\nconst row = env.overlay.children[0];\nconst author = row.children.find((child) => child.className === 'author');\nassert.strictEqual(author.style.color, undefined);\nassert.strictEqual(row.children.filter((child) => child.className === 'badge-img').length, 1);\nconst fragWrap = row.children.find((child) => child.className === 'fragments');\nassert.strictEqual(fragWrap.children.filter((child) => child.className === 'emote').length, 1);\n\nenv = run('');\nenv.socket.handlers.message({ data: JSON.stringify({ kind: 'System', fragments: [{ type: 'text', text: 'connected' }] }) });\nassert.match(env.overlay.children[0].className, /\\\\bsystem\\\\b/);\nassert.strictEqual(env.overlay.children[0].children.find((child) => child.className === 'author').textContent, 'System');\n\nconsole.log('targeted overlay checks passed');\nNODE\" in /mnt/d/workspace/fast-comment\ndiff --git a/templates/default/app.js b/templates/default/app.js\nindex b290518af2c9183bf0de029fa14fb65be50896d5..ac1697525bcc291262f875695ca4608b39acebd7\n--- a/templates/default/app.js\n+++ b/templates/default/app.js\n@@ -16,11 +16,12 @@\n   // ---- Config from URL params ----\n   const params = new URLSearchParams(location.search);\n   const CHANNEL_FILTER = params.get('channel') || null;\n-  const MAX_ROWS = parseInt(params.get('max') || '8', 10);\n-  const TTL_MS   = parseInt(params.get('ttl')  || '12000', 10);\n-  const WS_URL   = params.get('ws') || 'ws://127.0.0.1:11180/ws' + location.search;\n+  const MAX_ROWS = positiveIntParam('max', 8);\n+  const TTL_MS   = positiveIntParam('ttl', 12000);\n+  const WS_URL   = buildWsUrl(params.get('ws') || 'ws://127.0.0.1:11180/ws');\n \n   const overlay = document.getElementById('overlay');\n+  const LEAVE_MS = cssDurationMs('--leave-ms', 400);\n \n   // ---- Active row tracking ----\n   // Each entry: { el, timerId }\n@@ -82,11 +83,14 @@\n   function buildRow(msg) {\n     const div = document.createElement('div');\n     div.className = 'comment';\n+    const kind = msg.kind || 'normal';\n+    const isSystem = kind === 'system' || kind === 'System';\n \n     // Kind class for CSS highlights\n-    if (msg.kind === 'superChat')  div.classList.add('superchat');\n-    if (msg.kind === 'membership') div.classList.add('membership');\n-    if (msg.kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'superChat')  div.classList.add('superchat');\n+    if (kind === 'membership') div.classList.add('membership');\n+    if (kind === 'bits')       div.classList.add('bits');\n+    if (isSystem) div.classList.add('system');\n \n     // Platform dot\n     const dot = document.createElement('span');\n@@ -96,7 +100,7 @@\n     // Badges\n     const badges = (msg.author && msg.author.badges) || [];\n     badges.forEach((badge) => {\n-      if (badge.imageUrl) {\n+      if (isHttpUrl(badge.imageUrl)) {\n         const img = document.createElement('img');\n         img.className = 'badge-img';\n         img.src = badge.imageUrl;\n@@ -110,10 +114,10 @@\n     const author = document.createElement('span');\n     author.className = 'author';\n     const color = msg.author && msg.author.displayColor;\n-    if (color && msg.kind === 'normal') {\n+    if (isSafeHexColor(color) && kind === 'normal') {\n       author.style.color = color;\n     }\n-    author.textContent = (msg.author && msg.author.name) || '';\n+    author.textContent = (msg.author && msg.author.name) || (isSystem ? 'System' : '');\n     div.appendChild(author);\n \n     // Separator\n@@ -137,7 +141,7 @@\n     fragments.forEach((frag) => {\n       if (frag.type === 'text') {\n         fragWrap.appendChild(document.createTextNode(frag.text));\n-      } else if (frag.type === 'emote') {\n+      } else if (frag.type === 'emote' && isHttpUrl(frag.url)) {\n         const img = document.createElement('img');\n         img.className = 'emote';\n         img.src = frag.url;\n@@ -182,10 +186,53 @@\n \n   function startLeave(el) {\n     el.classList.add('leaving');\n-    // Remove from DOM after animation (matches CSS 0.4s)\n+    // Remove from DOM after the CSS-driven exit animation.\n     setTimeout(() => {\n       if (el.parentNode) el.parentNode.removeChild(el);\n-    }, 420);\n+    }, LEAVE_MS);\n+  }\n+\n+  function positiveIntParam(name, fallback) {\n+    const raw = params.get(name);\n+    if (raw === null) return fallback;\n+\n+    const value = Number(raw);\n+    if (!Number.isFinite(value) || value <= 0) return fallback;\n+    const intValue = Math.floor(value);\n+    return intValue > 0 ? intValue : fallback;\n+  }\n+\n+  function buildWsUrl(base) {\n+    const url = new URL(base, location.href);\n+    if (CHANNEL_FILTER) {\n+      url.searchParams.set('channel', CHANNEL_FILTER);\n+    }\n+    return url.toString();\n+  }\n+\n+  function isSafeHexColor(value) {\n+    return typeof value === 'string' && /^#[0-9a-fA-F]{3,8}$/.test(value);\n+  }\n+\n+  function isHttpUrl(value) {\n+    if (typeof value !== 'string') return false;\n+\n+    try {\n+      const url = new URL(value);\n+      return url.protocol === 'http:' || url.protocol === 'https:';\n+    } catch {\n+      return false;\n+    }\n+  }\n+\n+  function cssDurationMs(name, fallback) {\n+    const value = getComputedStyle(document.documentElement).getPropertyValue(name).trim();\n+    const match = value.match(/^([0-9]*\\.?[0-9]+)(ms|s)$/);\n+    if (!match) return fallback;\n+\n+    const duration = Number(match[1]);\n+    if (!Number.isFinite(duration) || duration < 0) return fallback;\n+    return match[2] === 's' ? duration * 1000 : duration;\n   }\n \n   // ---- Start ----\ndiff --git a/templates/default/index.html b/templates/default/index.html\nindex b8f1d118231da15f84dc71fe39a4d288f0689898..75d92c72aa656a00b261450b07a87fdef4d9db66\n--- a/templates/default/index.html\n+++ b/templates/default/index.html\n@@ -2,7 +2,6 @@\n <html lang=\"ja\">\n   <head>\n     <meta charset=\"UTF-8\" />\n-    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\" />\n     <title>fast-comment overlay</title>\n     <link rel=\"stylesheet\" href=\"style.css\" />\n   </head>\ndiff --git a/templates/default/style.css b/templates/default/style.css\nindex 05692edd4b93bf60a536289f534944b8655f60f8..4c36f4d0a72bd4762c1aca8a2b3b86291f438a0d\n--- a/templates/default/style.css\n+++ b/templates/default/style.css\n@@ -1,5 +1,9 @@\n /* OBS Browser Source overlay — transparent background */\n \n+:root {\n+  --leave-ms: 400ms;\n+}\n+\n *, *::before, *::after {\n   box-sizing: border-box;\n   margin: 0;\n@@ -49,7 +53,7 @@\n \n /* Exit animation applied via JS */\n .comment.leaving {\n-  animation: fade-out 0.4s ease-in forwards;\n+  animation: fade-out var(--leave-ms) ease-in forwards;\n }\n \n @keyframes fade-slide-in {\n@@ -136,6 +140,15 @@\n   color: #fff;\n }\n \n+.comment.system {\n+  background: rgba(32, 38, 44, 0.88);\n+  color: #d6dde5;\n+  font-style: italic;\n+}\n+.comment.system .author { color: #9fb3c8; }\n+.comment.system .fragments { color: #d6dde5; }\n+.comment.system .sep { color: rgba(214,221,229,0.45); }\n+\n /* ---- Amount badge ---- */\n .amount {\n   display: inline-block;\n\n succeeded in 477ms:\ntargeted overlay checks passed\n\nexec\n/bin/bash -lc 'git diff --name-only -- templates/default/app.js templates/default/style.css templates/default/index.html' in /mnt/d/workspace/fast-comment\nexec\n/bin/bash -lc 'git diff -- templates/default/app.js templates/default/style.css templates/default/index.html' in /mnt/d/workspace/fast-comment\nexec\n/bin/bash -lc 'git diff --check -- templates/default/app.js templates/default/style.css templates/default/index.html' in /mnt/d/workspace/fast-comment\n succeeded in 330ms:\ntemplates/default/app.js\ntemplates/default/index.html\ntemplates/default/style.css\n\n succeeded in 349ms:\ndiff --git a/templates/default/app.js b/templates/default/app.js\nindex b290518af2c9183bf0de029fa14fb65be50896d5..ac1697525bcc291262f875695ca4608b39acebd7\n--- a/templates/default/app.js\n+++ b/templates/default/app.js\n@@ -16,11 +16,12 @@\n   // ---- Config from URL params ----\n   const params = new URLSearchParams(location.search);\n   const CHANNEL_FILTER = params.get('channel') || null;\n-  const MAX_ROWS = parseInt(params.get('max') || '8', 10);\n-  const TTL_MS   = parseInt(params.get('ttl')  || '12000', 10);\n-  const WS_URL   = params.get('ws') || 'ws://127.0.0.1:11180/ws' + location.search;\n+  const MAX_ROWS = positiveIntParam('max', 8);\n+  const TTL_MS   = positiveIntParam('ttl', 12000);\n+  const WS_URL   = buildWsUrl(params.get('ws') || 'ws://127.0.0.1:11180/ws');\n \n   const overlay = document.getElementById('overlay');\n+  const LEAVE_MS = cssDurationMs('--leave-ms', 400);\n \n   // ---- Active row tracking ----\n   // Each entry: { el, timerId }\n@@ -82,11 +83,14 @@\n   function buildRow(msg) {\n     const div = document.createElement('div');\n     div.className = 'comment';\n+    const kind = msg.kind || 'normal';\n+    const isSystem = kind === 'system' || kind === 'System';\n \n     // Kind class for CSS highlights\n-    if (msg.kind === 'superChat')  div.classList.add('superchat');\n-    if (msg.kind === 'membership') div.classList.add('membership');\n-    if (msg.kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'superChat')  div.classList.add('superchat');\n+    if (kind === 'membership') div.classList.add('membership');\n+    if (kind === 'bits')       div.classList.add('bits');\n+    if (isSystem) div.classList.add('system');\n \n     // Platform dot\n     const dot = document.createElement('span');\n@@ -96,7 +100,7 @@\n     // Badges\n     const badges = (msg.author && msg.author.badges) || [];\n     badges.forEach((badge) => {\n-      if (badge.imageUrl) {\n+      if (isHttpUrl(badge.imageUrl)) {\n         const img = document.createElement('img');\n         img.className = 'badge-img';\n         img.src = badge.imageUrl;\n@@ -110,10 +114,10 @@\n     const author = document.createElement('span');\n     author.className = 'author';\n     const color = msg.author && msg.author.displayColor;\n-    if (color && msg.kind === 'normal') {\n+    if (isSafeHexColor(color) && kind === 'normal') {\n       author.style.color = color;\n     }\n-    author.textContent = (msg.author && msg.author.name) || '';\n+    author.textContent = (msg.author && msg.author.name) || (isSystem ? 'System' : '');\n     div.appendChild(author);\n \n     // Separator\n@@ -137,7 +141,7 @@\n     fragments.forEach((frag) => {\n       if (frag.type === 'text') {\n         fragWrap.appendChild(document.createTextNode(frag.text));\n-      } else if (frag.type === 'emote') {\n+      } else if (frag.type === 'emote' && isHttpUrl(frag.url)) {\n         const img = document.createElement('img');\n         img.className = 'emote';\n         img.src = frag.url;\n@@ -182,10 +186,53 @@\n \n   function startLeave(el) {\n     el.classList.add('leaving');\n-    // Remove from DOM after animation (matches CSS 0.4s)\n+    // Remove from DOM after the CSS-driven exit animation.\n     setTimeout(() => {\n       if (el.parentNode) el.parentNode.removeChild(el);\n-    }, 420);\n+    }, LEAVE_MS);\n+  }\n+\n+  function positiveIntParam(name, fallback) {\n+    const raw = params.get(name);\n+    if (raw === null) return fallback;\n+\n+    const value = Number(raw);\n+    if (!Number.isFinite(value) || value <= 0) return fallback;\n+    const intValue = Math.floor(value);\n+    return intValue > 0 ? intValue : fallback;\n+  }\n+\n+  function buildWsUrl(base) {\n+    const url = new URL(base, location.href);\n+    if (CHANNEL_FILTER) {\n+      url.searchParams.set('channel', CHANNEL_FILTER);\n+    }\n+    return url.toString();\n+  }\n+\n+  function isSafeHexColor(value) {\n+    return typeof value === 'string' && /^#[0-9a-fA-F]{3,8}$/.test(value);\n+  }\n+\n+  function isHttpUrl(value) {\n+    if (typeof value !== 'string') return false;\n+\n+    try {\n+      const url = new URL(value);\n+      return url.protocol === 'http:' || url.protocol === 'https:';\n+    } catch {\n+      return false;\n+    }\n+  }\n+\n+  function cssDurationMs(name, fallback) {\n+    const value = getComputedStyle(document.documentElement).getPropertyValue(name).trim();\n+    const match = value.match(/^([0-9]*\\.?[0-9]+)(ms|s)$/);\n+    if (!match) return fallback;\n+\n+    const duration = Number(match[1]);\n+    if (!Number.isFinite(duration) || duration < 0) return fallback;\n+    return match[2] === 's' ? duration * 1000 : duration;\n   }\n \n   // ---- Start ----\ndiff --git a/templates/default/index.html b/templates/default/index.html\nindex b8f1d118231da15f84dc71fe39a4d288f0689898..75d92c72aa656a00b261450b07a87fdef4d9db66\n--- a/templates/default/index.html\n+++ b/templates/default/index.html\n@@ -2,7 +2,6 @@\n <html lang=\"ja\">\n   <head>\n     <meta charset=\"UTF-8\" />\n-    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\" />\n     <title>fast-comment overlay</title>\n     <link rel=\"stylesheet\" href=\"style.css\" />\n   </head>\ndiff --git a/templates/default/style.css b/templates/default/style.css\nindex 05692edd4b93bf60a536289f534944b8655f60f8..4c36f4d0a72bd4762c1aca8a2b3b86291f438a0d\n--- a/templates/default/style.css\n+++ b/templates/default/style.css\n@@ -1,5 +1,9 @@\n /* OBS Browser Source overlay — transparent background */\n \n+:root {\n+  --leave-ms: 400ms;\n+}\n+\n *, *::before, *::after {\n   box-sizing: border-box;\n   margin: 0;\n@@ -49,7 +53,7 @@\n \n /* Exit animation applied via JS */\n .comment.leaving {\n-  animation: fade-out 0.4s ease-in forwards;\n+  animation: fade-out var(--leave-ms) ease-in forwards;\n }\n \n @keyframes fade-slide-in {\n@@ -136,6 +140,15 @@\n   color: #fff;\n }\n \n+.comment.system {\n+  background: rgba(32, 38, 44, 0.88);\n+  color: #d6dde5;\n+  font-style: italic;\n+}\n+.comment.system .author { color: #9fb3c8; }\n+.comment.system .fragments { color: #d6dde5; }\n+.comment.system .sep { color: rgba(214,221,229,0.45); }\n+\n /* ---- Amount badge ---- */\n .amount {\n   display: inline-block;\n\n succeeded in 390ms:\ndiff --git a/templates/default/app.js b/templates/default/app.js\nindex b290518..ac16975 100644\n--- a/templates/default/app.js\n+++ b/templates/default/app.js\n@@ -16,11 +16,12 @@\n   // ---- Config from URL params ----\n   const params = new URLSearchParams(location.search);\n   const CHANNEL_FILTER = params.get('channel') || null;\n-  const MAX_ROWS = parseInt(params.get('max') || '8', 10);\n-  const TTL_MS   = parseInt(params.get('ttl')  || '12000', 10);\n-  const WS_URL   = params.get('ws') || 'ws://127.0.0.1:11180/ws' + location.search;\n+  const MAX_ROWS = positiveIntParam('max', 8);\n+  const TTL_MS   = positiveIntParam('ttl', 12000);\n+  const WS_URL   = buildWsUrl(params.get('ws') || 'ws://127.0.0.1:11180/ws');\n \n   const overlay = document.getElementById('overlay');\n+  const LEAVE_MS = cssDurationMs('--leave-ms', 400);\n \n   // ---- Active row tracking ----\n   // Each entry: { el, timerId }\n@@ -82,11 +83,14 @@\n   function buildRow(msg) {\n     const div = document.createElement('div');\n     div.className = 'comment';\n+    const kind = msg.kind || 'normal';\n+    const isSystem = kind === 'system' || kind === 'System';\n \n     // Kind class for CSS highlights\n-    if (msg.kind === 'superChat')  div.classList.add('superchat');\n-    if (msg.kind === 'membership') div.classList.add('membership');\n-    if (msg.kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'superChat')  div.classList.add('superchat');\n+    if (kind === 'membership') div.classList.add('membership');\n+    if (kind === 'bits')       div.classList.add('bits');\n+    if (isSystem) div.classList.add('system');\n \n     // Platform dot\n     const dot = document.createElement('span');\n@@ -96,7 +100,7 @@\n     // Badges\n     const badges = (msg.author && msg.author.badges) || [];\n     badges.forEach((badge) => {\n-      if (badge.imageUrl) {\n+      if (isHttpUrl(badge.imageUrl)) {\n         const img = document.createElement('img');\n         img.className = 'badge-img';\n         img.src = badge.imageUrl;\n@@ -110,10 +114,10 @@\n     const author = document.createElement('span');\n     author.className = 'author';\n     const color = msg.author && msg.author.displayColor;\n-    if (color && msg.kind === 'normal') {\n+    if (isSafeHexColor(color) && kind === 'normal') {\n       author.style.color = color;\n     }\n-    author.textContent = (msg.author && msg.author.name) || '';\n+    author.textContent = (msg.author && msg.author.name) || (isSystem ? 'System' : '');\n     div.appendChild(author);\n \n     // Separator\n@@ -137,7 +141,7 @@\n     fragments.forEach((frag) => {\n       if (frag.type === 'text') {\n         fragWrap.appendChild(document.createTextNode(frag.text));\n-      } else if (frag.type === 'emote') {\n+      } else if (frag.type === 'emote' && isHttpUrl(frag.url)) {\n         const img = document.createElement('img');\n         img.className = 'emote';\n         img.src = frag.url;\n@@ -182,10 +186,53 @@\n \n   function startLeave(el) {\n     el.classList.add('leaving');\n-    // Remove from DOM after animation (matches CSS 0.4s)\n+    // Remove from DOM after the CSS-driven exit animation.\n     setTimeout(() => {\n       if (el.parentNode) el.parentNode.removeChild(el);\n-    }, 420);\n+    }, LEAVE_MS);\n+  }\n+\n+  function positiveIntParam(name, fallback) {\n+    const raw = params.get(name);\n+    if (raw === null) return fallback;\n+\n+    const value = Number(raw);\n+    if (!Number.isFinite(value) || value <= 0) return fallback;\n+    const intValue = Math.floor(value);\n+    return intValue > 0 ? intValue : fallback;\n+  }\n+\n+  function buildWsUrl(base) {\n+    const url = new URL(base, location.href);\n+    if (CHANNEL_FILTER) {\n+      url.searchParams.set('channel', CHANNEL_FILTER);\n+    }\n+    return url.toString();\n+  }\n+\n+  function isSafeHexColor(value) {\n+    return typeof value === 'string' && /^#[0-9a-fA-F]{3,8}$/.test(value);\n+  }\n+\n+  function isHttpUrl(value) {\n+    if (typeof value !== 'string') return false;\n+\n+    try {\n+      const url = new URL(value);\n+      return url.protocol === 'http:' || url.protocol === 'https:';\n+    } catch {\n+      return false;\n+    }\n+  }\n+\n+  function cssDurationMs(name, fallback) {\n+    const value = getComputedStyle(document.documentElement).getPropertyValue(name).trim();\n+    const match = value.match(/^([0-9]*\\.?[0-9]+)(ms|s)$/);\n+    if (!match) return fallback;\n+\n+    const duration = Number(match[1]);\n+    if (!Number.isFinite(duration) || duration < 0) return fallback;\n+    return match[2] === 's' ? duration * 1000 : duration;\n   }\n \n   // ---- Start ----\ndiff --git a/templates/default/index.html b/templates/default/index.html\nindex b8f1d11..75d92c7 100644\n--- a/templates/default/index.html\n+++ b/templates/default/index.html\n@@ -2,7 +2,6 @@\n <html lang=\"ja\">\n   <head>\n     <meta charset=\"UTF-8\" />\n-    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\" />\n     <title>fast-comment overlay</title>\n     <link rel=\"stylesheet\" href=\"style.css\" />\n   </head>\ndiff --git a/templates/default/style.css b/templates/default/style.css\nindex 05692ed..4c36f4d 100644\n--- a/templates/default/style.css\n+++ b/templates/default/style.css\n@@ -1,5 +1,9 @@\n /* OBS Browser Source overlay — transparent background */\n \n+:root {\n+  --leave-ms: 400ms;\n+}\n+\n *, *::before, *::after {\n   box-sizing: border-box;\n   margin: 0;\n@@ -49,7 +53,7 @@ html, body {\n \n /* Exit animation applied via JS */\n .comment.leaving {\n-  animation: fade-out 0.4s ease-in forwards;\n+  animation: fade-out var(--leave-ms) ease-in forwards;\n }\n \n @keyframes fade-slide-in {\n@@ -136,6 +140,15 @@ html, body {\n   color: #fff;\n }\n \n+.comment.system {\n+  background: rgba(32, 38, 44, 0.88);\n+  color: #d6dde5;\n+  font-style: italic;\n+}\n+.comment.system .author { color: #9fb3c8; }\n+.comment.system .fragments { color: #d6dde5; }\n+.comment.system .sep { color: rgba(214,221,229,0.45); }\n+\n /* ---- Amount badge ---- */\n .amount {\n   display: inline-block;\n\ncodex\n実装完了しました。変更は指定範囲の `templates/default/app.js`, `templates/default/style.css`, `templates/default/index.html` のみです。\n\n**変更内容**\n- `?ws=` 指定時/未指定時どちらも `new URL(...).searchParams.set('channel', ...)` で `channel` を付与。\n- `max` / `ttl` を `Number.isFinite && > 0` で検証し、不正値は既定値へフォールバック。\n- `System` / `system` メッセージを `.system` として描画し、CSSを追加。\n- `displayColor` は `#hex` のみ適用、badge/emote URL は `http:` / `https:` のみ許可。\n- 退場アニメ時間を CSS 変数 `--leave-ms` に集約し、JSは `getComputedStyle` から取得。\n- OBS用途で不要な viewport meta を削除。\n- batch配列受信と既存の再接続バックオフ処理は維持。\n\n**検証**\n- `node --check templates/default/app.js`\n- `git diff --check -- templates/default/app.js templates/default/style.css templates/default/index.html`\n- VMスタブで URL構築、NaNフォールバック、System描画、色/URLサニタイズの実行確認済み。\ndiff --git a/templates/default/app.js b/templates/default/app.js\nindex b290518af2c9183bf0de029fa14fb65be50896d5..ac1697525bcc291262f875695ca4608b39acebd7\n--- a/templates/default/app.js\n+++ b/templates/default/app.js\n@@ -16,11 +16,12 @@\n   // ---- Config from URL params ----\n   const params = new URLSearchParams(location.search);\n   const CHANNEL_FILTER = params.get('channel') || null;\n-  const MAX_ROWS = parseInt(params.get('max') || '8', 10);\n-  const TTL_MS   = parseInt(params.get('ttl')  || '12000', 10);\n-  const WS_URL   = params.get('ws') || 'ws://127.0.0.1:11180/ws' + location.search;\n+  const MAX_ROWS = positiveIntParam('max', 8);\n+  const TTL_MS   = positiveIntParam('ttl', 12000);\n+  const WS_URL   = buildWsUrl(params.get('ws') || 'ws://127.0.0.1:11180/ws');\n \n   const overlay = document.getElementById('overlay');\n+  const LEAVE_MS = cssDurationMs('--leave-ms', 400);\n \n   // ---- Active row tracking ----\n   // Each entry: { el, timerId }\n@@ -82,11 +83,14 @@\n   function buildRow(msg) {\n     const div = document.createElement('div');\n     div.className = 'comment';\n+    const kind = msg.kind || 'normal';\n+    const isSystem = kind === 'system' || kind === 'System';\n \n     // Kind class for CSS highlights\n-    if (msg.kind === 'superChat')  div.classList.add('superchat');\n-    if (msg.kind === 'membership') div.classList.add('membership');\n-    if (msg.kind === 'bits')       div.classList.add('bits');\n+    if (kind === 'superChat')  div.classList.add('superchat');\n+    if (kind === 'membership') div.classList.add('membership');\n+    if (kind === 'bits')       div.classList.add('bits');\n+    if (isSystem) div.classList.add('system');\n \n     // Platform dot\n     const dot = document.createElement('span');\n@@ -96,7 +100,7 @@\n     // Badges\n     const badges = (msg.author && msg.author.badges) || [];\n     badges.forEach((badge) => {\n-      if (badge.imageUrl) {\n+      if (isHttpUrl(badge.imageUrl)) {\n         const img = document.createElement('img');\n         img.className = 'badge-img';\n         img.src = badge.imageUrl;\n@@ -110,10 +114,10 @@\n     const author = document.createElement('span');\n     author.className = 'author';\n     const color = msg.author && msg.author.displayColor;\n-    if (color && msg.kind === 'normal') {\n+    if (isSafeHexColor(color) && kind === 'normal') {\n       author.style.color = color;\n     }\n-    author.textContent = (msg.author && msg.author.name) || '';\n+    author.textContent = (msg.author && msg.author.name) || (isSystem ? 'System' : '');\n     div.appendChild(author);\n \n     // Separator\n@@ -137,7 +141,7 @@\n     fragments.forEach((frag) => {\n       if (frag.type === 'text') {\n         fragWrap.appendChild(document.createTextNode(frag.text));\n-      } else if (frag.type === 'emote') {\n+      } else if (frag.type === 'emote' && isHttpUrl(frag.url)) {\n         const img = document.createElement('img');\n         img.className = 'emote';\n         img.src = frag.url;\n@@ -182,10 +186,53 @@\n \n   function startLeave(el) {\n     el.classList.add('leaving');\n-    // Remove from DOM after animation (matches CSS 0.4s)\n+    // Remove from DOM after the CSS-driven exit animation.\n     setTimeout(() => {\n       if (el.parentNode) el.parentNode.removeChild(el);\n-    }, 420);\n+    }, LEAVE_MS);\n+  }\n+\n+  function positiveIntParam(name, fallback) {\n+    const raw = params.get(name);\n+    if (raw === null) return fallback;\n+\n+    const value = Number(raw);\n+    if (!Number.isFinite(value) || value <= 0) return fallback;\n+    const intValue = Math.floor(value);\n+    return intValue > 0 ? intValue : fallback;\n+  }\n+\n+  function buildWsUrl(base) {\n+    const url = new URL(base, location.href);\n+    if (CHANNEL_FILTER) {\n+      url.searchParams.set('channel', CHANNEL_FILTER);\n+    }\n+    return url.toString();\n+  }\n+\n+  function isSafeHexColor(value) {\n+    return typeof value === 'string' && /^#[0-9a-fA-F]{3,8}$/.test(value);\n+  }\n+\n+  function isHttpUrl(value) {\n+    if (typeof value !== 'string') return false;\n+\n+    try {\n+      const url = new URL(value);\n+      return url.protocol === 'http:' || url.protocol === 'https:';\n+    } catch {\n+      return false;\n+    }\n+  }\n+\n+  function cssDurationMs(name, fallback) {\n+    const value = getComputedStyle(document.documentElement).getPropertyValue(name).trim();\n+    const match = value.match(/^([0-9]*\\.?[0-9]+)(ms|s)$/);\n+    if (!match) return fallback;\n+\n+    const duration = Number(match[1]);\n+    if (!Number.isFinite(duration) || duration < 0) return fallback;\n+    return match[2] === 's' ? duration * 1000 : duration;\n   }\n \n   // ---- Start ----\ndiff --git a/templates/default/index.html b/templates/default/index.html\nindex b8f1d118231da15f84dc71fe39a4d288f0689898..75d92c72aa656a00b261450b07a87fdef4d9db66\n--- a/templates/default/index.html\n+++ b/templates/default/index.html\n@@ -2,7 +2,6 @@\n <html lang=\"ja\">\n   <head>\n     <meta charset=\"UTF-8\" />\n-    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\" />\n     <title>fast-comment overlay</title>\n     <link rel=\"stylesheet\" href=\"style.css\" />\n   </head>\ndiff --git a/templates/default/style.css b/templates/default/style.css\nindex 05692edd4b93bf60a536289f534944b8655f60f8..4c36f4d0a72bd4762c1aca8a2b3b86291f438a0d\n--- a/templates/default/style.css\n+++ b/templates/default/style.css\n@@ -1,5 +1,9 @@\n /* OBS Browser Source overlay — transparent background */\n \n+:root {\n+  --leave-ms: 400ms;\n+}\n+\n *, *::before, *::after {\n   box-sizing: border-box;\n   margin: 0;\n@@ -49,7 +53,7 @@\n \n /* Exit animation applied via JS */\n .comment.leaving {\n-  animation: fade-out 0.4s ease-in forwards;\n+  animation: fade-out var(--leave-ms) ease-in forwards;\n }\n \n @keyframes fade-slide-in {\n@@ -136,6 +140,15 @@\n   color: #fff;\n }\n \n+.comment.system {\n+  background: rgba(32, 38, 44, 0.88);\n+  color: #d6dde5;\n+  font-style: italic;\n+}\n+.comment.system .author { color: #9fb3c8; }\n+.comment.system .fragments { color: #d6dde5; }\n+.comment.system .sep { color: rgba(214,221,229,0.45); }\n+\n /* ---- Amount badge ---- */\n .amount {\n   display: inline-block;\n\ntokens used\n66,044\n"
}